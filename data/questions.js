/* 面试题库 —— window.QUESTIONS
   cat: 分类key  level: 1 基础 / 2 进阶 / 3 硬核
   a: 支持 **粗体** `代码` - 列表 与空行分段
*/
window.QCATS = [
  { key: "math",      name: "数学 & 统计",     icon: "∑",  desc: "概率、线代、优化，面试的地基" },
  { key: "ml",        name: "经典机器学习",     icon: "◇",  desc: "树模型、正则、评估指标，基本必问" },
  { key: "dl",        name: "深度学习",        icon: "◈",  desc: "反向传播、归一化、优化器、正则" },
  { key: "nlp",       name: "NLP 基础",        icon: "语", desc: "分词、词向量、序列模型" },
  { key: "llm",       name: "Transformer & LLM", icon: "⬡", desc: "注意力、位置编码、架构细节，核心高频" },
  { key: "train",     name: "训练 & 对齐",      icon: "⚙",  desc: "预训练、SFT、LoRA、RLHF/DPO" },
  { key: "rag",       name: "RAG & Agent",     icon: "⌘",  desc: "检索增强、工具调用、多智能体" },
  { key: "infra",     name: "推理 & 部署",      icon: "⚡", desc: "KV Cache、量化、vLLM、吞吐与延迟" },
  { key: "design",    name: "LLM 系统设计",     icon: "▤",  desc: "开放题，拉开候选人差距的地方" },
  { key: "code",      name: "手撕代码",         icon: "{}", desc: "白板写注意力、写训练循环" },
  { key: "bq",        name: "行为面 & 简历",    icon: "◐",  desc: "STAR、项目深挖、反问环节" }
];

window.QUESTIONS = [
/* ───────────── 数学 & 统计 ───────────── */
{id:"m1",cat:"math",level:1,tags:["概率"],q:"极大似然估计（MLE）和最大后验估计（MAP）的区别？",
a:"MLE 最大化 `P(D|θ)`，只看数据；MAP 最大化 `P(θ|D) ∝ P(D|θ)P(θ)`，多了先验。\n\n关键联系：**MAP 等价于带正则的 MLE**。高斯先验 → L2 正则（权重衰减）；拉普拉斯先验 → L1 正则（稀疏）。\n\n数据量趋于无穷时先验被淹没，MAP 收敛到 MLE。小数据时先验起作用，这就是为什么小样本任务更依赖正则和预训练。"},
{id:"m2",cat:"math",level:1,tags:["概率"],q:"交叉熵、KL 散度、困惑度（Perplexity）三者的关系？",
a:"`KL(p‖q) = H(p,q) − H(p)`。训练时真实分布 p 是 one-hot，`H(p)=0`，所以**最小化交叉熵就是最小化 KL 散度**。\n\n困惑度 `PPL = exp(平均交叉熵)`，可理解为「模型在每个位置平均纠结于多少个候选词」。PPL=10 相当于每步在 10 个词里蒙。\n\n面试补一句：KL 不对称，`KL(p‖q)` 是均值寻求（覆盖所有模式），`KL(q‖p)` 是模式寻求（塌缩到单峰）—— 这正是 RLHF 里正向/反向 KL 讨论的由来。"},
{id:"m3",cat:"math",level:2,tags:["概率"],q:"什么是偏差-方差分解？大模型时代它还成立吗？",
a:"期望误差 = 偏差² + 方差 + 噪声。欠拟合 = 高偏差，过拟合 = 高方差。\n\n经典观点是 U 型曲线：模型越大方差越大。但**双下降（Double Descent）**现象表明，参数量越过插值阈值后测试误差会再次下降，大模型反而泛化更好。\n\n答题姿势：先讲经典分解，再指出它在过参数化区间失效，归因于隐式正则（SGD 偏好平坦极小值）+ 数据规模。这个「知道经典也知道它的边界」的回答很加分。"},
{id:"m4",cat:"math",level:2,tags:["线代"],q:"SVD 是什么？它和 PCA、LoRA 有什么联系？",
a:"`A = UΣVᵀ`，任意矩阵都可分解为「旋转-缩放-旋转」。取前 k 个奇异值得到最优的秩-k 近似（Eckart–Young 定理）。\n\n- **PCA**：对中心化数据矩阵做 SVD，右奇异向量就是主成分方向。\n- **LoRA**：假设权重更新 `ΔW` 本征秩很低，于是用 `BA`（`B∈R^{d×r}`, `A∈R^{r×k}`）去近似，参数量从 `d×k` 降到 `r(d+k)`。\n\n一句话串起来：三者都是「低秩结构无处不在」这个假设的不同用法。"},
{id:"m5",cat:"math",level:1,tags:["统计"],q:"中心极限定理在机器学习里有什么实际用处？",
a:"独立同分布变量的均值近似服从正态分布。实际用途：\n\n- **AB 测试 / 模型评估**：用样本均值的正态近似算置信区间，判断 A 模型比 B 模型高的 0.3 分是不是噪声。\n- **参数初始化**：多个随机输入加权求和近似高斯，这是 Xavier/He 初始化推导方差的前提。\n- **Bootstrap**：重采样估计指标的分布，小测试集上报告 metric 时非常有用。"},
{id:"m6",cat:"math",level:3,tags:["优化"],q:"为什么深度学习里用一阶方法（SGD/Adam）而不是牛顿法？",
a:"牛顿法需要 Hessian 的逆，`d` 维参数下是 `O(d²)` 存储、`O(d³)` 求逆 —— 70B 模型直接不可能。\n\n此外非凸问题中 Hessian 可能不正定，牛顿法会走向鞍点。\n\n折中方案：L-BFGS（有限内存拟牛顿，小模型可用）、K-FAC（Kronecker 分解近似 Fisher）、以及最常见的 **Adam**（对角二阶矩近似，相当于极廉价的预条件）。Adam 实际就是「穷人的二阶方法」。"},
{id:"m7",cat:"math",level:2,tags:["优化"],q:"Adam 和 AdamW 的区别？为什么大模型都用 AdamW？",
a:"Adam 的 L2 正则是加到梯度里的：`g ← g + λθ`，然后这个惩罚项会被自适应学习率 `1/√v` 缩放，导致梯度大的参数实际受到的权重衰减更弱 —— 正则失真。\n\nAdamW 把衰减**解耦**，直接作用在参数上：`θ ← θ − lr·(m̂/(√v̂+ε) + λθ)`。\n\n结果是泛化更好、超参更好调，因此成为 Transformer 训练的默认。常见配置：`β=(0.9, 0.95)`、`wd=0.1`、余弦退火 + warmup。"},
{id:"m8",cat:"math",level:2,tags:["概率"],q:"Softmax 的温度系数 T 起什么作用？",
a:"`softmax(z/T)`。T→0 趋近 argmax（确定性、贪心）；T→∞ 趋近均匀分布（完全随机）。\n\n三个场景：\n- **采样**：低温输出稳定适合代码/抽取，高温输出发散适合创意写作。\n- **知识蒸馏**：高温软化教师分布，暴露类间相似度这些「暗知识」，注意蒸馏损失要乘 `T²` 补偿梯度缩放。\n- **校准**：温度标定（temperature scaling）是最简单有效的置信度校准手段。"},

/* ───────────── 经典机器学习 ───────────── */
{id:"ml1",cat:"ml",level:1,tags:["评估"],q:"AUC 是什么含义？什么时候它会骗人？",
a:"AUC = 随机取一个正样本和一个负样本，模型给正样本更高分的概率。它只关心**排序**，与阈值无关，对类别不平衡不敏感。\n\n骗人的情况：\n- 极度不平衡（正样本 0.1%）时 AUC 0.95 看着很美，但 PR-AUC 可能只有 0.2，实际召回可用性很差 —— 这时该看 **PR 曲线**。\n- 业务只关心 Top-K（如推荐前 10 条），应该看 `Precision@K` / `NDCG`。\n- AUC 不反映概率校准，风控/定价场景需要额外看 Brier score 或校准曲线。"},
{id:"ml2",cat:"ml",level:1,tags:["评估"],q:"精确率和召回率如何取舍？举个实际例子。",
a:"Precision = 抓对的占抓到的比例；Recall = 抓到的占该抓的比例。\n\n- **垃圾邮件过滤**重 Precision：误杀一封正常邮件代价极高。\n- **癌症筛查 / 风控**重 Recall：漏掉一个代价极高，误报可以人工复核。\n- **LLM 内容安全**通常高 Recall + 二级人工/模型复审的级联结构。\n\nF1 是调和平均，Fβ 里 β>1 偏召回。回答时一定要落到具体业务代价上，面试官想听的是权衡思维。"},
{id:"ml3",cat:"ml",level:1,tags:["树模型"],q:"GBDT 和随机森林的本质区别？",
a:"- **随机森林 = Bagging**：并行训练多棵深树，行列双重采样，投票平均。主要降**方差**，不容易过拟合，好调参。\n- **GBDT = Boosting**：串行训练浅树，每棵拟合前面残差（负梯度）。主要降**偏差**，精度更高，但对超参和噪声敏感。\n\n工程上：RF 可完全并行、适合快速 baseline；GBDT（XGBoost/LightGBM/CatBoost）是表格数据的 SOTA。**结构化数据上 GBDT 至今仍常打败神经网络**，这个认知在面试里很实在。"},
{id:"ml4",cat:"ml",level:2,tags:["树模型"],q:"XGBoost 相比传统 GBDT 做了哪些改进？",
a:"1. **二阶泰勒展开**：同时用一阶和二阶梯度，逼近更准、收敛更快。\n2. **正则项进目标函数**：叶子数 `γT` + 叶权重 `λ‖w‖²`，从公式层面控制复杂度。\n3. **列采样**：借鉴随机森林，进一步降方差。\n4. **稀疏感知**：为缺失值学习默认分裂方向，不用填充。\n5. **近似分位数分裂 + 块存储**：支持并行找分裂点和外存训练。\n\nLightGBM 再往前一步：直方图算法 + leaf-wise 生长 + GOSS/EFB，速度快一个量级。"},
{id:"ml5",cat:"ml",level:1,tags:["正则"],q:"L1 和 L2 正则为什么一个稀疏一个不稀疏？",
a:"几何解释：L1 约束区域是菱形（有尖角），等高线更容易切在坐标轴的尖角上 → 某些权重恰好为 0。L2 是圆形，切点一般不在轴上 → 权重小但非零。\n\n梯度解释：L1 的梯度是常数 `λ·sign(w)`，无论 w 多小都在往 0 推；L2 梯度 `2λw` 随 w 变小而变弱，永远推不到 0。\n\n贝叶斯解释：L1 ↔ 拉普拉斯先验，L2 ↔ 高斯先验。Elastic Net 是两者结合，处理共线性特征时更稳。"},
{id:"ml6",cat:"ml",level:2,tags:["评估"],q:"数据不平衡（1:1000）怎么处理？",
a:"分层次回答：\n\n**数据层**：欠采样多数类（可能丢信息，配合 EasyEnsemble）、过采样少数类（SMOTE 合成，注意别在划分前做，会泄漏）。\n\n**算法层**：类别加权（`class_weight`）、Focal Loss（降低易分样本权重）、调整决策阈值而非默认 0.5。\n\n**评估层**：别看 Accuracy，用 PR-AUC、`Recall@固定Precision`。\n\n**问题重构**：极端情况下（1:10⁶）当成**异常检测**做，用 Isolation Forest / AutoEncoder 重构误差。\n\n加分项：说明先试类别加权 + 调阈值这种低成本手段，别一上来就 SMOTE。"},
{id:"ml7",cat:"ml",level:2,tags:["特征"],q:"什么是数据泄漏（Data Leakage）？举三个真实场景。",
a:"训练时用到了预测时拿不到的信息，导致线下指标虚高、线上崩盘。\n\n1. **预处理泄漏**：在全量数据上 fit 标准化/PCA/SMOTE 后再划分训练测试。正确做法是在训练折上 fit，用 Pipeline 封装。\n2. **时间泄漏**：随机划分时序数据，用未来预测过去。必须按时间切分，做 walk-forward 验证。\n3. **标签泄漏**：特征里混进了标签的下游结果，比如用「是否已赔付」预测「是否出险」。\n\nLLM 时代还有第四种：**测试集污染** —— 评测集出现在预训练语料里，这是现在刷榜争议的核心。"},
{id:"ml8",cat:"ml",level:2,tags:["无监督"],q:"K-means 的假设和缺陷是什么？如何选 K？",
a:"假设：簇是各向同性的球形、大小相近、用欧氏距离可分。因此对非凸形状（月牙、环）、密度不均、离群点都很脆弱。\n\n选 K 的方法：肘部法（SSE 拐点，主观）、**轮廓系数**（越接近 1 越好，更可靠）、Gap Statistic、或者直接用业务约束（要分 5 个用户层就 K=5）。\n\n替代：DBSCAN 处理任意形状和噪声；GMM 是软分配、允许椭圆簇；层次聚类可看树状图。LLM 场景下常在 embedding 上做聚类，注意先降维（UMAP）再聚，高维欧氏距离会退化。"},

/* ───────────── 深度学习 ───────────── */
{id:"d1",cat:"dl",level:1,tags:["基础"],q:"梯度消失和梯度爆炸的原因与解法？",
a:"链式法则中梯度是连乘，`<1` 的因子连乘趋于 0，`>1` 趋于无穷。\n\n**解法（按重要性）**：\n- 残差连接：给梯度一条恒等高速路，是深层网络能训起来的第一功臣。\n- 归一化：LayerNorm/BatchNorm 稳住激活分布。\n- 激活函数：ReLU 系列替代 Sigmoid/Tanh（后者饱和区导数趋 0）。\n- 合理初始化：Xavier / He，保持前向后向方差稳定。\n- 梯度裁剪：主要治爆炸，Transformer 训练标配 `clip_grad_norm=1.0`。"},
{id:"d2",cat:"dl",level:1,tags:["归一化"],q:"BatchNorm 和 LayerNorm 的区别？为什么 Transformer 用 LayerNorm？",
a:"BN 沿 batch 维归一化（对每个通道统计 batch 内均值方差），LN 沿特征维归一化（对每个样本独立统计）。\n\nTransformer 用 LN 的原因：\n1. **变长序列**：NLP 里 padding 会污染 batch 统计量。\n2. **batch size 小/变化**：大模型训练常用极小 micro-batch，BN 统计不稳。\n3. **推理一致性**：LN 训练和推理行为完全相同，BN 要维护滑动均值，自回归逐 token 生成时很别扭。\n\n现在主流是 **RMSNorm**：去掉减均值只做缩放，`x/RMS(x)·g`，效果相当但快 10~15%，LLaMA 系列全用它。"},
{id:"d3",cat:"dl",level:2,tags:["归一化"],q:"Pre-LN 和 Post-LN 的区别？为什么大模型都转向 Pre-LN？",
a:"- **Post-LN**（原始 Transformer）：`x + Sublayer(x)` 之后再 LN。表达能力略强，但深层时梯度不稳，**必须配 warmup**，否则训练直接发散。\n- **Pre-LN**：`x + Sublayer(LN(x))`。残差主干是干净的恒等路径，梯度可以直通到底，训练极稳，对 warmup 不敏感。\n\n代价：Pre-LN 深层时残差流方差累积增大，最终效果略逊，所以要在最后加一个 final LN。\n\n70B+ 的模型训练成本极高，**稳定性压倒一切**，所以 GPT/LLaMA 全系 Pre-LN。折中方案有 DeepNorm、Sandwich-LN。"},
{id:"d4",cat:"dl",level:1,tags:["正则"],q:"Dropout 的原理是什么？推理时怎么处理？为什么大模型少用了？",
a:"训练时以概率 p 随机置零神经元，迫使网络不依赖特定单元，相当于对指数多个子网络做集成。\n\n推理时不丢弃。实现上用 **Inverted Dropout**：训练时保留的激活除以 `(1−p)`，推理时原样前向，省去缩放。\n\n大模型少用的原因：预训练数据量极大（万亿 token），**单 epoch 甚至不到一遍数据，过拟合根本不是矛盾**，Dropout 反而拖慢收敛。所以 LLaMA 预训练 dropout=0，只在小数据微调时开一点。"},
{id:"d5",cat:"dl",level:2,tags:["优化"],q:"学习率 warmup 为什么必要？常见调度策略有哪些？",
a:"训练初期参数随机、梯度方向噪声大，且 Adam 的二阶矩 `v` 估计样本少、不可靠（`1/√v` 可能爆炸）。大学习率会把模型推到坏区域甚至 NaN。Warmup 用前 0.5~2% 步数线性升到峰值，让统计量稳定下来。\n\n常见调度：\n- **线性 warmup + 余弦退火**：LLM 预训练事实标准，末端衰减到峰值的 10%。\n- **WSD（Warmup-Stable-Decay）**：中间长期恒定，最后快速退火，好处是可以随时从稳定段续训，MiniCPM/DeepSeek 用得多。\n- Inverse sqrt：原始 Transformer 论文用法。"},
{id:"d6",cat:"dl",level:2,tags:["激活"],q:"从 ReLU 到 SwiGLU，激活函数演进的逻辑是什么？",
a:"- **ReLU**：解决梯度消失，但有 Dead ReLU（负区间梯度恒 0）。\n- **GELU**：`x·Φ(x)`，平滑且带随机正则解释，BERT/GPT-2 采用。\n- **Swish/SiLU**：`x·σ(x)`，与 GELU 极接近但计算更省。\n- **GLU 家族**：引入门控 `(xW)⊗σ(xV)`，用一路控制另一路的信息流。\n- **SwiGLU** = Swish + GLU：`Swish(xW)⊗(xV)`，多一个矩阵所以把隐层维度按 `2/3` 缩放保持参数量持平。\n\nLLaMA/PaLM 实测 SwiGLU 在同等参数下 loss 更低。Noam Shazeer 论文原话大意是「效果好，原因归于玄学」—— 这个梗面试说出来会显得你读过原文。"},
{id:"d7",cat:"dl",level:2,tags:["训练"],q:"混合精度训练是怎么回事？FP16 和 BF16 怎么选？",
a:"前向后向用低精度算（省显存、用上 Tensor Core），权重主副本和优化器状态用 FP32 保存。\n\n- **FP16**：5 位指数、10 位尾数。动态范围窄（最小约 6e-5），小梯度会下溢成 0，**必须配 Loss Scaling**（放大 loss，反传后再缩回）。\n- **BF16**：8 位指数、7 位尾数，动态范围和 FP32 一样，不需要 loss scaling，训练稳得多；代价是精度低，但深度学习对精度不敏感、对范围敏感。\n\n结论：A100/H100 上一律 **BF16**；只有老卡（V100/T4）不支持 BF16 才用 FP16。FP8 是 H100 起的新选项，需要更精细的 per-tensor scaling。"},
{id:"d8",cat:"dl",level:3,tags:["训练"],q:"梯度检查点（Gradient Checkpointing）是用什么换什么？",
a:"用**计算换显存**。正常反传需要缓存所有中间激活，显存 `O(L)`（L 为层数）。梯度检查点只保存少数几层的激活，反传时重新前向计算中间结果。\n\n经典策略：每 `√L` 层存一个检查点，显存降到 `O(√L)`，代价是多一次前向，约 **+30% 训练时间**。\n\n这是显存不够时的第一张牌，通常和 ZeRO、序列并行一起用。面试时能报出「30% 时间换 60~70% 激活显存」这种量级，比空谈概念强。"},

/* ───────────── NLP 基础 ───────────── */
{id:"n1",cat:"nlp",level:1,tags:["分词"],q:"BPE 分词的原理是什么？为什么 LLM 都用子词分词？",
a:"BPE：从字符级词表开始，反复统计语料中最高频的相邻符号对，合并成新符号，直到达到目标词表大小（如 32k/128k）。\n\n用子词的原因：\n- **词级**：词表爆炸、OOV 问题严重（新词、拼写错误直接变 UNK）。\n- **字符级**：序列太长，注意力 `O(n²)` 吃不消，语义稀薄。\n- **子词**：常见词保留完整，罕见词拆成有意义的片段（`unhappiness → un + happi + ness`），词表可控且零 OOV。\n\n变体：WordPiece（按似然增益而非频率合并，BERT）、Unigram LM（SentencePiece 默认，概率式剪枝）、BBPE（字节级 BPE，天然支持任意 Unicode，GPT 系用）。"},
{id:"n2",cat:"nlp",level:2,tags:["分词"],q:"分词器会带来哪些实际问题？",
a:"1. **多语言不公平**：中文/泰文等每字可能占 2~3 个 token，同样内容中文比英文贵一倍以上，还更容易触顶上下文。\n2. **数字处理差**：`1234` 可能被拆成 `12|34`，这是 LLM 算术能力弱的重要原因之一。所以 LLaMA 把数字逐位拆分。\n3. **Glitch Token**：`SolidGoldMagikarp` 这类在词表中但训练语料几乎没出现的 token，会让模型行为异常。\n4. **反转字符串 / 数字母**（比如 strawberry 里有几个 r）失败，因为模型看到的是 token 不是字符。\n5. **代码缩进**：GPT-2 分词器对连续空格处理低效，后来专门加了缩进 token。"},
{id:"n3",cat:"nlp",level:1,tags:["词向量"],q:"Word2Vec、GloVe 和 BERT 的词表示有什么本质区别？",
a:"Word2Vec/GloVe 是**静态**词向量：每个词一个固定向量，「苹果」在「吃苹果」和「苹果发布会」里完全相同，无法处理一词多义。\n\nBERT 是**上下文相关**表示：同一个词在不同句子中向量不同，由整句注意力计算得出。\n\n训练目标也不同：Word2Vec（Skip-gram + 负采样）预测邻近词，GloVe 拟合全局共现矩阵的对数，BERT 用 MLM（掩码语言模型）双向预测被遮住的词。\n\n一句话总结演进：从「词典查表」到「按语境现场生成」。"},
{id:"n4",cat:"nlp",level:2,tags:["模型"],q:"BERT 和 GPT 的核心差异？各自适合什么任务？",
a:"| | BERT | GPT |\n|---|---|---|\n|结构|Encoder，双向注意力|Decoder，因果掩码单向|\n|目标|MLM + NSP|Next Token Prediction|\n|擅长|理解类：分类、NER、检索|生成类：写作、对话、代码|\n\nBERT 看得到全文所以理解更强，但不能自然生成；GPT 只能看左边，但天然适合自回归生成，且**生成式统一了所有任务形式**（把分类写成填空），加上规模效应，最终 Decoder-only 成为主流。\n\n现在仍在用 BERT 系（如 BGE、E5）的地方：**embedding 模型和 reranker**，因为双向编码做句向量确实更强、更省。"},
{id:"n5",cat:"nlp",level:1,tags:["评估"],q:"BLEU、ROUGE、BERTScore 各自衡量什么？LLM 时代怎么评估？",
a:"- **BLEU**：n-gram 精确率 + 长度惩罚，机器翻译常用，偏重「说的词对不对」。\n- **ROUGE**：n-gram 召回率（ROUGE-L 用最长公共子序列），摘要常用，偏重「该说的说全没」。\n- **BERTScore**：用预训练模型的 embedding 算语义相似度，能识别同义改写。\n\n它们对开放式生成都很差（一个好回答可以和参考完全不同词）。现在主流是：\n1. **LLM-as-Judge**（GPT-4 打分，注意位置偏见、长度偏见，需要随机化顺序）；\n2. **人工 Pairwise 对比 + Elo**（Chatbot Arena 模式）；\n3. **可验证任务用执行结果**（代码跑单测、数学对答案）—— 这是最可靠的。"},

/* ───────────── Transformer & LLM ───────────── */
{id:"t1",cat:"llm",level:1,tags:["注意力"],q:"讲清楚 Self-Attention 的计算过程，以及为什么要除以 √d_k。",
a:"`Attention(Q,K,V) = softmax(QKᵀ/√d_k)V`。\n\n流程：输入 X 经三个线性层得到 Q/K/V → `QKᵀ` 算 token 两两相似度 → 缩放 → （因果掩码）→ softmax 归一化成权重 → 加权求和 V。\n\n**除以 √d_k 的原因**：假设 q、k 各分量独立、均值 0 方差 1，点积 `q·k` 的方差是 `d_k`。d_k=128 时点积标准差约 11，数值一大 softmax 就进入饱和区，输出接近 one-hot，**梯度趋近于 0**。除以 √d_k 把方差拉回 1，保持 softmax 在敏感区间。\n\n这题几乎必问，一定要能把方差推导讲出来，不能只说「为了稳定」。"},
{id:"t2",cat:"llm",level:1,tags:["注意力"],q:"为什么需要多头注意力？头数是不是越多越好？",
a:"单头 softmax 后注意力分布往往集中在少数位置，表达受限。多头把 `d_model` 切成 h 份并行做注意力，每个头在**不同子空间**学不同关系 —— 有的头学句法依存，有的学指代消解，有的只盯着相邻 token（归纳头 Induction Head 是上下文学习的关键机制）。\n\n注意总参数量不变（切分而非复制），所以近乎免费。\n\n不是越多越好：头数太多则每个头维度 `d_k = d_model/h` 太小，表达能力不足。经验值 `d_k` 保持在 64~128。而且论文《Are Sixteen Heads Really Better than One?》显示，推理时剪掉大部分头性能几乎不掉 —— 说明存在大量冗余。"},
{id:"t3",cat:"llm",level:2,tags:["位置编码"],q:"RoPE 旋转位置编码的原理和优势？",
a:"不加位置向量，而是把 Q、K 按位置 m 旋转一个角度：把向量两两配对看成复数，乘以 `e^{imθ}`。\n\n关键性质：`⟨R_m q, R_n k⟩` 只依赖**相对位置 `m−n`**。即绝对方式实现，相对效果达成。\n\n优势：\n1. 相对位置感知，且不增加参数。\n2. **可外推 / 易扩展**：NTK-aware 插值、YaRN 只需改 `θ` 的基频就能把 4k 上下文扩到 128k，不用重训 —— 这是它压倒性胜出的原因。\n3. 远程衰减：高频维度随距离自然衰减，符合语言的局部性先验。\n4. 兼容 FlashAttention 和 KV Cache（旋转可在缓存前完成）。\n\n对比：绝对正弦编码外推差；可学习位置编码完全无法外推；ALiBi 外推好但长文召回弱。"},
{id:"t4",cat:"llm",level:2,tags:["架构"],q:"MHA、MQA、GQA 的区别？为什么现在都用 GQA？",
a:"区别在 K/V 头的数量：\n- **MHA**：n 个 Q 头配 n 组 KV。效果最好，KV Cache 最大。\n- **MQA**：n 个 Q 头共享 **1 组** KV。缓存减少 n 倍，但质量掉点明显、训练不稳。\n- **GQA**：分 g 组，每组共享一份 KV（如 32 头分 8 组）。\n\n动机是**推理瓶颈在显存带宽而非算力**：自回归解码每生成一个 token 都要读一遍全部 KV Cache，缓存越大越慢。GQA 把缓存降 4~8 倍，质量几乎无损，是完美折中，LLaMA-2 70B 起成为标配。\n\n更激进的是 DeepSeek 的 **MLA**：把 KV 压缩成低秩潜向量存储，缓存更小且效果反超 MHA。"},
{id:"t5",cat:"llm",level:2,tags:["架构"],q:"FFN（前馈层）的作用是什么？为什么中间维度是 4 倍？",
a:"注意力负责 token 之间**混合信息**，FFN 负责在每个位置上**逐 token 变换和存储知识**。研究（Geva et al.）表明 FFN 可视为 key-value 记忆：第一层矩阵匹配模式，第二层输出对应的知识向量。**大模型的事实知识主要存在 FFN 里**，参数量也占 2/3。\n\n4 倍是经验值：升维到高维空间做非线性分离再降回来，类似 SVM 核技巧的直觉。太小则容量不足，太大则收益递减且显存吃紧。\n\n用 SwiGLU 时因为多一个门控矩阵，通常取 `8/3 ≈ 2.67` 倍再对齐到 128 的倍数，保持总参数量不变。"},
{id:"t6",cat:"llm",level:3,tags:["架构"],q:"MoE（混合专家）是怎么工作的？主要工程挑战是什么？",
a:"把 FFN 替换成 N 个专家 + 一个路由器，每个 token 只激活 Top-k（通常 1~2）个专家。于是**总参数量大、激活参数量小**：Mixtral 8x7B 总参 47B，推理只激活 13B。\n\n挑战：\n1. **负载不均衡**：路由容易塌缩到少数热门专家。解法是辅助的 load-balancing loss、专家容量上限（超出就丢弃 token），DeepSeek 用无辅助损失的偏置调整法。\n2. **通信开销**：专家分布在不同卡上，需要 All-to-All 通信，是分布式训练的瓶颈。\n3. **显存**：虽然激活少，但**全部专家都要放进显存**，部署成本并不低。\n4. **微调易过拟合**，且路由不稳定。\n\n趋势：细粒度专家 + 共享专家（DeepSeekMoE），让专家更专一同时保留公共知识。"},
{id:"t7",cat:"llm",level:2,tags:["生成"],q:"Top-k、Top-p、温度这些解码参数分别怎么影响输出？",
a:"- **温度 T**：缩放 logits，控制整体随机性（见 softmax 温度题）。\n- **Top-k**：只在概率最高的 k 个词里采样。缺点是 k 固定 —— 分布很尖时会引入垃圾候选，很平时又砍掉合理候选。\n- **Top-p（核采样）**：按概率从高到低累加到 p（如 0.9）为止，候选集**动态调整**，比 Top-k 更合理，是当前默认。\n- **重复惩罚 / presence penalty**：压制复读。\n- **Beam Search**：在翻译等有唯一优解的任务上好，但开放生成里会导致乏味重复（因为高概率序列往往平淡）。\n\n实务建议：代码/抽取/函数调用用 `T=0`（贪心）保证可复现；对话用 `T=0.7, top_p=0.9`；创意写作 `T=1.0+`。"},
{id:"t8",cat:"llm",level:2,tags:["能力"],q:"什么是 Scaling Law？Chinchilla 结论改变了什么？",
a:"Kaplan 等发现 loss 与参数量 N、数据量 D、算力 C 呈**幂律关系**，且在多个数量级上光滑可预测 —— 这让「先用小模型拟合曲线，再预测大模型效果」成为可能，是敢砸几千万训练的底气。\n\n**Chinchilla（2022）修正**：在固定算力下，此前的模型（如 GPT-3 175B 只训 300B token）都**严重训练不足**。最优配比约为 **每参数 20 token**。70B 的 Chinchilla 打败了 280B 的 Gopher。\n\n后续演变：考虑**推理成本**后，业界普遍「过度训练」小模型 —— LLaMA-3 8B 用了 15T token，远超 20:1，因为训练是一次性成本而推理是永久成本。\n\n当前焦点已转向数据质量、合成数据，以及**推理时扩展**（test-time scaling，即 o1 式的长思维链）。"},
{id:"t9",cat:"llm",level:3,tags:["能力"],q:"为什么会出现涌现能力（Emergent Abilities）？有争议吗？",
a:"现象：某些能力（多步推理、指令跟随）在模型小于某个规模时接近随机，超过阈值后突然出现。\n\n主流解释：复杂任务需要多个子能力**同时**达标，每个子能力平滑提升，但联合成功率呈非线性跃升。\n\n**争议**（Schaeffer et al. NeurIPS 2023 最佳论文）：涌现可能是**度量方式造成的幻觉**。用「完全匹配」这种非线性指标会看到突变，换成 token 级编辑距离等连续指标，曲线是平滑的。\n\n面试答这题时把两面都讲，并指出实践含义：**评估指标选择会影响你对模型能力的判断**，做小模型实验时应该用连续指标才能看到进步。"},
{id:"t10",cat:"llm",level:2,tags:["能力"],q:"上下文学习（ICL）的机制是什么？为什么不用更新参数就能学？",
a:"给几个示例，模型就能完成新任务。主流假说：\n\n1. **隐式梯度下降**：注意力的前向计算在数学上可类比一步梯度更新，Transformer 相当于在前向过程中做了「元优化」。\n2. **任务定位说**（更被认可）：示例并没有教会模型新知识，而是**从预训练中已有的能力里定位出目标任务**。证据：把示例的标签随机打乱，性能下降有限 —— 说明格式和标签空间比正确性更重要。\n3. **归纳头（Induction Head）**：模型内部形成「看到 [A][B]…[A] 就预测 [B]」的电路，是 ICL 的底层机制，在训练中会突然相变出现。\n\n实践推论：示例的**格式、分布、多样性**比标签正确性更关键；示例顺序有影响（近因偏差），重要的放最后。"},

/* ───────────── 训练 & 对齐 ───────────── */
{id:"f1",cat:"train",level:1,tags:["范式"],q:"完整讲一遍 LLM 的训练流程（预训练 → 对齐）。",
a:"1. **预训练**：万亿级 token 自监督预测下一个词。学到语言、世界知识、基本推理。成本占 99%。\n2. **继续预训练（可选）**：注入领域语料（医疗/法律/代码）或扩展语言、扩上下文窗口。\n3. **SFT（监督微调）**：用几万到几十万条高质量「指令-回答」对，教会模型**遵循指令的格式**，把补全器变成助手。质量远比数量重要（LIMA 用 1000 条就有效）。\n4. **偏好对齐**：RLHF（奖励模型 + PPO）或 DPO/GRPO 等。教会模型**人类偏好哪种回答**——有用、无害、诚实。\n5. **推理强化（新范式）**：在可验证任务（数学/代码）上用 RL 训练长思维链，即 o1/R1 路线。\n\n一句话记忆：预训练给知识，SFT 给格式，RLHF 给品味，RL-reasoning 给思考。"},
{id:"f2",cat:"train",level:2,tags:["微调"],q:"LoRA 的原理是什么？rank 和 alpha 怎么设？",
a:"冻结原权重 W，旁路加低秩分解 `ΔW = BA`，前向变成 `h = Wx + (α/r)·BAx`。A 用高斯初始化，**B 初始化为 0**，保证训练开始时等价于原模型。\n\n参数量从 `d×k` 降到 `r(d+k)`，通常只有 0.1~1%。显存省的大头其实是**优化器状态**（Adam 的 m、v 各占参数量一份 FP32）。\n\n超参经验：\n- `r`：简单任务 8~16，复杂/领域迁移 32~64。再大收益递减。\n- `alpha`：常取 `2r`（缩放系数 2），或固定 16/32 后只调 r。\n- **作用模块**：只加 q/v 是最初做法，实践中**加到所有线性层（含 FFN）效果明显更好**。\n- 学习率比全参微调大一个量级，`1e-4 ~ 3e-4`。\n\n推理时可 `W' = W + BA` 合并，**零额外延迟**；也可保留多个 adapter 动态切换（多租户服务的常见做法）。"},
{id:"f3",cat:"train",level:2,tags:["微调"],q:"QLoRA 在 LoRA 基础上做了什么？",
a:"目标是在单张 24G 消费卡上微调 65B 模型。三个关键技术：\n\n1. **NF4 量化**：4-bit NormalFloat，基于「预训练权重近似正态分布」设计的信息论最优量化区间，比 int4 精度高。基座模型以 NF4 存储，计算时反量化成 BF16。\n2. **双重量化**：连量化常数本身也量化（每参数再省约 0.37 bit）。\n3. **Paged Optimizer**：利用 NVIDIA 统一内存，显存峰值时把优化器状态换页到 CPU，避免 OOM 崩溃。\n\n注意：LoRA 适配器仍是 BF16 的，梯度只流经适配器。代价是**速度比 LoRA 慢约 30%**（反量化开销），但换来了「能跑」。\n\n面试补充：想要推理也快，应该训完合并后再用 AWQ/GPTQ 重新量化，而不是直接部署 NF4。"},
{id:"f4",cat:"train",level:3,tags:["对齐"],q:"RLHF 的完整流程？PPO 在这里的四个模型分别是什么？",
a:"**流程**：SFT 模型 → 收集人类对回答的偏好排序 → 训练奖励模型（RM，Bradley-Terry 损失 `−log σ(r_chosen − r_rejected)`）→ 用 PPO 优化策略最大化奖励。\n\n**四个模型同时在显存里**：\n1. **Actor**：正在训练的策略模型。\n2. **Critic**：价值网络，估计状态价值以降低方差（GAE）。\n3. **Reward Model**：给完整回答打分，冻结。\n4. **Reference Model**：SFT 初始模型，冻结，用于算 KL 惩罚。\n\n目标函数：`奖励 − β·KL(π‖π_ref)`。KL 项防止**奖励攻陷（reward hacking）**—— 模型发现输出一堆「谢谢您的提问」就能骗高分，然后彻底跑偏。\n\n痛点：四模型显存爆炸、超参极其敏感、训练不稳。这正是 DPO 流行的原因。"},
{id:"f5",cat:"train",level:2,tags:["对齐"],q:"DPO 相比 PPO 好在哪？它有什么局限？",
a:"DPO 的洞见：RLHF 的最优策略与奖励函数有闭式对应关系，于是可以把奖励模型**解析地消掉**，直接用偏好数据做分类式的监督训练：\n\n`L = −log σ(β[log π(y_w|x)/π_ref(y_w|x) − log π(y_l|x)/π_ref(y_l|x)])`\n\n好处：只需 2 个模型（策略 + 参考）、无需采样、稳定、代码量少一个量级。\n\n**局限**（能讲出来是加分项）：\n- **离线**：只在固定数据集上学，无法像 PPO 那样在线探索新回答，容易过拟合偏好数据的分布。\n- 会同时降低 chosen 和 rejected 的概率（把概率质量推给了分布外的回答）。\n- 对偏好数据噪声敏感，没有 RM 的平滑作用。\n\n后续：IPO（防过拟合）、KTO（只需好坏二元标签，不用成对）、SimPO（去掉参考模型）、**GRPO**（DeepSeek：组内多次采样用相对优势替代 Critic，省掉价值网络，已成推理模型训练主流）。"},
{id:"f6",cat:"train",level:2,tags:["微调"],q:"什么是灾难性遗忘？微调时如何缓解？",
a:"在新任务上微调后，模型丢失原有的通用能力（典型现象：微调完专业问答，模型不会聊天了、中文能力退化）。\n\n缓解手段：\n1. **混入通用数据**：SFT 数据里掺 10~30% 的通用指令数据，最简单最有效。\n2. **参数高效微调**：LoRA 冻结主干，天然遗忘更少。\n3. **小学习率 + 少 epoch**：SFT 通常 1~3 epoch，`lr=1e-5`（全参）就够，训多了必崩。\n4. **正则约束**：加对原模型输出的 KL 惩罚，或 EWC（按 Fisher 信息保护重要参数）。\n5. **回放（Replay）**：保留部分预训练数据继续训。\n\n评估上必须建**回归测试集**：微调后同时跑领域指标和通用榜（MMLU、中文能力），别只看目标任务涨了就上线。"},
{id:"f7",cat:"train",level:2,tags:["数据"],q:"构建 SFT 数据集时最需要注意什么？",
a:"核心认知：**质量 >> 数量**。LIMA 证明 1000 条精心标注的数据就能激发出很好的对话能力，因为 SFT 是在「激活」预训练已有的能力，不是灌输新知识。\n\n要点：\n- **多样性**：任务类型、长度、难度、领域要覆盖广，去重（MinHash / embedding 聚类）很关键。\n- **格式一致**：统一的 chat template，system prompt 的使用方式要和线上一致。\n- **回答风格统一**：不要混入风格迥异的数据，否则模型输出不稳定。\n- **难度匹配**：别用超出模型能力的数据（比如让 7B 学 GPT-4 的复杂推理），会导致模型学会「一本正经地编」——**幻觉往往是 SFT 数据教出来的**。\n- **Loss Mask**：只在回答部分算 loss，prompt 部分要 mask 掉。这个实现细节面试常考。\n- **多轮对话**：注意只对每轮的 assistant 部分算 loss，可以一次前向算多轮。"},

/* ───────────── RAG & Agent ───────────── */
{id:"r1",cat:"rag",level:1,tags:["RAG"],q:"RAG 的完整链路是什么？每一步有哪些优化点？",
a:"**离线**：文档解析 → 切块 → 向量化 → 建索引。\n**在线**：查询改写 → 检索 → 重排 → 拼 Prompt → 生成 → 引用标注。\n\n各环节优化：\n- **解析**：PDF 表格/公式是重灾区，用版面分析模型（如 PP-Structure）而非纯文本提取。\n- **切块**：语义切分优于固定长度；保留标题层级作为上下文；块间重叠 10~20%；**Small-to-Big**（用小块检索，喂大块给模型）。\n- **检索**：向量 + BM25 **混合检索**（RRF 融合）几乎总是比单一方式好，关键词能兜住专有名词。\n- **重排**：Cross-Encoder reranker，召回 50 → 精排留 5，这一步性价比最高。\n- **生成**：要求引用来源，检索不到时明确说不知道。"},
{id:"r2",cat:"rag",level:2,tags:["RAG"],q:"RAG 效果不好，你如何系统地定位问题？",
a:"先**拆解归因**，别一上来就换模型。构造带标注的评测集，分段量化：\n\n1. **召回率（Recall@k）**：正确文档进候选集了吗？没有 → 问题在切块/embedding/查询改写。\n2. **排序质量（MRR/NDCG）**：正确文档排到前列了吗？没有 → 加 reranker。\n3. **上下文利用**：正确答案在 context 里但模型没用对？→ 检查「中间遗失」（lost in the middle，重要文档放首尾）、prompt 设计、上下文过长。\n4. **忠实度（Faithfulness）**：模型编造了 context 里没有的内容？→ 加约束、降温度、换更强模型。\n\n工具：RAGAS 框架提供 faithfulness / answer_relevancy / context_precision 等指标。\n\n**这题是 RAG 岗位的分水岭**，能讲出分段归因说明你真做过线上系统。"},
{id:"r3",cat:"rag",level:2,tags:["检索"],q:"向量检索的索引结构有哪些？HNSW 和 IVF 怎么选？",
a:"暴力检索 `O(N)` 在百万级以上不可行，需要 ANN（近似最近邻）。\n\n- **HNSW**（分层可导航小世界图）：多层跳表式图结构，上层稀疏长边快速定位，下层稠密精找。**查询快、召回高**，但内存占用大、构建慢、不支持高效删除。\n- **IVF**（倒排文件）：先 k-means 聚成 nlist 个簇，查询时只搜最近的 nprobe 个簇。内存友好、构建快，召回率靠 nprobe 调节。\n- **PQ（乘积量化）**：把向量切段分别量化，压缩 10~50 倍，常与 IVF 组合成 `IVF-PQ` 用于亿级数据。\n\n选型：**千万以下、内存够 → HNSW**（默认首选，Qdrant/Milvus/pgvector 都支持）；**亿级、成本敏感 → IVF-PQ**；数据频繁更新 → 考虑 IVF 或带增量合并的方案。"},
{id:"r4",cat:"rag",level:2,tags:["Agent"],q:"Agent 的基本构成是什么？ReAct 范式怎么工作？",
a:"四要素：**规划（Planning）+ 记忆（Memory）+ 工具（Tools）+ 执行循环**。\n\n**ReAct** = Reasoning + Acting，交替进行：\n```\nThought: 我需要先查一下今天的汇率\nAction: search(\"USD CNY rate\")\nObservation: 7.12\nThought: 现在可以计算了\nAction: calculator(\"1000*7.12\")\n...\nFinal Answer: ...\n```\n关键在于**每次行动后把真实观测结果喂回去**，让模型基于事实而非想象继续推理，大幅降低幻觉。\n\n工程要点：设最大迭代轮数防死循环；工具返回要截断（否则撑爆上下文）；工具调用失败要有错误信息回传让模型自我修正；现在优先用模型原生的 **function calling / tool use** 接口而不是解析文本。"},
{id:"r5",cat:"rag",level:3,tags:["Agent"],q:"Agent 系统在生产环境中最大的挑战是什么？",
a:"1. **错误累积**：每步 95% 成功率，10 步就只剩 60%。解法：减少步数、关键步骤加校验、允许回溯重试。\n2. **成本与延迟**：多轮调用 + 长上下文，一次任务可能几十次 LLM 调用。解法：小模型做路由/简单步骤，缓存，并行化独立子任务。\n3. **可靠性与可观测**：必须有完整 trace（LangSmith/Langfuse），否则线上出问题无法定位。\n4. **安全边界**：工具有副作用（发邮件、删数据、下单）。必须做**权限最小化 + 危险操作人工确认 + 沙箱执行**。\n5. **提示注入**：检索到的网页/文档里藏着「忽略之前的指令」。解法是把外部内容明确标记为数据、输出做校验、工具权限隔离。\n\n面试时强调第 4、5 点会显得你有生产意识 —— 大多数候选人只会讲 ReAct 论文。"},
{id:"r6",cat:"rag",level:2,tags:["RAG"],q:"长上下文模型（1M token）出现后，RAG 还有必要吗？",
a:"仍然必要，理由是四个维度：\n\n- **成本**：1M token 输入每次调用极贵，RAG 只送 4k 相关内容，差两三个数量级。\n- **延迟**：长上下文的 prefill 时间随长度增长，首 token 延迟不可接受。\n- **规模**：企业知识库是 TB 级，再长的窗口也放不下。\n- **准确性**：长上下文存在「中间遗失」，海量无关信息会干扰；且 RAG 天然提供**可溯源引用**，这在金融/医疗/法务是合规硬需求。\n- **时效**：知识更新只需重建索引，不用重训。\n\n但长上下文确实改变了 RAG 的形态：可以少切块、多召回（送 20 个块而不是 3 个）、用上下文缓存（prompt caching）降本。**两者是互补而非替代**。"},

/* ───────────── 推理 & 部署 ───────────── */
{id:"i1",cat:"infra",level:1,tags:["推理"],q:"KV Cache 是什么？为什么它是推理优化的核心？",
a:"自回归生成时，每生成一个 token 都要对全部历史做注意力。由于因果掩码，历史 token 的 K、V 不会变，缓存起来就不必重算，把每步复杂度从 `O(n²)` 降到 `O(n)`。\n\n**代价是显存**：`2 × 层数 × 头数 × 头维度 × 序列长度 × batch × 字节数`。\n举例 LLaMA-7B（32层、32头、128维、FP16）：每 token 约 **512 KB**，2048 长度就是 1GB —— 一个 batch 16 的请求，KV Cache 比模型权重还大。\n\n所以 LLM 推理是**显存带宽瓶颈**（memory-bound）而非算力瓶颈：GPU 大部分时间在搬数据。围绕它的优化有 GQA/MLA（减少缓存）、PagedAttention（减少碎片）、量化 KV Cache、以及 prefix caching（跨请求复用）。"},
{id:"i2",cat:"infra",level:2,tags:["推理"],q:"Prefill 和 Decode 两阶段有什么本质区别？",
a:"- **Prefill**：并行处理整个 prompt，一次算完所有位置的 KV。是**计算密集型**（compute-bound），GPU 利用率高，大矩阵乘法。决定 **TTFT（首 token 延迟）**。\n- **Decode**：逐个生成 token，每步只处理 1 个 token 但要读全部权重和 KV Cache。是**访存密集型**（memory-bound），算力利用率可能不到 5%。决定 **TPOT（每 token 延迟）**。\n\n实践含义：\n- 两阶段特性冲突，混在一起调度会互相干扰（一个长 prefill 会卡住所有正在 decode 的请求）。因此有 **Chunked Prefill**（切块混合）和 **PD 分离**（分别用不同 GPU 池，DeepSeek/Mooncake 的做法）。\n- Decode 阶段增大 batch 几乎不增加延迟却成倍提升吞吐 → 这是 **Continuous Batching** 的价值来源。\n- 投机解码之所以有效，也是因为 decode 阶段算力有大量空闲。"},
{id:"i3",cat:"infra",level:2,tags:["推理"],q:"vLLM 的 PagedAttention 解决了什么问题？",
a:"传统实现要为每个请求**预分配最大长度**的连续 KV 显存，导致：内部碎片（实际生成 100 token 却占了 2048 的空间）+ 外部碎片，实测显存浪费 **60~80%**。\n\nPagedAttention 借鉴操作系统虚拟内存：把 KV Cache 切成固定大小的 block（如 16 token），用块表映射逻辑位置到物理块，**按需分配、非连续存储**。\n\n收益：\n- 显存利用率接近 100% → 同样显存能放下 2~4 倍的并发请求 → 吞吐提升数倍。\n- 天然支持**前缀共享**：多个请求共用同一 system prompt 时物理块只存一份（copy-on-write），并行采样/beam search 也能共享。\n\n配合 **Continuous Batching**（请求一完成就立刻换入新请求，而不是等整批做完），是 vLLM 吞吐碾压 HF Transformers 的两大支柱。"},
{id:"i4",cat:"infra",level:2,tags:["量化"],q:"GPTQ、AWQ、GGUF 有什么区别？怎么选？",
a:"都是**训练后量化（PTQ）**，目标是把权重从 FP16 压到 4-bit。\n\n- **GPTQ**：逐层用少量校准数据，基于二阶信息（Hessian）逐列量化并补偿误差。精度好，GPU 推理快。\n- **AWQ**：观察到约 1% 的「显著权重」由激活幅度决定，保护这部分（按激活尺度缩放）再量化。**不依赖反传、校准更快、泛化更好**，通常是 GPU 部署首选。\n- **GGUF**（llama.cpp）：面向 **CPU / 端侧 / Apple Silicon**，支持多种量化位宽（Q4_K_M 是精度-体积甜点），可 CPU-GPU 混合卸载。\n\n选型：**服务端 GPU → AWQ**（配合 vLLM）；**本地/Mac/无显卡 → GGUF**；需要极致压缩且能接受掉点 → 更低位宽或 SmoothQuant（含激活量化）。\n\n经验数据：4-bit 量化通常掉 1~2% 精度，换来 3.5 倍体积压缩和 2~3 倍吞吐提升，绝大多数场景划算。"},
{id:"i5",cat:"infra",level:3,tags:["推理"],q:"投机解码（Speculative Decoding）为什么能加速？会影响输出质量吗？",
a:"用一个小的**草稿模型**一次性预测 k 个 token，再让大模型**一次前向并行验证**这 k 个。接受匹配的前缀，从第一个不匹配处重新采样。\n\n为什么快：decode 是访存瓶颈，大模型验证 5 个 token 和验证 1 个 token 的耗时几乎一样（都要完整读一遍权重）。所以「一次验证多个」是白赚的。\n\n**质量无损** —— 这是关键卖点。通过精心设计的拒绝采样（rejection sampling），最终输出分布与直接用大模型采样**严格一致**，数学上可证明。\n\n实测 1.5~3 倍加速，取决于草稿模型的接受率。变体：Medusa（在原模型上加多个预测头，省掉独立草稿模型）、EAGLE（在特征层做草稿，接受率更高）、Lookahead Decoding（无需草稿模型，用 n-gram 猜）。"},
{id:"i6",cat:"infra",level:2,tags:["部署"],q:"线上 LLM 服务应该监控哪些指标？",
a:"**性能**：\n- TTFT（首 token 延迟，影响体感）、TPOT（每 token 延迟）、端到端延迟，都要看 **P50/P95/P99** 而不是平均。\n- 吞吐：请求数/秒、token 数/秒（区分输入输出）。\n- GPU 利用率、显存占用、**KV Cache 使用率**（接近上限说明要扩容或降并发）、排队长度。\n\n**质量**：\n- 拒答率、空回复率、截断率（撞 max_tokens）、工具调用成功率。\n- 采样送人工/LLM-Judge 打分，做每日质量回归。\n\n**业务与成本**：\n- 每请求 token 成本、缓存命中率（prefix cache 命中能显著降本）、用户点赞点踩、重试率。\n\n**安全**：命中安全策略的比例、提示注入检测告警。\n\n加一句：一定要有**完整请求 trace + 可回放**，线上事故靠日志复现是基本功。"},
{id:"i7",cat:"infra",level:3,tags:["分布式"],q:"数据并行、张量并行、流水线并行分别怎么切？ZeRO 三个阶段是什么？",
a:"- **数据并行（DP）**：每卡一份完整模型，切分数据，梯度 All-Reduce。通信量小，但显存冗余。\n- **张量并行（TP）**：把单个矩阵切开（如 FFN 按列切、注意力按头切），每层都要 All-Reduce。通信极频繁，**只在单机内 NVLink 上用**，一般 TP≤8。\n- **流水线并行（PP）**：按层切分到不同卡，用 micro-batch 填充气泡（1F1B 调度）。通信量最小，适合跨机，但有气泡损失。\n- **序列并行 / 上下文并行**：长序列时按序列维度切，配合 Ring Attention。\n\n**ZeRO**（数据并行的显存优化）：\n- **Stage 1**：切分优化器状态（省 4 倍）\n- **Stage 2**：再切分梯度（省 8 倍）\n- **Stage 3**：再切分参数本身（省 N 倍，但通信增加约 50%）\n\n实践：千亿模型常用 **3D 并行** = TP(机内) × PP(机间) × DP(集群)，再叠加 ZeRO-1 和梯度检查点。"},

/* ───────────── LLM 系统设计 ───────────── */
{id:"s1",cat:"design",level:3,tags:["系统设计"],q:"设计一个企业内部知识库问答系统（10 万份文档，500 名员工）。",
a:"**按这个框架答，别直接跳到技术选型：**\n\n**1. 澄清需求**：文档类型（PDF/Confluence/邮件）？更新频率？是否需要权限隔离？延迟要求？预算？是否必须私有化部署？\n\n**2. 规模估算**：10 万文档 × 平均 20 块 = 200 万向量；1024 维 FP32 ≈ 8GB，HNSW 索引约 1.5 倍 → 单机内存可放下。500 人 × 每天 10 次 = 5000 QPD，峰值 QPS 很低 → **瓶颈不在检索而在生成**。\n\n**3. 架构**：\n- 离线：文档接入 → 版面解析 → 语义切块（保留标题层级 + 权限元数据）→ embedding → 向量库（Qdrant/Milvus）+ ES（BM25）。增量更新用消息队列驱动。\n- 在线：查询改写（补全指代、多查询扩展）→ **混合检索 + 权限过滤**（必须在检索层过滤，不能生成后过滤）→ Cross-Encoder 重排 → 拼 prompt → LLM 生成带引用的答案。\n\n**4. 关键权衡**：私有化用 Qwen/DeepSeek 自部署（数据不出内网、成本可控）vs API（效果好、免运维）。企业场景一般选前者。\n\n**5. 评估与迭代**：建 200 条标注问答集，分段测 Recall@10 / NDCG / 忠实度；线上收集点踩 + badcase 回流。\n\n**6. 可扩展性**：加缓存（相同问题）、加多轮对话记忆、加「找不到就说不知道」的兜底。"},
{id:"s2",cat:"design",level:3,tags:["系统设计"],q:"设计一个 LLM 客服系统，要求控制成本和保证安全。",
a:"**分层路由是核心思路**：\n1. **缓存层**：高频问题走精确匹配 + 语义缓存（相似度 >0.95 直接返回），能挡掉 30~50% 流量，成本近乎 0。\n2. **意图分类**：小模型（或 BERT）判断意图，简单问题走 FAQ 检索直接返回模板答案。\n3. **RAG + 小模型**：中等复杂度用 7B 微调模型 + 知识库。\n4. **大模型兜底**：复杂/情绪激动的对话升级到大模型或转人工。\n\n**安全设计（双向）**：\n- 输入：提示注入检测、敏感词、越权请求识别。\n- 输出：事实性校验（答案必须有引用支撑）、合规过滤、**绝不承诺退款/赔付等有法律效力的内容**（这类必须转人工）。\n- 全链路日志留存，支持事后审计。\n\n**兜底机制**：连续 2 轮用户表达不满 → 自动转人工；模型不确定 → 主动说「帮您转接」。宁可转人工也不要胡说。\n\n**指标**：解决率、转人工率、平均对话轮数、单次会话成本、CSAT。"},
{id:"s3",cat:"design",level:3,tags:["系统设计"],q:"如何评估一个自己微调的模型是否可以上线？",
a:"建立**多层评估体系**，别只看一个分数：\n\n1. **自动指标（离线）**\n   - 目标任务测试集：准确率 / F1 / 业务定制指标。\n   - **回归测试**：通用能力榜（MMLU、C-Eval、GSM8K）确认没有灾难性遗忘。\n   - 安全测试集：有害提问拒答率、越狱抵抗率。\n\n2. **LLM-as-Judge**：对开放式回答用更强模型打分，注意消除位置偏见（交换顺序跑两遍）和长度偏见。\n\n3. **人工评估**：抽 200~500 条，多人标注 pairwise 对比，算胜率和标注者一致性（Kappa）。这是最贵但最可信的。\n\n4. **性能指标**：延迟 P99、吞吐、显存占用是否满足 SLA。\n\n5. **灰度上线**：1% 流量 A/B，看真实业务指标（点踩率、转人工率、任务完成率）+ 监控告警，逐步放量。**留好一键回滚**。\n\n关键原则：**测试集必须和训练数据严格隔离**，并且尽量用线上真实分布采样，而不是构造的漂亮题目。"},
{id:"s4",cat:"design",level:3,tags:["系统设计"],q:"如何降低一个已上线 LLM 应用的成本，同时不明显降低质量？",
a:"按**性价比顺序**列举，这个顺序本身就是答案：\n\n1. **Prompt Caching / 前缀缓存**：system prompt 和固定文档命中缓存，输入成本降 50~90%，零质量损失。最优先。\n2. **精简 Prompt**：删冗余的 few-shot 示例（很多时候 3 个和 8 个效果一样）、压缩检索内容。\n3. **语义缓存**：重复/相似问题直接命中。\n4. **模型路由**：用小模型处理 80% 的简单请求，难的才升级。可以用一个分类器或置信度判断。\n5. **蒸馏**：用大模型生成数据微调小模型，在垂直任务上 7B 常能追平 GPT-4 级别。这是长期最优解，但要投入。\n6. **推理优化**：自部署时用量化（AWQ）+ vLLM + 高并发批处理，单位 token 成本可降数倍。\n7. **限制输出长度**：输出 token 通常比输入贵 3~5 倍，让模型简洁作答收益明显。\n\n注意强调：每一步都要有 A/B 和质量监控，**降本必须量化质量损失**，否则就是耍流氓。"},

/* ───────────── 手撕代码 ───────────── */
{id:"c1",cat:"code",level:2,tags:["手撕"],q:"手写 Multi-Head Self-Attention（带因果掩码）。",
a:"```python\nimport torch, torch.nn as nn, torch.nn.functional as F\n\nclass MHA(nn.Module):\n    def __init__(self, d_model, n_head, dropout=0.0):\n        super().__init__()\n        assert d_model % n_head == 0\n        self.h, self.dk = n_head, d_model // n_head\n        self.qkv  = nn.Linear(d_model, 3 * d_model, bias=False)\n        self.proj = nn.Linear(d_model, d_model, bias=False)\n        self.drop = nn.Dropout(dropout)\n\n    def forward(self, x, mask=None):\n        B, T, C = x.shape\n        q, k, v = self.qkv(x).split(C, dim=2)\n        # (B, T, C) -> (B, h, T, dk)\n        q = q.view(B, T, self.h, self.dk).transpose(1, 2)\n        k = k.view(B, T, self.h, self.dk).transpose(1, 2)\n        v = v.view(B, T, self.h, self.dk).transpose(1, 2)\n\n        att = (q @ k.transpose(-2, -1)) / (self.dk ** 0.5)   # (B,h,T,T)\n        causal = torch.tril(torch.ones(T, T, device=x.device)).bool()\n        att = att.masked_fill(~causal, float('-inf'))\n        if mask is not None:                       # padding mask (B,1,1,T)\n            att = att.masked_fill(mask == 0, float('-inf'))\n        att = self.drop(att.softmax(dim=-1))\n\n        y = (att @ v).transpose(1, 2).contiguous().view(B, T, C)\n        return self.proj(y)\n```\n**面试官会追问的点**：为什么除 √dk；`transpose` 后为什么要 `contiguous`；mask 为什么填 `-inf` 而不是 0（softmax 前）；如何改成 GQA（把 k/v 的头数改小再 `repeat_interleave`）。"},
{id:"c2",cat:"code",level:2,tags:["手撕"],q:"手写 LayerNorm 和 RMSNorm。",
a:"```python\nclass LayerNorm(nn.Module):\n    def __init__(self, d, eps=1e-5):\n        super().__init__()\n        self.g = nn.Parameter(torch.ones(d))\n        self.b = nn.Parameter(torch.zeros(d))\n        self.eps = eps\n    def forward(self, x):\n        mu  = x.mean(-1, keepdim=True)\n        var = x.var(-1, keepdim=True, unbiased=False)   # 注意有偏\n        return self.g * (x - mu) / torch.sqrt(var + self.eps) + self.b\n\nclass RMSNorm(nn.Module):\n    def __init__(self, d, eps=1e-6):\n        super().__init__()\n        self.g = nn.Parameter(torch.ones(d))\n        self.eps = eps\n    def forward(self, x):\n        rms = x.pow(2).mean(-1, keepdim=True).sqrt()\n        return self.g * x / (rms + self.eps)\n```\n要点：沿**最后一维**统计；`unbiased=False`；RMSNorm 去掉了减均值和 bias，少一次归约、少一组参数，速度快 10~15% 且效果相当，所以 LLaMA 系全用它。实现时注意用 FP32 做归一化再转回 BF16，避免精度损失。"},
{id:"c3",cat:"code",level:1,tags:["手撕"],q:"手写带温度和 Top-p 的采样函数。",
a:"```python\ndef sample(logits, temperature=1.0, top_p=0.9, top_k=None):\n    if temperature == 0:                    # 贪心\n        return logits.argmax(-1)\n    logits = logits / temperature\n\n    if top_k:\n        kth = logits.topk(top_k, dim=-1).values[..., -1:]\n        logits = logits.masked_fill(logits < kth, float('-inf'))\n\n    probs = logits.softmax(-1)\n    sp, si = probs.sort(descending=True, dim=-1)\n    cum = sp.cumsum(-1)\n    # 保留累积概率刚好超过 p 的那个 token\n    remove = cum - sp > top_p\n    sp[remove] = 0.0\n    sp = sp / sp.sum(-1, keepdim=True)\n\n    idx = torch.multinomial(sp, 1)\n    return si.gather(-1, idx)\n```\n易错点：`cum - sp > top_p` 这个写法保证**至少保留一个** token（否则最高概率词若已超过 p 会被全部滤掉）；重新归一化不能忘；temperature=0 要单独处理避免除零。"},
{id:"c4",cat:"code",level:2,tags:["手撕"],q:"手写一个完整的 PyTorch 训练循环（含混合精度、梯度累积、裁剪）。",
a:"```python\nscaler = torch.amp.GradScaler()          # BF16 可不用\nmodel.train()\nfor step, batch in enumerate(loader):\n    with torch.autocast('cuda', dtype=torch.bfloat16):\n        out  = model(**batch)\n        loss = out.loss / ACC_STEPS       # 梯度累积要先除\n\n    scaler.scale(loss).backward()\n\n    if (step + 1) % ACC_STEPS == 0:\n        scaler.unscale_(optimizer)        # 裁剪前必须 unscale\n        torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0)\n        scaler.step(optimizer)\n        scaler.update()\n        optimizer.zero_grad(set_to_none=True)\n        scheduler.step()                  # 注意跟着优化器步而非 batch\n```\n高频考点：\n- loss 除以累积步数（否则等效放大学习率）。\n- 裁剪前要 `unscale_`，否则裁的是放大后的梯度。\n- `zero_grad(set_to_none=True)` 更省显存。\n- scheduler 在优化器 step 之后调用，且按 **optimizer step** 计数。\n- 评估时 `model.eval()` + `torch.no_grad()`，别忘了切回来。"},
{id:"c5",cat:"code",level:2,tags:["手撕"],q:"手写 RoPE 旋转位置编码的应用。",
a:"```python\ndef build_rope(dim, seq_len, base=10000.0, device='cuda'):\n    inv = 1.0 / (base ** (torch.arange(0, dim, 2, device=device).float() / dim))\n    t   = torch.arange(seq_len, device=device).float()\n    freqs = torch.outer(t, inv)              # (T, dim/2)\n    return freqs.cos(), freqs.sin()\n\ndef rotate_half(x):\n    x1, x2 = x.chunk(2, dim=-1)\n    return torch.cat((-x2, x1), dim=-1)\n\ndef apply_rope(q, k, cos, sin):\n    # q,k: (B, h, T, dk);  cos/sin: (T, dk/2) -> 复制成 (1,1,T,dk)\n    cos = torch.cat([cos, cos], -1)[None, None]\n    sin = torch.cat([sin, sin], -1)[None, None]\n    return q * cos + rotate_half(q) * sin, k * cos + rotate_half(k) * sin\n```\n要点：只对 **Q 和 K** 施加，V 不动（因为位置信息只需影响注意力权重）；带 KV Cache 时用**绝对位置偏移**取对应的 cos/sin；扩展上下文时调大 `base`（NTK 插值）即可外推。"},
{id:"c6",cat:"code",level:1,tags:["手撕"],q:"实现 Beam Search 的核心逻辑（思路 + 伪码）。",
a:"维护 `beam_size` 条候选序列，每步扩展所有候选 × 词表，取总分最高的 k 条。\n\n```python\nbeams = [([bos], 0.0)]                       # (序列, 累积 logprob)\nfor _ in range(max_len):\n    cand = []\n    for seq, score in beams:\n        if seq[-1] == eos:\n            cand.append((seq, score)); continue\n        logp = model(seq)[-1].log_softmax(-1)\n        top  = logp.topk(beam_size)\n        for lp, idx in zip(top.values, top.indices):\n            cand.append((seq + [idx.item()], score + lp.item()))\n    # 长度惩罚，避免偏好短句\n    beams = sorted(cand, key=lambda x: x[1] / (len(x[0]) ** 0.7))[-beam_size:]\n    if all(b[0][-1] == eos for b in beams):\n        break\n```\n关键点：用 **log 概率相加**而非概率相乘（防下溢）；**长度归一化**（`score / len^α`，α≈0.6~0.7）否则总选最短的；结束的候选要保留但不再扩展。\n\n补一句认知：开放式生成不用 beam search，因为最高概率序列往往是最无聊的（「我不知道」类回答概率很高）。"},

/* ───────────── 行为面 & 简历 ───────────── */
{id:"b1",cat:"bq",level:1,tags:["行为面"],q:"「介绍一下你自己」——两分钟自我介绍怎么组织？",
a:"用 **现在 → 过去 → 未来** 三段式，全程对准岗位 JD：\n\n1. **一句话定位**（15秒）：「我是做 LLM 应用落地的，主要经验在 RAG 和模型微调。」\n2. **最相关的一段经历 + 量化结果**（60秒）：讲一个项目，说清楚**问题、你的方法、数字结果**。「做了一个企业知识库问答，通过混合检索 + 重排把召回率从 62% 提到 89%，人工客服工单降了 30%。」\n3. **技能广度点一句**（20秒）：熟悉的框架、部署经验。\n4. **为什么是这家公司**（20秒）：具体到他们的产品或技术方向，证明你做过功课。\n\n三个禁忌：从大学开始按时间流水账；只讲用了什么框架不讲解决了什么问题；超过 3 分钟。\n\n**一定要提前写稿并计时练 5 遍**，这是唯一能百分百准备好的题。"},
{id:"b2",cat:"bq",level:2,tags:["行为面"],q:"「讲一个你做过的最有挑战的项目」——怎么答才加分？",
a:"用 **STAR** 但重心放在 **A（Action）** 上，并且一定要讲**权衡**和**失败尝试**。\n\n结构模板：\n- **S/T（20%）**：背景和你的目标，要量化（「要在 200ms 内返回，准确率超过 85%」）。\n- **A（60%）**：\n  - 你怎么定位问题的（数据分析/消融实验，展示方法论）\n  - **试过什么没成功**，为什么（这是最能体现真实性和深度的部分，多数人不敢讲）\n  - 最终方案，以及为什么选它而不是其他方案（权衡：成本 vs 效果 vs 工期）\n- **R（20%）**：量化结果 + 上线后的实际影响 + 你学到了什么。\n\n加分动作：主动说「这个方案现在回头看还有什么不足，如果重做我会怎么改」。这句话几乎总能让面试官高看一眼。\n\n**提前准备 3 个故事**：一个技术深度的、一个跨团队协作的、一个搞砸后挽救的。"},
{id:"b3",cat:"bq",level:2,tags:["简历"],q:"没有大厂经验、没有大模型训练资源，简历上怎么写项目才有说服力？",
a:"招聘方在中小规模岗位上真正看的不是「你训过 70B」，而是**你能不能独立解决问题并量化结果**。\n\n写法要点：\n1. **量化一切**：不是「搭建了 RAG 系统」，而是「基于 8k 份内部文档构建 RAG，混合检索 + reranker 把 Recall@5 从 61% 提升到 88%，P95 延迟 1.2s」。没有真实业务数据就自建评测集，说明评测方法。\n2. **展示工程完整度**：有评测集、有消融实验、有部署（Docker/API）、有监控 —— 这比模型多大重要得多。\n3. **突出「为什么这么选」**：写一句技术选型理由，证明你在做判断而不是抄教程。\n4. **开源可查**：GitHub 链接 + 清晰 README + 效果截图，面试官真的会点开。\n5. **规避雷区**：别写「精通」，别堆砌没用过的名词 —— 简历上每个词都可能被追问 20 分钟。\n\n结构建议：个人信息 / 技能栈（分层：精通-熟悉-了解）/ 项目经历（3 个，倒序）/ 工作或实习 / 教育。**一页，最多两页**。"},
{id:"b4",cat:"bq",level:1,tags:["行为面"],q:"面试最后「你有什么问题问我吗」，该问什么？",
a:"**必须问**，不问等于不在意这个岗位。准备 4~5 个，现场挑 2~3 个。\n\n好问题（体现你在认真评估这份工作）：\n- 「这个岗位入职后前三个月最需要解决的问题是什么？」——最有价值的一问，直接暴露真实工作内容。\n- 「团队目前在 LLM 技术栈上是自研微调为主还是调用 API 为主？未来半年的规划是？」\n- 「模型效果的评估标准是怎么定的？有没有线上 A/B 的基础设施？」——显示你有生产思维。\n- 「团队的构成是什么样的，算法和工程怎么配合？」\n- 「您觉得在这个团队做得好的人有什么共同点？」\n\n避免：一上来就问薪资福利加班（留到 HR 轮）、问官网首页就能查到的信息、问「我表现怎么样」（把面试官架住了）。\n\n可以收尾加一句：「如果有后续轮次，我想提前准备一下，有没有建议的方向？」——态度分。"},
{id:"b5",cat:"bq",level:2,tags:["行为面"],q:"被问到不会的技术问题，怎么处理最好？",
a:"**绝对不要硬编**。面试官大概率比你懂，编造会直接判定为诚信问题，比不会严重得多。\n\n推荐的三步：\n1. **诚实但不空手**：「这个我没有直接实践过，不过我理解它应该是要解决 XX 问题，我猜测思路可能是 YY，不确定对不对。」—— 展示推理能力比展示记忆力更重要。\n2. **迁移已知**：「我做过的 XX 和这个有点像，那边我们是这么处理的……」\n3. **反问澄清**：如果是开放设计题，主动问「您更关注延迟还是成本？」—— 这本身就是能力的体现。\n\n面试后加一句「回去我会查一下这块」，并且**如果有下一轮，真的查了再主动提起** —— 这个动作非常加分。\n\n心态上：一场面试答不出 20~30% 的问题是完全正常的，面试官通常故意问到你的边界为止。不要因为一题崩掉而影响后面。"},
{id:"b6",cat:"bq",level:2,tags:["求职"],q:"转行做 LLM 相关岗位，应该投什么职位？各自要求是什么？",
a:"几条常见路径，按门槛从低到高：\n\n- **LLM 应用工程师 / AI 应用开发**：最好入门。要求会 Python + 后端 + Prompt 工程 + RAG/Agent 框架（LangChain/LlamaIndex）。有扎实工程背景的人转这个最快。\n- **算法工程师（NLP/LLM 应用方向）**：要求懂微调（LoRA/SFT）、评估体系、能读论文。要有完整的微调项目经验。\n- **推理优化 / AI Infra**：要求 CUDA、vLLM/TensorRT、分布式。有 C++/系统背景的人极其吃香，**缺口大、竞争小**，很值得考虑。\n- **数据工程（面向 LLM）**：数据清洗、合成数据、评测集构建。被严重低估但需求真实。\n- **预训练 / 大模型研发**：门槛最高，通常要求顶会论文 + 大规模训练经验，转行基本走不通，不建议作为第一目标。\n\n建议：**用工程能力做支点**切入应用层或 Infra，先进圈再往核心走。同时准备 2 个高完成度的开源项目作为敲门砖。"}
];
