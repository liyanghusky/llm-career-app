/* Infra 主攻线 · 课程（二）：分布式并行 / 量化与精度 / 服务化与容量
   ⚠ 约定：字符串里的中文引号一律用「」，不要用 ASCII 双引号；反引号写成 \`
*/

window.LESSONS.inf4 = {
track: "infra", crumb: "Infra 主攻线 · 第 4 课",
title: "分布式：一张卡放不下之后",
sub: "四种并行方式、它们各自的通信代价、以及为什么标准布局是 TP 在机内、PP 跨机、DP 在最外层。目标是让你能推导而不是背诵。",
est: "读 90 分钟 + 做题 45 分钟",
sections: [
{h:"1. 先搞清楚要切的是什么",
b:`单卡装不下有两种情况，对应完全不同的解法。

**情况一：数据太多，模型装得下。**
你只是想训得更快。解法是**数据并行** —— 每张卡放一份完整模型，各自处理不同的数据，最后把梯度汇总。

**情况二：模型本身就装不下。**
70B 模型训练需要 1120 GB 状态，单卡 80GB 差了 14 倍。这时必须把**模型本身**切开。

切模型又有几种切法：
- 把**每个矩阵**横着或竖着切开 → **张量并行（TP）**
- 把**层**分给不同的卡 → **流水线并行（PP）**
- MoE 的**专家**分给不同的卡 → **专家并行（EP）**
- 把**序列**切开（长上下文时）→ **序列/上下文并行**

还有一种特殊的：模型不切，但**状态分片存储**，用的时候临时聚合 → **ZeRO / FSDP**。

**这一课的核心问题不是有哪些并行，而是什么时候用哪个。**
答案永远来自同一个地方：**通信代价**。`},

{h:"2. 一切判断的起点：带宽层级",
b:`把这张表记死，后面所有的策略选择都从它推导出来。

| 通路 | 带宽（H100 世代） | 相对倍数 |
|---|---|---|
| HBM（卡内显存） | 3350 GB/s | 基准 |
| **NVLink 4.0**（机内卡间） | 900 GB/s | 约 1/4 |
| **InfiniBand NDR**（跨机） | 50 GB/s | 约 1/67 |
| PCIe 5.0 x16（CPU↔GPU） | 64 GB/s | 约 1/52 |

关键的不等式：

$$HBM ≫ NVLink ≫ IB ≈ PCIe$$

**NVLink 比 InfiniBand 快约 18 倍。** 这个数字是后面所有决策的依据。

机内 8 张卡通过 **NVSwitch** 全互联，任意两卡之间都是 900 GB/s。超出 8 卡就要跨机，掉到 IB 的 50 GB/s。

> **所以机内 8 卡是一道天然的分界线。** 任何通信频繁的并行方式，都应该被限制在这条线以内。

再补一个维度：**通信不只有带宽，还有延迟。** 每次集合通信有固定的启动开销（几微秒到几十微秒）。如果你要通信一千次，哪怕每次数据量很小，固定开销累积起来也很可观。

**所以评估一种并行方式，要看两件事：每次通信多少字节（带宽敏感）、一共通信多少次（延迟敏感）。**`},

{h:"3. 数据并行：最简单，但有冗余",
b:`**做法**：每张卡放一份完整的模型，各自用不同的数据算前向和反向，然后把梯度做 **All-Reduce**（求和后广播回所有卡），各卡用相同的梯度更新参数，保持模型一致。

**通信特性**：
- **时机**：每个 step 末尾一次（频率极低）
- **数据量**：整个模型的梯度大小
- 因为频率低，即使跨机也能接受 → **所以 DP 放在最外层**

**问题：显存冗余。**

每张卡都存了一份完整的：参数、梯度、优化器状态。用 AdamW 的话是 \`16N\` 字节。DP=16 就是同样的东西存了 16 份。

**ZeRO 就是来消除这个冗余的**：

- **Stage 1**：优化器状态（m 和 v，占 8N）切成 DP 份，每卡只存 1/DP。**省约 4 倍，通信量基本不变** —— 这是免费的午餐，应该总是开。
- **Stage 2**：梯度也切分。省约 8 倍。
- **Stage 3**：参数本身也切分。省约 N 倍，但前向和反向**每层都要 All-Gather 把参数聚回来**（用完立即释放），**通信量增加约 50%**。

**FSDP** 是 PyTorch 原生的 ZeRO-3 等价实现。

> **一个高区分度的问题：ZeRO-3 和张量并行有什么本质区别？**
>
> 它们都把参数分散到多卡，但机制完全不同：
> - **ZeRO-3**：参数**分片存储**，计算时先 All-Gather **还原成完整的层**，然后每张卡算完整的计算。通信是**参数搬运**，可以预取、可以和计算重叠。
> - **TP**：矩阵被**真正切开**，每张卡只算自己那一片，**从不还原**。通信是计算中途的**归约**，在关键路径上躲不开。
>
> 一句话：**ZeRO-3 是用通信换显存的存储优化，TP 是真正的计算切分。**`},

{h:"4. 张量并行：把矩阵切开",
b:`**做法**：把单个权重矩阵切成几份，每张卡只持有并计算其中一片。

**怎么切才能少通信**（这是 Megatron 的精髓）：

**FFN 部分**（\`Y = GELU(X·W1)·W2\`）：
- \`W1\` **按列切**：每张卡算出中间结果的一部分列。因为 GELU 是逐元素的，**这里不需要通信**。
- \`W2\` **按行切**：每张卡用自己那部分中间结果算出完整形状的部分和，最后做**一次 All-Reduce** 求和。

**注意力部分**：
- 按**头**切。每张卡负责一部分注意力头，各自完整地算 QKV 和注意力。
- 输出投影按行切，最后**一次 All-Reduce**。

所以**每个 Transformer 层需要 2 次 All-Reduce**（FFN 一次、注意力一次）。

**这个通信频率有多高？** 80 层的模型，一次前向就是 160 次 All-Reduce。反向再来一轮。

**算一下 decode 时的通信量**：单次 All-Reduce 约 \`2 × batch × hidden × 字节 × (TP−1)/TP\`。
batch=32、hidden=8192、BF16、TP=8：约 **0.9 MB**。

160 次 × 0.9 MB ≈ 145 MB/token。走 NVLink 900 GB/s 理论只要 0.16 ms。

**但是**：160 次通信的**固定延迟**（每次几微秒到几十微秒）加起来可能有好几毫秒 —— **这往往才是主要成本**，而不是带宽。

**结论**：
- TP 通信极频繁 → **必须在 NVLink 域内** → \`TP ≤ 8\`
- TP=16 跨机的话，通信成本放大约 18 倍，几乎总是不划算
- 而且 TP 越大，单卡的 GEMM 越瘦（hidden/TP 变小），**Tensor Core 利用率下降**
- **TP=8 附近是收益拐点**`},

{h:"5. 流水线并行：把层分开",
b:`**做法**：把模型的层分成几段（stage），每张卡负责一段。数据像流水线一样依次穿过。

**通信特性**：只在 **stage 边界**传激活值。通信量是所有并行方式里**最小的** → 适合跨机。

**代价：气泡（bubble）。**

朴素做法下，stage 1 算完才能轮到 stage 2，此时 stage 1 在空等。整条流水线大部分时间在空转。

**解法：把一个 batch 切成多个 micro-batch**，让它们像流水一样连续进入，填满空隙。

**气泡率公式**（必须记住）：
$$气泡率 = (stage数 − 1) / (micro\\_batch数 + stage数 − 1)$$

- 4 个 stage、4 个 micro-batch：\`3/7 ≈ 43%\` —— 很差
- 4 个 stage、32 个 micro-batch：\`3/35 ≈ 8.6%\` —— 可接受
- **micro-batch 数要远大于 stage 数**

调度方式上，**1F1B**（one-forward-one-backward）比朴素的 GPipe 更省激活显存 —— 它让每个 stage 尽早做反向，及时释放激活。

> **为什么推理里很少用 PP？**
>
> 因为 **decode 阶段没有 micro-batch 可切**。每一步只生成 1 个 token，它必须串行穿过所有 stage。4 个 stage 就是 4 段串行，气泡率接近 75%，而且单 token 延迟变成所有 stage 之和，TPOT 明显变差。
>
> **什么时候推理也用 PP**：① 模型太大，单机 8 卡也放不下，必须跨机（这时 TP 跨机更糟）② 离线批量推理，有大量请求可以当 micro-batch 填流水线。`},

{h:"6. 组合成 3D 并行",
b:`现在把三者组合起来。**排列顺序是从带宽推导出来的**：

$$TP(机内) × PP(机间) × DP(最外层)$$

- **TP 最内层**：通信最频繁 → 放在最快的 NVLink 上，且不超过 8
- **PP 中间层**：通信量最小 → 可以跨机走 IB
- **DP 最外层**：通信频率最低（每 step 一次）→ 跨越整个集群没问题

**走一遍完整的推导：512 张 H100 训 70B。**

**① 定 TP**：必须机内，且 hidden=8192 时 TP=8 每卡还有 1024 维，GEMM 不算太瘦。**取 TP=8**（一台机）。

**② 定 PP**：70B 有 80 层，PP=4 则每 stage 20 层。micro_batch 取 32 时气泡率 \`3/35 ≈ 8.6%\`，可接受。**取 PP=4**（4 台机）。
→ 一个模型副本 = \`8 × 4 = 32\` 卡 = 4 台机

**③ 定 DP**：\`512 / 32 = 16\`。**DP=16**。

**④ 叠加 ZeRO-1**：在 DP 维上切优化器状态，省 4 倍显存且通信基本不变。不用 ZeRO-3，因为 TP+PP 已经切了模型，再来 All-Gather 是浪费。

**⑤ 其他必开**：BF16 混合精度、FlashAttention、选择性梯度检查点、序列并行（配合 TP 省激活）、\`clip_grad_norm=1.0\`。

**⑥ 验证**：算 **MFU（Model FLOPs Utilization）**
$$MFU = 6 × N × tokens\\_per\\_step ÷ (step时间 × GPU数 × 单卡峰值FLOPS)$$
（6N 来自前向 2N + 反向 4N；开了梯度检查点要按 8N 算）

**健康区间是 40-55%。低于 35% 说明有明显瓶颈。**

**MFU 低的排查顺序**：数据加载是否拖后腿 → 通信是否和计算重叠 → 流水线气泡是否过大 → 算子是否高效（FlashAttention、torch.compile）→ 梯度检查点是否开过头 → 有没有慢节点。`},

{h:"7. 通信原语与重叠",
b:`**必须认识的集合通信原语**：

- **All-Reduce**：每张卡都得到所有卡数据的求和结果。= Reduce-Scatter + All-Gather
- **All-Gather**：每张卡收集到所有卡的数据拼接
- **Reduce-Scatter**：求和后每卡只拿走自己那一片
- **All-to-All**：每张卡给每张卡都发一份不同的数据（MoE 的专家路由用它）
- **Broadcast**：一张卡发给所有卡

**Ring All-Reduce 的通信量**（会算这个是加分项）：
设数据量 D、卡数 P，每卡收发 \`2D(P−1)/P ≈ 2D\`。

**关键性质：通信量近似与卡数无关。** 这是它能扩展到几千卡的原因。代价是**延迟随 P 线性增长**（需要 \`2(P−1)\` 步），所以超大规模会改用分层算法（机内 ring + 机间 tree）。

**怎么让通信和计算重叠**（这是拿到高 MFU 的关键）：

**① 梯度分桶（bucketing）** —— PyTorch DDP 的核心机制。
反向传播是从后往前算的，最后几层的梯度先出来。把参数分成若干桶（默认 25MB），**一个桶凑齐就立刻发起该桶的 All-Reduce**，不等整个反向结束。这样通信和前面层的反向计算重叠。
- \`bucket_cap_mb\` 太小 → 通信次数多、固定开销占比高
- 太大 → 重叠机会少
- 大模型可以适当调大

**② ZeRO-3 / FSDP 的参数预取** —— 算第 i 层时异步 All-Gather 第 i+1 层的参数。

**③ TP 内的细粒度重叠** —— 把 All-Reduce 拆块，与 GEMM 的分块计算交错。

**怎么验证重叠生效了**：用 Nsight Systems 看时间线，NCCL kernel 应该和计算 kernel **在时间上并行**。如果看到明显的「算一段 → 停下来传 → 再算」的锯齿，说明没重叠。`}
],
quiz: [
{id:"if4q1", type:"num", q:"PP 有 8 个 stage，一个 batch 切成 24 个 micro-batch。气泡率是多少百分比？（保留整数）",
hint:"气泡率 = (stage数 − 1) / (micro_batch数 + stage数 − 1)。代入 stage=8、micro=24。", ans:23, tol:1,
why:"\`(8−1) / (24+8−1)\` = \`7/31\` ≈ **23%**。\n\n意思是有约四分之一的 GPU 时间在空转。这个数字偏高。\n\n**怎么改善**：\n- 增加 micro-batch 数。改成 64 个：\`7/71 ≈ 9.9%\`，明显好转\n- 减少 stage 数（但那要求每张卡能装下更多层）\n- 用 **interleaved 1F1B**（把每个 stage 再切成多个虚拟 stage 交错执行），能进一步压低气泡\n\n**记住这个规律**：micro-batch 数要**远大于** stage 数，通常至少 4 倍以上才能把气泡压到 10% 以内。"},

{id:"if4q2", type:"mc", q:"为什么张量并行必须限制在单机内？",
choices:["因为跨机的 GPU 型号可能不同",
         "因为 TP 每层都要 All-Reduce，通信极频繁，跨机带宽比 NVLink 低约 18 倍",
         "因为 NCCL 不支持跨机的 All-Reduce",
         "因为跨机会导致显存不足"], ans:1,
hint:"回想 TP 的通信频率：一个 80 层的模型，一次前向要通信多少次？再对比 NVLink 和 InfiniBand 的带宽差距。",
why:"**核心是通信频率 × 带宽差距。**\n\nTP 每个 Transformer 层需要 **2 次 All-Reduce**（FFN 后 + 注意力后）。80 层的模型一次前向就是 160 次，反向再来一轮。\n\nNVLink 900 GB/s vs InfiniBand 50 GB/s —— **差约 18 倍**。把如此高频的通信放到慢 18 倍的链路上，通信开销会彻底吃掉并行带来的收益。\n\n**还有一个常被忽略的因素**：除了带宽，每次集合通信有**固定延迟**（几微秒到几十微秒）。160 次的固定开销累积起来可能有好几毫秒，跨机时这个延迟还会更高。\n\n其余为什么错：\n- NCCL **完全支持**跨机 All-Reduce，技术上做得到，只是不划算\n- 和 GPU 型号、显存都没关系\n\n**由此得到的推论**：\`TP ≤ 8\`（NVLink 全互联域的大小），需要更多切分时用 PP 跨机。这就是 3D 并行布局的由来。"},

{id:"if4q3", type:"mc", q:"ZeRO-3 和张量并行的本质区别是什么？",
choices:["ZeRO-3 只能用于训练，TP 训练推理都能用",
         "ZeRO-3 把参数分片存储、计算时 All-Gather 还原成完整的层；TP 真正切开矩阵，每卡只算自己那片，从不还原",
         "ZeRO-3 的通信量更大",
         "两者完全等价，只是实现不同"], ans:1,
hint:"想一个关键问题：在实际计算某一层时，ZeRO-3 的每张卡手里拿的是完整的权重还是一片？TP 呢？",
why:"**区别在于计算时还不还原完整权重。**\n\n- **ZeRO-3**：参数平时分片存着省显存，但**要算某一层时先 All-Gather 把完整权重聚回来**，然后每张卡用完整权重算自己那批数据（本质还是数据并行），算完立即释放。通信是**参数搬运**，可以预取、可以和计算重叠。\n- **TP**：矩阵被**物理切开**，每张卡只持有并计算一片，**从不还原完整权重**。通信是计算中途的**归约**（All-Reduce），在关键路径上，躲不开。\n\n一句话：**ZeRO-3 是用通信换显存的存储优化，TP 是真正的计算切分。**\n\n其余为什么错：\n- 第一个：TP 在推理里也常用（大模型单卡放不下），ZeRO-3 主要用于训练，但这不是本质区别\n- 第三个：ZeRO-3 通信量确实比 ZeRO-2 高约 50%，但和 TP 不好直接比（性质不同）\n- 第四个：明显不等价\n\n**实践推论**：如果已经用 TP+PP 切了模型，就不该再叠 ZeRO-3（重复劳动且增加通信），叠 **ZeRO-1** 就够了 —— 它只切优化器状态，几乎免费。"},

{id:"if4q4", type:"text", q:"256 张 H100 训练一个模型，hidden=8192、共 64 层。请给出一组合理的 TP/PP/DP 组合。（格式：TP=x,PP=y,DP=z）",
ans:["TP=8,PP=4,DP=8","TP=8,PP=2,DP=16","tp=8,pp=4,dp=8","tp=8,pp=2,dp=16","TP=8, PP=4, DP=8","TP=8, PP=2, DP=16"],
hint:"先定 TP（受 NVLink 域限制），再定 PP（看层数够不够分、气泡率能否接受），剩下的除给 DP。三者相乘要等于 256。",
why:"**两组都合理**：\`TP=8, PP=4, DP=8\` 或 \`TP=8, PP=2, DP=16\`。\n\n**推导过程**：\n\n**① TP=8** —— 必须在机内 NVLink 域内，8 是全互联域的大小。hidden=8192 切 8 份后每卡 1024 维，GEMM 还不算太瘦，可以接受。\n\n**② PP 取 2 或 4** —— 64 层分 4 段是每段 16 层，分 2 段是每段 32 层。都合理，取决于单卡显存能装多少层。PP=4 时气泡率（micro=32）约 8.6%，PP=2 时约 3%，**PP 越小气泡越少**，所以显存允许的话优先 PP=2。\n\n**③ DP = 256 / (TP×PP)** —— PP=4 时 DP=8，PP=2 时 DP=16。\n\n**④ 叠加 ZeRO-1** 在 DP 维切优化器状态。\n\n**评分要点**（面试时说出来）：\n- TP 不能超过 8（跨机通信代价放大 18 倍）\n- PP 能小就小（气泡是纯损失）\n- 三者相乘必须等于总卡数\n- 最后要提一句「具体选哪组要看单卡显存能装多少层，我会先用 1-2 台机跑 scaling 测试测 MFU 再定」"},

{id:"if4q5", type:"num", q:"用 Ring All-Reduce 同步 2 GB 的梯度，卡数 P=16。每张卡总共要收发多少 GB？（保留一位小数）",
hint:"Ring All-Reduce = Reduce-Scatter + All-Gather，每阶段每卡收发 D(P−1)/P。两个阶段加起来。", ans:3.8, tol:0.2,
why:"\`2 × D × (P−1)/P\` = \`2 × 2 × 15/16\` = **3.75 GB** ≈ 3.8 GB。\n\n**这个公式最重要的性质是：通信量近似与卡数无关。**\nP=16 时是 3.75 GB，P=128 时是 \`2×2×127/128 ≈ 3.97 GB\` —— 几乎没变。这就是 Ring All-Reduce 能扩展到几千卡的根本原因。\n\n**但代价是延迟**：需要 \`2(P−1)\` 个步骤，P 越大步骤越多，固定延迟累积。所以超大规模会改用**分层算法**（机内先 ring 聚合，机间再走 tree），或者用 NVSwitch 的硬件归约（NVLS）。\n\n**实践意义**：知道通信量后，可以估算通信耗时（÷ 带宽），再和计算耗时对比，判断重叠的收益空间有多大。"},

{id:"if4q6", type:"mc", q:"某次训练测出 MFU 只有 22%。下面哪个最不可能是原因？",
choices:["数据加载太慢，GPU 在等数据",
         "通信没有和计算重叠，时间线上呈锯齿状",
         "GPU 的峰值算力标称值写错了",
         "流水线并行的 micro-batch 数太少，气泡率高"], ans:2,
hint:"前三个选项里，哪一个描述的是真实的性能瓶颈，哪一个只是计算 MFU 时的参数问题？",
why:"**第三个最不可能。** 峰值算力是硬件的公开规格，不是会写错的东西（当然你可能代入错了数，但那是算错不是瓶颈）。\n\n其余三个都是 MFU 低的**真实常见原因**：\n\n- **数据加载慢**：GPU 空等 dataloader。查 profiler 里的 dataloader 时间占比，解法是加 worker、预取、pin_memory、离线做 tokenization、数据 packing。\n- **通信没重叠**：Nsight 时间线上出现「算一段 → 停下来传 → 再算」的锯齿。解法是调 bucket_cap_mb、开 TP 通信重叠、检查 NCCL 拓扑是否正确。\n- **气泡率高**：micro-batch 太少。解法是增加 micro-batch 或减少 stage。\n\n**完整的排查顺序**（值得记住）：\n1. 数据加载\n2. 通信占比与重叠\n3. 流水线气泡\n4. 算子效率（FlashAttention？torch.compile？GEMM 是否太瘦）\n5. 梯度检查点是否开过头（它有确定的 ~30% 时间成本）\n6. 慢节点（看 per-rank step 时间分布）\n\n**健康区间**：大规模训练 40-55%。低于 35% 一定有问题值得查。"},

{id:"if4q7", type:"open", q:"为什么 DP 放在最外层、TP 放在最内层？如果反过来会发生什么？",
hint:"分别想两者的通信频率和通信量。哪个通信更频繁？最快的链路（NVLink）应该留给谁？",
ref:"**排列顺序完全由通信频率和带宽层级的匹配决定。**\n\n**TP 放最内层（机内 NVLink）的理由**：\nTP 每个 Transformer 层都要 2 次 All-Reduce，80 层的模型一次前向就是 160 次，反向再来一轮。这是**极高频**的通信，而且在计算的关键路径上（算完 FFN 必须等归约完成才能继续下一层）。所以必须放在最快的链路上。\n\n**DP 放最外层（跨集群）的理由**：\nDP 只在每个 step 的末尾做一次梯度 All-Reduce。虽然数据量大（整个模型的梯度），但**频率极低**，而且可以通过梯度分桶和反向传播重叠 —— 反向算到哪就传到哪，通信几乎完全被计算掩盖。所以即使走慢的 IB 也能接受。\n\n**反过来会发生什么**：\n\n如果把 **TP 跨机**：160 次高频通信全部走 50 GB/s 的 IB（比 NVLink 慢 18 倍），而且跨机的固定延迟更高。通信开销会彻底吃掉并行收益，实测吞吐可能掉到原来的几分之一 —— **这是分布式训练最经典的配置错误之一**。\n\n如果把 **DP 限制在机内**：浪费了 NVLink 的高带宽（DP 根本不需要那么快），同时模型能扩展的规模被严重限制（机内 8 卡要同时承担 TP 和 DP）。\n\n**PP 为什么在中间**：它的通信量最小（只在 stage 边界传激活），所以最适合承担跨机的角色。\n\n**一句话总结**：**把最快的链路留给通信最频繁的并行方式。** 这个原则不只适用于当前硬件 —— 换一代硬件（比如 NVLink 域扩大到 72 卡的 GB200），TP 的上限就会跟着变。能说出这一层，说明你理解的是原理而不是配置。"}
]
};

window.LESSONS.inf5 = {
track: "infra", crumb: "Infra 主攻线 · 第 5 课",
title: "量化与数值精度：用更少的比特换性能",
sub: "从浮点数怎么存讲起，一直到 AWQ 为什么比 GPTQ 更常用。核心是搞清一件事：量化到底省的是显存还是算力。",
est: "读 80 分钟 + 做题 40 分钟",
sections: [
{h:"1. 浮点数是怎么存的",
b:`一个浮点数由三部分组成：**符号位 + 指数位 + 尾数位**。

$$值 = (-1)^{符号} × 1.尾数 × 2^{指数}$$

两部分各管一件事：
- **指数位决定动态范围** —— 能表示多大和多小的数
- **尾数位决定精度** —— 在给定量级下能分辨多细的差别

常见格式：

| 格式 | 总位数 | 指数 | 尾数 | 动态范围 |
|---|---|---|---|---|
| FP32 | 32 | 8 | 23 | ±3.4e38 |
| **BF16** | 16 | **8** | 7 | **±3.4e38**（同 FP32）|
| FP16 | 16 | 5 | 10 | ±65504 |
| FP8 E4M3 | 8 | 4 | 3 | ±448 |
| FP8 E5M2 | 8 | 5 | 2 | ±57344 |

注意 **BF16 和 FP16 的对比**：两者都是 16 位，但分配方式完全不同。
- BF16 把位数给了指数 → **范围和 FP32 一样宽**，但精度只有 7 位尾数
- FP16 把位数给了尾数 → 精度好，但**范围窄**，最小正规数约 6e-5

**这个差异决定了它们在深度学习里的命运。**`},

{h:"2. 为什么训练默认用 BF16 而不是 FP16",
b:`这是面试里的送分题，但要能答到点子上。

**核心事实：深度学习对动态范围敏感，对精度不敏感。**

训练时梯度的量级跨度极大，小梯度可能到 1e-7 甚至更小。

用 **FP16**（最小正规数约 6e-5）：这些小梯度会**下溢成 0**，参数根本不更新。
解法是 **loss scaling** —— 把 loss 乘一个大系数（比如 1024）放大梯度，反传后再把梯度除回来。但这引入新问题：系数太大会溢出成 inf，太小又还是下溢，所以要**动态调整**（遇到 inf 就减半、连续正常就加倍），实现复杂且偶尔会浪费步数。

用 **BF16**（范围和 FP32 一样）：小梯度不会下溢，**完全不需要 loss scaling**。虽然精度只有 7 位尾数，但实践证明足够 —— 因为神经网络本身对权重的微小扰动不敏感（这也是它能被量化的根本原因）。

**所以现代训练（A100 之后）一律用 BF16。** FP16 只在不支持 BF16 的老卡（V100、T4）上才用。

**混合精度的完整做法**：
- 前向和反向用 BF16 算（省显存、用上 Tensor Core）
- **参数的主副本用 FP32 保存**（避免累积更新时的精度损失）
- 优化器状态用 FP32
- 这就是为什么训练显存是 \`16N\` 而不是 \`2N\``},

{h:"3. FP8 与它的难点",
b:`H100 开始有原生的 **FP8 Tensor Core**，算力约为 BF16 的 **2 倍**。

**两种 FP8 格式分工**：
- **E4M3**（4 位指数、3 位尾数）：范围 ±448，精度稍好 → 用于**前向的激活和权重**
- **E5M2**（5 位指数、2 位尾数）：范围 ±57344 → 用于**梯度**（梯度的动态范围更大）

**难点在于 scaling。**

FP8 E4M3 的表示范围只有 ±448，而 Transformer 的激活有严重的**离群值**（某些通道的数值能比其他通道大 100 倍以上）。用一个全局的缩放因子，要么离群值溢出，要么正常值被压到 0 附近损失殆尽。

**解决思路是细化缩放粒度**：
- per-tensor（整个张量一个 scale）→ 太粗
- per-channel（每个通道一个 scale）
- **per-block**（DeepSeek-V3 用 128×128 的块级 scaling）→ 目前的主流做法

还要配合 **delayed scaling**：用历史几步的 amax（最大绝对值）来预测这一步的缩放因子，避免每步都要先扫一遍数据。

另外**累加必须在 FP32 里做**，否则长序列 GEMM 的累加误差会炸。

**推理侧 FP8 是特例，值得单独强调**：因为有硬件原生支持，FP8 不只省显存，**是真的提算力**。这一点和 INT4 权重量化有本质区别，下一节展开。`},

{h:"4. 推理量化：分清三个对象",
b:`说量化时必须问清楚：**量化什么？** 三个对象的难度和收益完全不同。

**① 权重量化（最常见、最容易）**

把模型权重从 FP16 压到 INT4/INT8。

- 权重的分布**接近高斯、没有极端离群值**，所以好量化
- 4-bit 通常掉 1-2% 精度，换来 **3.5 倍体积压缩**
- 代表方法：GPTQ、AWQ、GGUF

> **最关键的认知**：W4A16（4-bit 权重、16-bit 激活）在多数实现里，**计算时要把权重反量化回 FP16 再做 GEMM**。
>
> 所以它省的是**显存和显存带宽**，**不省算力**。
> - decode 阶段是 memory-bound → **明显提速**（搬的字节少了）
> - prefill 阶段是 compute-bound → **可能反而略慢**（多了反量化开销）

**② 激活量化（难得多）**

LLM 的激活有**系统性的离群值** —— 某些隐藏维度的数值特别大，而且跨样本一致地出现在固定通道上。per-tensor 量化时缩放因子被离群值撑大，正常值的有效位数只剩两三位，精度崩塌。

**SmoothQuant 的解法**：利用一个等价变换把难度从激活迁移一部分到权重
\`\`\`
Y = (X · diag(s)⁻¹) · (diag(s) · W)
\`\`\`
数学上完全等价，但激活的第 j 通道被除以 \`s_j\`（压平离群）、权重第 j 行乘以 \`s_j\`（权重本来平坦，扛得住）。缩放因子可以离线吸收进前一层，推理时零开销。

这让 **W8A8** 可行 → 能用上 INT8 Tensor Core → **真正省算力**。

**③ KV Cache 量化（被低估）**

长上下文高并发时 KV Cache 常常比权重还大。量化到 FP8/INT8 直接翻倍并发。

坑在于 **K 和 V 的分布不同**：K 有明显的 per-channel 离群值，V 相对均匀。所以 K 适合 per-channel 量化、V 适合 per-token 量化，用统一的 per-tensor 会掉点明显。`},

{h:"5. GPTQ vs AWQ：算法差异与选型",
b:`**GPTQ** —— 基于二阶信息的误差补偿

逐层、**逐列**量化权重。每量化完一列，就用 Hessian 的逆信息去**调整剩余未量化的列**，补偿刚才引入的误差，使整层输出的重建误差最小。

- 需要校准数据来估 Hessian（\`H = 2XXᵀ\`）
- 需要 Cholesky 分解和逐列迭代 → **量化过程慢**（几十分钟到小时级）
- 对校准集有一定敏感性

**AWQ（Activation-aware Weight Quantization）** —— 保护显著权重

核心观察：**权重的重要性由激活的幅度决定**。大约 **1% 的显著权重**（对应激活值大的输入通道）承载了主要的精度影响。

做法：按激活的 per-channel 尺度，对这些重要通道**先放大权重再量化**（缩放因子吸收进前一层），使显著权重在量化后保留更多有效位。

- **不需要反向传播、不需要 Hessian**，只要统计激活幅度 → 量化快得多（分钟级）
- 对校准集不敏感，**泛化更好**
- 有高效的 W4A16 GEMM kernel

**为什么生产上 AWQ 更常用**：
1. 量化快 → 迭代和上线成本低
2. 对校准数据不敏感 → 不担心校准集和线上分布不匹配
3. 精度相当或更好
4. 工程上更可预测，不会出现换个校准集结果差很多的情况

**H100 上的选型建议（这一句能体现判断力）**：
优先试 **FP8** 而不是 4-bit。因为 FP8 有原生 Tensor Core，**显存和算力双收益**，且精度损失更小（<1%）；而 4-bit 只有显存收益，prefill 还可能变慢。

只有在显存极度紧张（比如要在单卡塞下 70B）时，4-bit 才是更好的选择。`},

{h:"6. 怎么评估量化后掉了多少",
b:`**先说一个常见错误：只看 PPL（困惑度）。**

PPL 是在大量普通文本上的平均预测损失。量化的损伤往往集中在**少数关键位置**（长距离依赖、精确数字、罕见 token），这些在平均值里被淹没。

实测常见的情况：**PPL 只涨 0.05，但代码生成通过率掉 8%。**

**该建的评估矩阵**：

**① 目标任务的真实指标** —— 最重要。你的服务干什么就测什么。

**② 敏感探针**（这两个最容易暴露问题）：
- **推理类**：GSM8K / MATH。多步推理里一步错就全错，对精度损失最敏感。
- **长上下文召回**：needle-in-a-haystack。KV 量化必测这个。

**③ 信号干净的可验证任务**：
- 代码：HumanEval / MBPP（执行结果客观，不受评分主观性影响）

**④ 格式与鲁棒性**：
- JSON 格式合法率（量化后格式崩掉是常见现象）
- 指令遵循、拒答率变化

**⑤ 快速冒烟测试**（进阶技巧）：
对同一批 prompt，比较量化前后**输出 token 分布的 KL 散度**或 **top-1 一致率**。这能在跑完整评测前几分钟内发现明显问题。

**工程流程**：
- 固定 \`temperature=0\` 做可复现对比
- 建**回归门禁**：目标指标掉超过阈值就阻断上线
- 保留 badcase 集，专门收集量化后变差的例子

**面试里该说的那句话**：「我会用 PPL 做快速 smoke test，但**上线决策必须基于目标任务指标 + 推理和长上下文这两个敏感探针**」。这显示你知道指标的适用边界。`}
],
quiz: [
{id:"if5q1", type:"mc", q:"BF16 相比 FP16 最大的优势是什么？",
choices:["精度更高，尾数位更多",
         "动态范围和 FP32 一样宽，训练时不需要 loss scaling",
         "占用显存更少",
         "计算速度更快"], ans:1,
hint:"两者都是 16 位。看它们怎么分配指数位和尾数位，再想训练时哪个更容易出问题 —— 是精度不够，还是数值范围不够？",
why:"**BF16 有 8 位指数（和 FP32 相同），动态范围一样宽。**\n\nFP16 只有 5 位指数，最小正规数约 6e-5，训练时的小梯度会**下溢成 0**，必须靠 loss scaling（把 loss 放大后反传、再把梯度缩回来）来救，而且要动态调整系数，实现复杂还偶尔浪费步数。\n\nBF16 不需要这一套。\n\n**代价是精度**：BF16 只有 7 位尾数，比 FP16 的 10 位少。但**深度学习对精度不敏感、对范围敏感**，所以这个交换是划算的。\n\n其余为什么错：\n- 精度：BF16 **更低**不是更高\n- 显存：两者都是 16 位，**完全一样**\n- 速度：在支持两者的硬件上基本相同\n\n**结论**：A100 之后一律用 BF16；FP16 只在不支持 BF16 的老卡（V100、T4）上才用。"},

{id:"if5q2", type:"mc", q:"W4A16（4-bit 权重、16-bit 激活）量化，在哪个阶段收益最明显？",
choices:["prefill 阶段，因为它是 compute-bound",
         "decode 阶段，因为它是 memory-bound，而 4-bit 减少了要搬的字节数",
         "两个阶段收益一样",
         "只在训练时有收益"], ans:1,
hint:"W4A16 在多数实现里，计算时会把权重反量化回 FP16 再做矩阵乘。那它实际减少的是什么？这个减少对哪个阶段的瓶颈有帮助？",
why:"**decode 阶段。**\n\n关键在于：W4A16 在多数实现里，**计算时要把权重反量化回 FP16 再做 GEMM**。所以它减少的是**显存占用和显存带宽消耗**，而不是算力。\n\n- **decode 是 memory-bound**（瓶颈在读权重）→ 权重字节数减少 4 倍 → **明显提速**\n- **prefill 是 compute-bound**（瓶颈在算）→ 算力没变，还多了反量化的开销 → **可能反而略慢**\n\n**这是量化里最重要、也最容易搞错的一个认知。**\n\n**对比 FP8**：H100 有原生 FP8 Tensor Core，所以 FP8 **既省显存又提算力**，prefill 和 decode 都受益。这就是为什么 H100 上应该优先考虑 FP8 而不是 4-bit。\n\n**一句话记忆**：4-bit 省的是搬，FP8 省的是搬和算。"},

{id:"if5q3", type:"num", q:"一个 70B 模型，权重从 FP16 量化到 INT4。量化后的权重显存是多少 GB？",
hint:"FP16 每参数 2 字节，INT4 每参数 0.5 字节。", ans:35, tol:1,
why:"FP16：\`70e9 × 2\` = **140 GB**\nINT4：\`70e9 × 0.5\` = **35 GB**\n\n压缩 4 倍。\n\n**实践意义**：\n- 140 GB 需要至少 2 张 80G 卡（考虑 KV Cache 实际要 4 张）\n- 35 GB **单卡就能装下**，还剩 45GB 给 KV Cache\n\n这个差别是决定性的 —— 从必须多卡加承担 TP 通信开销，变成单卡搞定，成本和延迟同时改善。\n\n**但要记住代价**：4-bit 通常掉 1-2% 精度，而且 prefill 阶段可能略慢。所以要评估目标任务能不能接受。\n\n**中间选项**：FP8 是 70 GB，单卡刚好装不下但 TP=2 很舒服，且精度损失 <1%、算力还翻倍。H100 上这往往是最优解。"},

{id:"if5q4", type:"mc", q:"为什么激活量化比权重量化难得多？",
choices:["激活的数据量更大",
         "激活有系统性的离群值，某些通道的数值能比其他大 100 倍以上，撑大了缩放因子",
         "激活需要在运行时量化，没有时间做优化",
         "激活的精度要求本身就更高"], ans:1,
hint:"想象你要用 8 个 bit 表示一组数。如果这组数里大部分在 0~1 之间，但有几个是 100，缩放因子该怎么定？定完之后那些小数还剩几位有效精度？",
why:"**核心是离群值（outlier）。**\n\n实测发现 Transformer 某些隐藏维度上的激活值能比其他维度大 **100 倍以上**，而且这些离群通道是**系统性的** —— 固定出现在某些 channel 上，跨样本一致。\n\n后果：per-tensor 量化时，缩放因子必须照顾到最大值（否则溢出），于是绝大多数正常值被压缩到量化区间的极小一段，**有效位数只剩两三位**，精度崩塌。\n\n而**权重的分布平坦得多**（近似高斯，没有极端离群），所以权重量化到 4-bit 都还能接受，激活量化到 8-bit 就已经很难。\n\n**SmoothQuant 的解法**：用等价变换把难度从激活迁移一部分到权重 —— 激活除以 s、权重乘以 s，数学上不变，但让两边的量化难度平衡。缩放因子能离线吸收进前一层，推理零开销。这让 W8A8 可行，从而用上 INT8 Tensor Core、**真正省算力**。\n\n**另一条路是 FP8**：它有指数位，动态范围天然宽，对离群值的容忍度比 INT8 高得多。这是 H100 之后 FP8 逐渐取代 INT8 激活量化的根本原因。"},

{id:"if5q5", type:"mc", q:"评估量化后的模型，下面哪个做法最不可靠？",
choices:["只看 PPL（困惑度）变化",
         "测 GSM8K 等多步推理任务",
         "测 needle-in-a-haystack 长上下文召回",
         "测 JSON 格式合法率"], ans:0,
hint:"PPL 是在大量文本上的平均值。量化的损伤如果只集中在少数关键位置，会在平均值里表现出来吗？",
why:"**只看 PPL 最不可靠。**\n\nPPL 是大量普通文本上的**平均**预测损失，太钝了。量化带来的损伤往往集中在**少数关键位置**（长距离依赖、精确数字、罕见 token），这些在平均里被彻底淹没。\n\n实测常见：**PPL 只涨 0.05，但代码生成通过率掉 8%。**\n\n其余三个都是好探针：\n- **GSM8K 等多步推理**：一步错就全错，对精度损失**最敏感**，是最好的早期预警\n- **needle-in-a-haystack**：KV Cache 量化必测，长上下文召回最容易被量化误差伤到\n- **JSON 格式合法率**：量化后格式崩掉是很常见的现象，而且直接影响线上可用性\n\n**正确做法**：PPL 只用作**快速冒烟测试**（几分钟内发现灾难性问题），**上线决策必须基于目标任务指标 + 推理和长上下文两个敏感探针**。\n\n再补一个进阶技巧：对同一批 prompt 比较量化前后的 **top-1 token 一致率**或输出分布的 KL 散度，能在跑完整评测前快速判断。"},

{id:"if5q6", type:"open", q:"你负责一个跑在 H100 上的 70B 推理服务，现在要降成本。在量化方案上你会怎么选，为什么？需要验证什么？",
hint:"先想 H100 有什么特殊的硬件能力。再想 70B 在不同精度下的显存和需要几张卡。最后想怎么证明质量没崩。",
ref:"**我会优先试 FP8，而不是直接上 4-bit。理由分三层：**\n\n**① 硬件匹配**\nH100 有原生 FP8 Tensor Core，算力约为 BF16 的 2 倍。所以 FP8 是**显存和算力双收益** —— prefill 和 decode 都提速。而 W4A16 只省显存带宽，prefill 因为多了反量化开销可能反而变慢。在 H100 上不用 FP8 是浪费硬件。\n\n**② 显存账**\n- BF16：140 GB → 需要 TP=4（考虑 KV Cache）\n- **FP8：70 GB → TP=2 就很舒服**，剩下的显存都能给 KV Cache，并发能上去\n- INT4：35 GB → 单卡可行，但精度损失更大\n\nTP 从 4 降到 2，不仅省一半卡，还减少了通信开销（每层 2 次 All-Reduce 的参与方少了）。\n\n**③ 精度损失**\nFP8 通常 <1%，4-bit 通常 1-2%。FP8 有指数位，对激活离群值的容忍度也更好。\n\n**验证计划**（这部分不能省）：\n\n**性能侧**：用**真实流量的长度分布**做 open-loop 压测，画并发-吞吐-延迟三条曲线，对比 BF16 基线的 TTFT/TPOT/吞吐/显存/最大并发。\n\n**质量侧**：\n- 目标任务的真实指标（这是决策依据）\n- GSM8K（推理敏感探针）\n- HumanEval（信号干净、可执行验证）\n- needle-in-a-haystack（如果有长上下文场景）\n- JSON 格式合法率\n- 固定 temperature=0 保证可复现\n\n**上线方式**：影子流量比对 → 1% 金丝雀看真实业务指标（点踩率、重试率）→ 逐步放量，预设自动回滚阈值。\n\n**什么情况我会改选 4-bit**：如果显存是硬约束（比如必须单卡部署、或者要把并发再翻一倍），且目标任务对精度不那么敏感（比如分类、抽取而非复杂推理），那 4-bit 是合理的。\n\n**还要补一句**：量化不是第一优先的降本手段。**prefix caching 零质量损失、收益可能更大**，应该先做。量化排在缓存和 prompt 精简之后。"}
]
};

window.LESSONS.inf6 = {
track: "infra", crumb: "Infra 主攻线 · 第 6 课",
title: "服务化与容量规划：从能跑到能上线",
sub: "SLO 怎么定、容量怎么算、多租户怎么隔离、成本怎么降。这一课是系统设计轮的主要考点，也是你在 Bedrock 的主场。",
est: "读 85 分钟 + 做题 40 分钟",
sections: [
{h:"1. LLM 服务的两个延迟指标",
b:`普通服务只有一个响应时间，LLM 服务有两个，而且它们的优化方向不同。

**TTFT（Time To First Token）—— 首个 token 的延迟**
用户点了发送，到看见第一个字出现的时间。
- 主要由 **prefill** 决定（prompt 越长越慢），加上排队时间
- 影响「感觉响应快不快」
- 典型目标：**P95 < 1 秒**

**TPOT（Time Per Output Token）—— 每个后续 token 的间隔**
也叫 ITL（Inter-Token Latency）。
- 由 **decode** 决定
- 影响「打字流畅不流畅」
- 典型目标：**P95 < 50 毫秒**（约每秒 20 字，比多数人阅读速度快）

**端到端延迟** = TTFT + (输出长度 − 1) × TPOT

**为什么必须分开看**：
- 一个 TTFT 3 秒但 TPOT 20ms 的服务，用户觉得「启动慢但流畅」
- 一个 TTFT 0.3 秒但 TPOT 200ms 的服务，用户觉得「秒回但一顿一顿的」
- 两者的优化手段完全不同：TTFT 靠 prefix caching、chunked prefill、减少排队；TPOT 靠减小 batch、投机解码、提高单步速度

> **永远看 P95/P99，不看平均值。** LLM 的延迟分布长尾极重（prompt 长度差异巨大），平均值毫无意义。`},

{h:"2. 吞吐和延迟是一对矛盾",
b:`这是 LLM 服务最核心的权衡。

**增大 batch** → 权重读取被更多请求摊薄 → **吞吐上升**
但同时 → 每次 iteration 要算更多东西、请求要排队等批次 → **单请求 TPOT 变差**

画出来是这样：横轴并发数，纵轴同时看吞吐和延迟。
- 并发从 1 涨到某个点：**吞吐几乎线性上升，延迟几乎不变**（这段是白赚的）
- 超过某个点：吞吐增长放缓，**延迟开始快速上升**
- 再往后：KV Cache 打满，开始抢占和重算，**延迟暴涨、吞吐反而下降**

**容量规划的本质，就是找到 SLO 约束下的那个最优点。**

不是最大吞吐，而是「**满足 TTFT P95 < 1s 且 TPOT P95 < 50ms 的前提下，能达到的最大吞吐**」。

**关键参数**：
- \`max_num_seqs\`：最大并发请求数。这是控制吞吐-延迟权衡的主旋钮。
- \`max_num_batched_tokens\`：单次 iteration 的 token 预算。控制 prefill 对 decode 的干扰。
- \`gpu_memory_utilization\`：留给 KV Cache 的显存比例（通常 0.9，留 10% 给 CUDA context 和临时缓冲）。

**SLO 分层是个被低估的手段**：不是所有请求都需要低延迟。把流量分成**交互式**（低延迟优先）和**批处理**（吞吐优先）两类，走不同的池或不同的参数。批处理类可以用大得多的 batch，整体吞吐能提升很多。`},

{h:"3. 容量怎么算：从 QPS 推到 GPU 数",
b:`面试系统设计轮必算。走一遍完整流程。

**① 明确输入**
- 峰值 QPS、峰谷比
- 输入/输出长度的**分布**（要分位数，不是平均值 —— P95 的长请求决定容量）
- SLO：TTFT P95、TPOT P95
- 模型大小与精度

**② 算单实例的并发上限（显存约束）**
\`\`\`
可用KV显存 = 卡显存 × 利用率 − 权重 − 激活/开销
单请求KV = 2 × 层数 × KV头数 × 头维度 × 平均总长 × 字节数
并发上限 = 可用KV显存 ÷ 单请求KV
\`\`\`

例：70B FP8（权重 70GB）、TP=4、80GB 卡 × 0.9 = 72GB 可用
→ 每卡剩 \`72 − 17.5\` = 54.5GB，4 卡共 **218GB** 给 KV
单请求（4k 总长、GQA 8 组、FP16 KV）约 1.3GB
→ **并发上限约 160**

**③ 算单实例的实际吞吐（SLO 约束）**

> **这一步是关键，也是最多人漏掉的：并发上限 ≠ 最优并发。**

要实测在目标 SLO 下的稳定输出速率。假设实测发现并发 64 时 TPOT P95 刚好达标，此时输出 **1200 token/s**。

注意 64 远小于显存允许的 160 —— **延迟才是真正的约束，不是显存**。

**④ 算需求**
\`\`\`
需要的输出token/s = QPS × 平均输出长度
实例数 = 需要的token/s ÷ 单实例吞吐
\`\`\`
QPS=20、平均输出 500 token → 需要 10000 token/s
→ \`10000 / 1200 ≈ 9\` 个实例 → \`9 × 4\` = **36 张卡**

**⑤ 加余量**
- 冗余 N+1
- 峰值缓冲（按峰谷比和扩缩响应时间，留 20-30%）
- 跨故障域分散
→ 实际约 **48 张卡**

**⑥ 用真实流量分布压测验证**，不要用固定长度的合成负载 —— 长度分布对 KV 占用和调度行为影响极大。`},

{h:"4. 多租户：隔离与公平",
b:`共享一个推理池服务多个租户，核心问题是**别让一个租户影响其他人**。

**第一道闸：准入与配额**
- 按租户设 **TPM（tokens per minute）+ RPM（requests per minute）** 双限流
- **token 限流才是真限流** —— 请求数不反映负载（一个 8k prompt 的请求 = 100 个短请求）
- 令牌桶允许突发，但设上限
- 超限直接返回 429 + \`Retry-After\`，**不要让它进队列拖垮别人**

**第二道闸：队列与公平调度**
- 纯 FIFO 的问题：一个租户灌入几百个长请求，后面所有人饿死
- 做法：**per-tenant 队列 + 加权公平调度**（WFQ/DRR），权重按套餐等级
- 计权要**按 token 数而非请求数**，否则长请求的租户占便宜
- 超长 prompt 单独限制或走低优先级池

**第三道闸：执行层隔离**
- **共享池 + 配额**：利用率最高，默认方案
- **独占池**：给需要强隔离或合规的大客户，成本高
- 共享池里的 noisy neighbor 主要靠 **chunked prefill** 控制单次 iteration 的工作量
- 小模型多租户可以用 **MIG** 做硬件隔离
- **LoRA 多适配器**共享基座，服务大量定制模型（这是平台型产品的刚需能力）

**容易漏但很加分的一点：安全隔离**
> **prefix cache 必须按租户隔离命名空间。**
> 否则可以通过命中缓存时 TTFT 明显更短这个时间差，推测其他租户的 prompt 内容。这是真实的信息泄漏风险。

**可观测**：所有指标都要**按租户打标**，否则出问题时无法定位是谁的流量导致的。`},

{h:"5. 扩缩容与冷启动",
b:`**先说什么指标不能用**：

- **CPU 利用率**：完全没用，计算在 GPU 上
- **GPU utilization（nvidia-smi 那个）**：**也不可靠**。它只表示有 kernel 在跑的时间占比。decode 阶段 kernel 一直在跑但算力利用率不到 5%，所以它经常显示 90%+ 却毫无信息量

**该用的指标（按优先级）**：
1. **KV Cache 使用率** —— 最好的扩容信号，直接代表还能接多少并发。超过 85% 就该扩了
2. **队列等待时长的 P95** —— 直接对应用户等待
3. **抢占率** —— 如果引擎频繁抢占请求，说明容量明显不足，这是最干净的过载信号
4. **TTFT P99** —— 贴近体感，但是滞后指标，适合兜底告警而非主信号

**缩容要格外小心**：
- 不能直接杀实例（正在生成的请求会断），需要 **graceful drain**：先从负载均衡摘除 → 等现有请求完成或超时 → 再销毁
- 缩容阈值要比扩容阈值低很多（**滞回**），并加冷却期，否则会在阈值附近反复抖动

**冷启动是扩缩的核心矛盾**：
一个实例从启动到可服务要经历：拉权重（几十到几百 GB）→ 加载进显存 → 建 NCCL 通信域 → warmup / 捕获 CUDA Graph。**可能几分钟。**

扩容决策到实例可用之间有这么长的延迟，所以要么**提前预测扩容**（按历史流量曲线预热），要么维持 **warm pool**（预热好的备用实例，用成本换响应速度）。

**优化冷启动的手段**：权重放节点本地 NVMe 或节点级共享缓存、分片并行下载、镜像预拉、跳过不必要的 warmup。`},

{h:"6. 降本的优先级",
b:`被问到「这个服务每月烧 50 万，怎么降到 20 万」时，**按性价比排序回答，这个顺序本身就是答案**。

**① Prefix / Prompt Caching**（先做，零质量损失）
固定 system prompt、长文档、多轮对话历史命中缓存。输入侧成本可降 **50-90%**。如果你的 system prompt 有几千 token 而对话很多轮，这一项可能单独砍掉三成成本。

**② 精简 prompt + 限制输出长度**
few-shot 从 8 个减到 3 个往往效果一样。**输出 token 通常比输入贵 3-5 倍**，让模型简洁作答收益明显。

**③ 语义缓存**
相似问题直接返回。注意设高相似度阈值，避免答非所问。

**④ 模型路由**
小模型接住简单请求，难的才升级。需要分类器 + 兜底机制。典型能把 60-80% 的流量导到便宜模型。

**⑤ 量化 + 提升批处理并发**
FP8/AWQ + 调 \`max_num_seqs\`，单卡吞吐提升 2-3 倍 = 单位成本降 50-65%。

**⑥ 部署形态优化**
PD 分离提升利用率；批处理流量单独调度用大 batch；spot/预留实例组合。

**⑦ 蒸馏小模型**（长期最优，投入最大）
用大模型造数据微调 7B，垂直任务上常能追平，成本降一个数量级。但要几周工程投入。

**每一步都必须说三件事**：预期收益量级、质量风险、**怎么验证**（A/B + 质量回归门禁）。

**要避免的答法**：罗列一堆手段不排序；一上来说换更小的模型（那是牺牲质量最直接的做法，应该排在后面）；不提验证。

**加分收尾**：「我会先做成本归因，按 租户 × 模型 × 场景 拆出 top 消耗 —— 往往 20% 的场景占 80% 的成本，优化就从那里开始。」`},

{h:"7. 该监控什么，出问题怎么查",
b:`**监控指标清单**：

**性能**（都要 P50/P95/P99）：TTFT、TPOT、端到端延迟、排队时长、吞吐（输入和输出 token/s **分开统计**）、batch size 分布

**资源**：GPU 的 SM 利用率**和**显存带宽利用率（两回事，都要看）、显存占用、**KV Cache 使用率**、抢占率、温度与功耗（降频会导致神秘的性能下降）

**质量**：空回复率、截断率（撞 max_tokens）、JSON 解析成功率、**prefix cache 命中率**（直接对应成本）、抽样送 LLM-Judge 的日常回归

**成本**：每请求 token 成本、按租户/模型分摊

**全链路排查方法**：

必须有 **trace_id 串起整条链路**：网关 → 鉴权 → 路由 → 队列 → 引擎调度 → 前向 → 流式返回。每段打时间戳，能回答「这 3 秒花在哪」。

**延迟拆解**：
\`\`\`
端到端 = 网络 + 鉴权限流 + 排队 + prefill + N×decode + 流式传输
\`\`\`

**TTFT P99 突然涨 3 倍的标准排查流程**：
1. **界定影响面**：所有请求还是某一类？某个模型/租户/区域？什么时间点开始？**有没有伴随变更**（变更是第一嫌疑）
2. **顺延迟链拆解**：排队涨了 → 容量或扩缩问题；排队正常但 prefill 慢 → 看 prompt 长度分布是否变了；GPU 利用率低但延迟高 → CPU 侧瓶颈；KV 打满 → 抢占重算
3. **看 per-instance 分布**：全局平均正常但某实例异常，是最常见的部分用户受影响的根因
4. **动作顺序**：先止血（限流、隔离、扩容、回滚）→ 再定位根因 → 最后补监控和预案

> **面试要点**：面试官看的是**方法论**，不是你猜得对不对。直接说「可能是缓存没命中」会扣分；说「我会先分层缩小范围，第一步看影响面和变更」会加分。`}
],
quiz: [
{id:"if6q1", type:"num", q:"一个请求 TTFT 是 800ms，TPOT 是 40ms，生成了 200 个 token。端到端延迟是多少秒？（保留一位小数）",
hint:"端到端 = TTFT + (输出长度 − 1) × TPOT。注意第一个 token 已经算在 TTFT 里了。", ans:8.8, tol:0.2,
why:"\`800 + (200−1) × 40\` = \`800 + 7960\` = 8760 ms ≈ **8.8 秒**。\n\n**注意是 199 不是 200** —— 第一个 token 已经包含在 TTFT 里了。\n\n**这个计算揭示了一件重要的事**：长输出场景下，**TPOT 才是主导因素**。这里 TPOT 贡献了 7960ms，占 91%。\n\n所以优化方向要看场景：\n- **短输出**（分类、抽取）：优化 TTFT 更重要 → prefix caching、减少排队\n- **长输出**（写作、代码生成）：优化 TPOT 更重要 → 减小 batch、投机解码、提高单步速度\n\n很多人只盯着 TTFT，在长输出场景下优化错了方向。"},

{id:"if6q2", type:"mc", q:"给 LLM 推理服务做自动扩缩，最好的主信号是什么？",
choices:["CPU 利用率",
         "nvidia-smi 显示的 GPU utilization",
         "KV Cache 使用率 + 队列等待时长",
         "每分钟请求数"], ans:2,
hint:"想想什么指标能直接回答这个实例还能再接多少请求。CPU 和那个 GPU util 分别有什么问题？",
why:"**KV Cache 使用率 + 队列等待时长。**\n\n- **KV Cache 使用率**直接代表还能接多少并发 —— 它是 LLM 服务真正的容量约束。超过 85% 就该扩容。\n- **队列等待时长**直接对应用户体验的恶化。\n\n其余为什么不行：\n- **CPU 利用率**：计算在 GPU 上，CPU 常年低占用，完全没有信息量\n- **GPU utilization**：这是个**陷阱指标**。它只表示有 kernel 在跑的时间占比，不代表算力被用满。decode 阶段 kernel 一直在跑但算力利用率不到 5%，所以它经常显示 90%+ 却什么也说明不了\n- **QPS**：不反映负载。一个 8k prompt 的请求约等于 100 个短请求，按请求数扩缩会严重误判\n\n**补充两个好用的辅助信号**：\n- **抢占率**：引擎频繁抢占请求 = 容量明显不足，这是最干净的过载信号\n- **TTFT P99**：贴近体感，但滞后，适合做兜底告警\n\n能主动指出 GPU util 也不可靠这一层，比只说 CPU 没用深一档。"},

{id:"if6q3", type:"num", q:"QPS = 10，平均输出长度 400 token。单个实例在 SLO 约束下实测吞吐是 800 token/s。不算冗余的话需要几个实例？",
hint:"先算总共需要多少 token/s（QPS × 平均输出长度），再除以单实例吞吐，向上取整。", ans:5,
why:"需要的吞吐：\`10 × 400\` = **4000 token/s**\n实例数：\`4000 / 800\` = **5 个**\n\n**但实际部署要加余量**：\n- **冗余 N+1**：一个实例故障不能影响 SLO → 6 个\n- **峰值缓冲**：按峰谷比和扩缩响应时间留 20-30% → 7-8 个\n- **跨故障域分散**：不能都放一个机架\n\n**这道题最重要的一个前提，容易被忽略**：那个 800 token/s 必须是**在 SLO 约束下实测的**，不是峰值吞吐。\n\n显存可能允许并发 160，但如果在并发 64 时 TPOT P95 就已经触及红线，那你的有效吞吐就是并发 64 时的数字。**用峰值吞吐算容量会严重低估需求。**\n\n并发上限不等于最优并发，是这道题真正想考的点。"},

{id:"if6q4", type:"mc", q:"多租户推理服务里，下面哪种限流方式最合理？",
choices:["按每分钟请求数（RPM）限流",
         "按每分钟 token 数（TPM）限流，辅以 RPM",
         "按并发连接数限流",
         "不限流，靠队列自然排队"], ans:1,
hint:"一个 8000 token 的长请求和一个 50 token 的短请求，对 GPU 的负载一样吗？哪个指标更能反映真实资源消耗？",
why:"**TPM 为主、RPM 为辅。**\n\n**token 数才真实反映负载**。一个 8k prompt 的请求消耗的资源约等于 100 个短请求。只按 RPM 限流，一个租户完全可以用少量长请求打爆整个池子，而限流器毫无察觉。\n\n**为什么还要辅以 RPM**：防止大量极短请求把调度器打满（每个请求都有固定的调度、鉴权、日志开销）。两者配合才完整。\n\n其余为什么不行：\n- **只按 RPM**：无法反映长请求的真实消耗\n- **按并发连接数**：流式响应下连接会长期保持，这个指标和资源消耗关系不直接\n- **不限流靠排队**：队列会无限增长，导致所有人的延迟都崩，而且一个租户能饿死所有人。**过载时必须拒绝，而不是让所有人一起慢**\n\n**完整方案还要加**：per-tenant 队列 + 加权公平调度（按 token 计权），超限返回 429 + Retry-After。"},

{id:"if6q5", type:"text", q:"一个服务每月成本 50 万，要降到 20 万。按性价比，第一个该做的优化是什么？（填名称）",
ans:["prefix caching","prefix cache","前缀缓存","prompt caching","提示缓存","prefixcaching"],
hint:"哪个手段是零质量损失且不需要改模型的？想想如果有固定的长 system prompt，每次请求都要重新计算它，这个浪费有多大。",
why:"**Prefix / Prompt Caching。**\n\n它是唯一一个**零质量损失**、实施成本极低、收益却可能极大的手段。固定的 system prompt、长文档、多轮对话历史命中缓存后，输入侧成本能降 **50-90%**。\n\n如果你的服务有几千 token 的 system prompt 而对话轮数多，这一项可能单独就砍掉三成成本 —— 而且**输出质量完全不变**。\n\n**完整的优先级顺序**（这个排序本身就是答案）：\n1. **Prefix caching**（零质量损失，先做）\n2. 精简 prompt + 限制输出长度（输出 token 比输入贵 3-5 倍）\n3. 语义缓存\n4. 模型路由（小模型接住简单请求）\n5. 量化 + 提升批处理并发\n6. 部署形态优化（PD 分离、spot 实例）\n7. 蒸馏小模型（长期最优但投入大）\n\n**面试时要避免的答法**：一上来说换更小的模型 —— 那是牺牲质量最直接的做法，应该排在最后面。\n\n**加分收尾**：先做成本归因，按 租户 × 模型 × 场景 拆出 top 消耗，往往 20% 的场景占 80% 的成本。"},

{id:"if6q6", type:"open", q:"线上 TTFT 的 P99 突然涨了三倍，你怎么查？描述你的排查步骤。",
hint:"不要直接猜原因。先想：怎么快速缩小范围？延迟由哪几段构成，每一段有什么可观测的信号？",
ref:"**核心原则：先分层缩小范围，不要猜原因。面试官看的是方法论。**\n\n**① 界定影响面（两分钟内要有答案）**\n- 所有请求还是某一类？某个模型 / 租户 / 区域 / 实例？\n- 什么时间点开始？阶跃还是渐变？\n- **有没有伴随变更**（发版、配置、模型切换、流量结构变化）—— **变更永远是第一嫌疑**，先看变更日志\n\n**② 顺着延迟链逐段拆解**\n\`端到端 = 网络 + 鉴权限流 + 排队 + prefill + 首token返回\`\n\n看指标定位到哪一段：\n- **排队时长涨了** → 容量不足或扩缩失效。查 KV Cache 使用率、运行中请求数、抢占率。是流量涨了还是实例少了？\n- **排队正常但 prefill 慢** → 看 **prompt 长度分布是否变了**。最常见的真因：某个租户开始发超长 prompt，而 chunked prefill 没配或预算设得太大\n- **GPU 利用率低但延迟高** → CPU 侧瓶颈（tokenize、调度、序列化）、锁竞争、或 CUDA Graph 失效退回 eager 模式\n- **KV Cache 打满** → 请求被抢占后重算，延迟翻倍\n\n**③ 看 per-instance 的指标分布**\n全局平均正常但某个实例异常，是最常见的部分用户受影响的根因。一个降频或坏卡的节点会拖高整体 P99（P99 本来就反映最差的那部分）。\n\n**④ 常见真因清单（按概率）**\n1. 流量结构变化（长 prompt 变多）\n2. prefix cache 命中率骤降（缓存被清、路由亲和性失效）\n3. 某节点变慢（ECC、降频、网络劣化）\n4. 扩缩容失效或冷启动卡住（镜像/权重下载慢）\n5. 上游依赖变慢（鉴权、日志、向量库）\n\n**⑤ 动作顺序：先止血，再定位，最后补防线**\n- **止血**：限流压住异常租户、隔离坏节点、手动扩容、回滚变更\n- **定位**：拿 trace 采样、在测试环境复现\n- **补防线**：加缺失的监控、加自动化防护（超长 prompt 自动降级）、写 postmortem\n\n**加分项**：主动提到必须有 trace_id 串起全链路并能回放请求 —— 这是 oncall 的基本功，没有它所有排查都是猜。"}
]
};
