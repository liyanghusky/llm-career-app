/* Inference & ML Infra 主攻线 · 深度题库（二）
   分布式推理 / 分布式训练 / 服务化与平台 / 成本与容量 / 性能调试
*/
window.INFRA_QA = window.INFRA_QA.concat([

/* ══════════ 分布式推理 ══════════ */
{id:"di1",cat:"dinf",level:2,tags:["TP"],q:"推理里的张量并行怎么切？通信开销多大，收益拐点在哪？",
a:`**怎么切**（Megatron 式，关键是让通信次数最少）：
- **注意力**：按**头**切。每张卡负责一部分头，算完各自的 \`O = softmax(QKᵀ)V\`，最后对输出投影做一次 All-Reduce。
- **FFN**：第一个矩阵 \`W1\` **按列切**（每卡产出一部分中间维），第二个矩阵 \`W2\` **按行切**，这样中间的激活不需要通信，只在 \`W2\` 之后做一次 All-Reduce。
- **Embedding / lm_head**：按词表维切，需要一次 All-Gather 或分布式 softmax。

所以**每个 transformer 层有 2 次 All-Reduce**（注意力后 + FFN 后）。

**通信量**：每次 All-Reduce 传输约 \`2 × batch × seq × hidden × bytes × (TP−1)/TP\`。
decode 时 seq=1，单层单次约 \`2 × B × hidden × 2\` 字节 —— 数据量不大，但**次数极多**（层数 × 2），所以瓶颈是**延迟而非带宽**。

**算个例子**：70B 模型 80 层，TP=8，batch=32，hidden=8192，BF16：
单次 All-Reduce 约 \`32 × 8192 × 2 × 2 × 7/8\` ≈ 0.9 MB。160 次 → 约 145 MB/token。NVLink 900 GB/s 下理论 0.16 ms，但**每次 All-Reduce 有固定延迟（几微秒到几十微秒）**，160 次的固定开销就有几毫秒 —— 这往往才是主要成本。

**收益拐点**：
- TP 翻倍，单卡权重和 KV 减半（好），但通信次数不变而每次的固定延迟占比上升（坏）
- 同时单卡的 GEMM 变小，M/N 维缩水，**Tensor Core 利用率下降**
- 实践中 **TP=8 附近是拐点**（刚好是 NVLink 全互联域的大小）。TP=16 跨机后通信成本暴涨，几乎总是不划算

**优化手段**：
- **序列并行**配合 TP：把 LayerNorm/Dropout 这些逐 token 操作按序列切，减少激活冗余（Megatron-SP）
- 通信与计算重叠（把 All-Reduce 拆开与 GEMM 流水）
- 用 **All-Gather + Reduce-Scatter 替代 All-Reduce** 配合序列并行，总通信量相同但能省激活显存`,
ask:"必问。要能算数并说出 TP=8 的拐点理由。"},

{id:"di2",cat:"dinf",level:2,tags:["PP"],q:"为什么推理里很少用流水线并行？什么情况下会用？",
a:`**训练里 PP 很好用**，因为有大量 micro-batch 可以填流水线气泡 —— 一个 batch 切成 32 个 micro-batch，气泡率只有 \`(stage−1)/(micro+stage−1)\`，很小。

**推理里 PP 很难用**，原因：

**① decode 阶段没有 micro-batch 可切。** 每一步只生成 1 个 token，这个 token 必须串行穿过所有 stage。4 个 stage 就是 4 段串行，**气泡率接近 75%** —— 大部分卡在等。
**② 延迟直接变差。** 单 token 的延迟变成所有 stage 的延迟之和，TPOT 显著上升。
**③ 显存收益和 TP 一样，但延迟代价大得多。** 既然都要切，不如切 TP（机内 NVLink 快）。

**什么时候会用 PP**：

1. **跨机不得不用**。模型太大，单机 8 卡（TP=8）也放不下，必须跨机 —— 这时 TP 跨机代价更高，用 PP 跨机 + TP 机内是标准组合。比如 405B 模型：TP=8（机内）× PP=2（两台机）。
2. **吞吐优先、延迟不敏感的批处理场景**。离线批量推理时可以把很多请求当作 micro-batch 填流水线，气泡就被填掉了，PP 变得高效。
3. **异构硬件拼凑**。不同型号的卡凑一个流水线（每个 stage 用一种卡）。

**答题要点**：不要只说「PP 不适合推理」，要说**为什么**（decode 没有 micro-batch → 气泡填不满），以及**什么时候例外**（跨机 + 离线批处理）。这个「说清边界」的习惯在系统设计轮里很值钱。`,
ask:"追问并行策略时的好题。"},

{id:"di3",cat:"dinf",level:3,tags:["MoE"],q:"MoE 推理的核心瓶颈是什么？专家并行怎么优化？",
a:`**先纠正一个常见误解：MoE 推理不省显存。**
Mixtral 8x7B 总参 47B、激活 13B，但**全部 8 个专家都必须在显存里**（任何 token 都可能路由到任何专家）。所以显存按 47B 算，只有算力按 13B 算。

**核心瓶颈：All-to-All 通信。**
专家分布在不同卡上（专家并行 EP），每个 MoE 层需要：
1. **dispatch**：把每个 token 发送到它被路由到的专家所在的卡（All-to-All）
2. **combine**：把各专家的输出收回到 token 原本所在的卡（再一次 All-to-All）

所以**每个 MoE 层两次 All-to-All**。All-to-All 的特点是所有卡对所有卡传输，对网络拓扑极其敏感，跨机时是明确的瓶颈。

**第二个瓶颈：负载不均 + 小矩阵。**
- 热门专家被打爆、冷专家闲置 → 实际吞吐被最慢的卡决定
- 小 batch 时每个专家只分到几个 token，GEMM 退化成极瘦的矩阵，**Tensor Core 利用率极低**。这就是为什么 **MoE 推理特别需要大 batch** 才划算

**优化手段**：
- **专家放置策略**：把常一起被激活的专家放同一卡/同机，减少跨机流量；按历史统计把热门专家多放副本
- **通信与计算重叠**：dispatch 发出后立刻开始算已收到的部分（DeepSeek 的 DualPipe 思路）
- **共享专家**（DeepSeekMoE）：一部分专家所有 token 都过，不需要路由通信，只有细粒度专家走 All-to-All
- **容量因子调优**：设每个专家的 token 上限，超出丢弃或回退到共享专家，避免长尾拖慢整批
- **分组路由限制**：限制一个 token 最多跨几个节点（DeepSeek 的 node-limited routing），直接砍掉跨机 All-to-All 的扇出

**面试甄别题**：「MoE 推理为什么不省显存」—— 答不出来说明没真部署过。`,
ask:"DeepSeek/Mixtral 之后的常见追问。"},

{id:"di4",cat:"dinf",level:3,tags:["长上下文"],q:"超长上下文推理怎么做分布式？Ring Attention 的原理是什么？",
a:`**问题**：128k、1M 上下文时，注意力的 \`O(N²)\` 计算和 \`O(N)\` 的 KV Cache 都超出单卡能力。TP 切的是 hidden 维，对序列长度无能为力。

**序列并行 / 上下文并行（Context Parallel）**：把**序列**切到多张卡上，每卡持有一段 token 的 Q/K/V。

难点是注意力**需要全局信息** —— 每个 Q 要和所有的 K/V 交互，而 K/V 分散在各卡。

**Ring Attention 的解法**：
1. 每张卡先用本地的 Q 和本地的 K/V 算一部分注意力
2. 然后把自己的 K/V **环形传给下一张卡**，同时接收上一张卡的 K/V
3. 用本地 Q 和新收到的 K/V 继续累积注意力（用 **online softmax** 累积，和 FlashAttention 同一个机制）
4. 转 \`P−1\` 轮后，每张卡的 Q 都见过了全部 K/V

**关键优势**：K/V 的传输可以和注意力计算**重叠** —— 算当前块的同时异步收下一块。如果计算时间 ≥ 传输时间，通信就被完全隐藏，实现近线性扩展。这要求 \`每块计算量 / 每块传输量\` 足够大，即**块要够大**（序列够长），所以它只在长序列上划算。

**变体**：
- **Striped Attention**：解决因果掩码导致的负载不均（朴素切分下，靠后的卡要算更多有效注意力）。做法是把 token 按 stripe 交错分配而非连续切块。
- **Ulysses（DeepSpeed）**：用 All-to-All 在序列维和头维之间转置，让每卡持有全部序列但部分头。通信量更小但要求头数能被卡数整除。
- 生产上常把两者结合（\`USP\`：Unified Sequence Parallelism）。

**KV Cache 的分布式管理**：长上下文 + 多轮对话时，KV 本身成为需要跨机管理的状态。**Mooncake** 把 KV 做成分布式多级缓存池（HBM/DRAM/SSD），用 RDMA 传输，让 prefill 池和 decode 池共享 KV。

**面试价值**：能讲清「Ring Attention 用 online softmax 累积 + 通信计算重叠」就已经很扎实。`,
ask:"长上下文方向的深度题。"},

{id:"di5",cat:"dinf",level:2,tags:["容错"],q:"多机推理服务的容错和副本管理怎么设计？",
a:`**和无状态微服务的关键区别：一个模型实例横跨多张卡甚至多台机，任何一张卡挂了整个实例就不可用。**

**① 故障域的定义。**
一个"实例"是 TP×PP 的完整组合（比如 TP=8 = 一台机）。健康检查必须以**实例**为单位，不是以进程或 pod 为单位。一张卡 ECC 错误 → 整个实例摘除。

**② 快速摘除与排水。**
- 负载均衡器要能在秒级摘除故障实例（主动健康探针 + 请求失败率熔断）
- 摘除时**正在生成的请求怎么办**：要么在客户端重试（需要幂等和请求缓存），要么接受这批请求失败。生成类请求没法简单重放（已经吐出去的 token 收不回），通常做法是流式场景直接断开让客户端重试。

**③ 副本粒度与冗余。**
- 副本数至少 N+1，且**跨故障域分布**（不同机架/不同电源域/不同交换机）
- 大模型副本很贵（一个 70B TP=4 实例占 4 张卡），所以冗余成本高 → 更依赖快速恢复而非厚冗余

**④ 启动时间是关键指标。**
一个实例重启要：拉权重（几十到几百 GB）→ 加载进显存 → 建 NCCL 通信域 → warmup/捕获 CUDA Graph。可能几分钟。
优化：权重放本地 NVMe 或节点级共享缓存、并行分片下载、镜像预热、维持 **warm pool**（预热好的备用实例）。

**⑤ PD 分离带来的额外复杂度。**
prefill 池和 decode 池分离后，KV 传输链路本身也是故障点。需要：传输失败时回退到「在 decode 侧重新 prefill」、以及两个池的容量独立监控与扩缩。

**⑥ NCCL 相关的坑**（能说出来很加分）：
- NCCL 通信域一旦建立，成员挂掉会导致其他成员**hang 而不是报错**，需要设 \`NCCL_TIMEOUT\` 并把超时当作故障信号
- 摘除后重建通信域需要协调，不能让半个实例活着

**要点**：强调「有状态 + 故障域大 + 启动慢」这三个和普通服务不同的地方，然后每一条给对策。`,
ask:"平台岗深度题。你在 Bedrock 应该有相关经验，值得挖成故事。"},

/* ══════════ 分布式训练 ══════════ */
{id:"t1",cat:"dtrain",level:2,tags:["并行"],q:"给你 512 张 H100 训一个 70B 模型，你怎么定并行策略？",
a:`**按这个顺序推导，每一步都有依据**：

**① 先算显存需求。** 70B AdamW 混合精度 = \`16N\` = 1120 GB 状态，加激活。单卡 80GB → 至少需要把状态切到 16 张卡以上。

**② 定 TP。** TP 必须在机内（NVLink），所以 \`TP ≤ 8\`。70B 的 hidden 约 8192，TP=8 时每卡 1024 —— GEMM 还不算太瘦，可接受。**取 TP=8**（一台机）。

**③ 定 PP。** 剩下的显存压力用 PP 或 ZeRO 解决。70B 有 80 层，PP=4 则每 stage 20 层。气泡率 \`(4−1)/(micro+3)\`，micro_batch 取 32 时约 8.6% —— 可接受。**取 PP=4**（4 台机）。
- 一个模型副本 = TP8 × PP4 = **32 卡 = 4 台机**

**④ 定 DP。** 512 / 32 = **DP=16**。

**⑤ 叠加 ZeRO-1。** 在 DP 维上切分优化器状态（省 4 倍且几乎不增加通信），这是"免费"的。不用 ZeRO-3，因为已经有 TP+PP 切分了模型，ZeRO-3 的额外 All-Gather 是浪费。

**⑥ 全局 batch size 与学习率。**
\`global_batch = DP × micro_batch × grad_accum × seq_len\`。要落在合理区间（太小则 GPU 利用率低、通信占比高；太大则收敛变差、需要按 √ 或线性规则调学习率）。通常目标是 **4M 左右的 token/step**。

**⑦ 其他必开项**：
- 梯度检查点（选择性开，只在激活大的层）—— 用 30% 时间换 60-70% 激活显存
- 序列并行配合 TP（省激活）
- BF16 混合精度
- \`clip_grad_norm=1.0\`
- FlashAttention

**⑧ 验证方式**：先用小规模（1-2 台机）跑 scaling 测试，测 MFU（Model FLOPs Utilization）。**健康的大规模训练 MFU 应在 40-55%**，低于 35% 说明有明显瓶颈要查。

**答题关键**：每一步都说出**为什么**（带宽层级 → TP 机内；气泡率公式 → PP 层数；ZeRO-1 免费 → 叠加）。只报一组数字是不够的。`,
ask:"训练岗必问。MFU 这个数值报得出来很专业。"},

{id:"t2",cat:"dtrain",level:2,tags:["ZeRO"],q:"ZeRO 三个阶段分别切什么？ZeRO-3 和张量并行的本质区别是什么？",
a:`**ZeRO 优化的是数据并行的显存冗余** —— DP 下每张卡都存一份完整的参数、梯度、优化器状态，全是重复的。

- **Stage 1**：切分**优化器状态**（Adam 的 m、v）。省约 4 倍（16N → 4N + 12N/DP）。通信不变（还是一次 All-Reduce，实际改成 Reduce-Scatter + All-Gather，总量相同）。**几乎免费，总是该开。**
- **Stage 2**：再切分**梯度**。省约 8 倍。通信量仍与 DP 相当。
- **Stage 3**：再切分**参数本身**。省约 N 倍。代价是前向和反向**每一层都要 All-Gather 参数**（用完立即释放），**通信量增加约 50%**。

**FSDP** 是 PyTorch 原生的 ZeRO-3 等价实现，按 module 包装，支持 \`reshard_after_forward\` 等策略调节显存-通信权衡。

**和 TP 的本质区别** —— 这是高区分度的问题：

| | ZeRO-3 | 张量并行 |
|---|---|---|
| 切什么 | 参数在 **DP 维**上分片存储 | 单个矩阵在**计算维**上真正切开 |
| 计算时 | **先 All-Gather 还原完整权重**，再各自算完整的层 | 每卡只算自己那一片，**从不还原** |
| 通信性质 | 参数搬运（可预取、可与计算重叠） | 计算中途的归约（在关键路径上） |
| 扩展性 | 受总带宽限制，可跨机 | 受延迟限制，**必须机内** |

一句话：**ZeRO-3 是"用通信换显存"的存储优化，TP 是真正的计算切分。** ZeRO-3 的通信可以预取隐藏，TP 的 All-Reduce 在关键路径上躲不开。

**实践选择**：
- 中等规模（模型能在 TP≤8 内放下）：TP + PP + DP + ZeRO-1
- 超大规模或不想用 PP：FSDP/ZeRO-3 + TP
- ZeRO-Offload/Infinity（换到 CPU/NVMe）：能跑但慢，是"跑得动"而非"跑得快"的方案

**面试价值**：能讲清「All-Gather 还原 vs 从不还原」这个区别，就说明真理解了。`,
ask:"「ZeRO-3 和 TP 有什么区别」是很好的深度题。"},

{id:"t3",cat:"dtrain",level:2,tags:["NCCL"],q:"Ring All-Reduce 的通信量是多少？怎么让通信和计算重叠？",
a:`**Ring All-Reduce = Reduce-Scatter + All-Gather，两个阶段各 \`P−1\` 步。**

设数据量 \`D\`、卡数 \`P\`。每一步每张卡发送 \`D/P\`：
- Reduce-Scatter：\`P−1\` 步 × \`D/P\` = \`D(P−1)/P\`
- All-Gather：同样 \`D(P−1)/P\`
- **总计每卡收发 \`2D(P−1)/P ≈ 2D\`**

**关键性质：通信量近似与卡数无关**（P 大时趋于 2D）。这就是 Ring All-Reduce 能扩展的根本原因。代价是**延迟随 P 线性增长**（\`2(P−1)\` 步），所以超大规模下改用分层算法（机内 ring + 机间 tree/双二叉树），或 NVLS（NVSwitch 硬件归约）。

**怎么重叠通信与计算**：

**① 梯度分桶（bucketing）** —— PyTorch DDP 的核心机制。反向传播是从后往前算的，所以最后几层的梯度先出来。把参数分成若干桶（默认 25MB），**一个桶的梯度凑齐就立刻发起该桶的 All-Reduce**，不等整个反向结束。这样通信和后续层的反向计算重叠。
- 调 \`bucket_cap_mb\`：太小则通信次数多、固定开销占比高；太大则重叠机会少。25MB 是经验值，大模型可以调大。
- \`gradient_as_bucket_view=True\` 省一次拷贝。

**② ZeRO-3 / FSDP 的参数预取** —— 算第 i 层时，异步 All-Gather 第 i+1 层的参数（\`forward_prefetch\`、\`backward_prefetch\`）。

**③ 独立 CUDA stream** —— 通信放在单独的 stream，用 event 同步而不是全局 barrier。

**④ TP 里的细粒度重叠** —— 把 All-Reduce 拆成多块，与 GEMM 的分块计算交错（Megatron 的 \`--tp-comm-overlap\`）。

**怎么验证重叠有效**：Nsight Systems 看时间线，NCCL kernel 应该和计算 kernel **在时间上并行**而不是串排。如果看到明显的「算一段 → 停下来传 → 再算」的锯齿，说明重叠没生效。

**诊断工具**：\`NCCL_DEBUG=INFO\` 看它选的算法和拓扑对不对（有时会退化到走 PCIe 而不是 NVLink，吞吐差十几倍）。`,
ask:"Meta/NVIDIA 会问。能说出 bucketing 和怎么验证很实用。"},

{id:"t4",cat:"dtrain",level:2,tags:["检查点"],q:"梯度检查点的成本模型是什么？怎么决定在哪些层开？",
a:`**机制**：正常反向传播需要缓存所有中间激活，显存 \`O(L)\`。梯度检查点只保存少数几层的激活，反向时**重新前向计算**中间结果。

**成本模型**：
- 经典策略每 \`√L\` 层存一个检查点，显存降到 \`O(√L)\`
- 代价是多一次前向：反向的计算量从 2× 前向变成 3× 前向 → **总训练时间增加约 30%**（前向 1 + 反向 2 = 3，加一次重算 → 4，4/3 ≈ 1.33）
- 显存节省通常 **60-70%** 的激活部分

**全开 vs 选择性开**：全开是浪费的，因为不同层的「激活显存 / 重算代价」比值差别很大。

**该优先开的层**：
- **注意力层**（尤其长序列）—— 激活显存随 \`seq²\` 或 \`seq\` 增长，是大头。但如果已经用了 FlashAttention，注意力的中间矩阵本来就不物化，收益变小
- **FFN 的中间激活** —— 4× hidden 的那个张量很大，而重算只是一次 GEMM，性价比高
- **靠前的层** —— 它们的激活要存活最久

**不该开的层**：
- 计算很贵但激活很小的（比如已经融合优化过的算子）
- 已经被其他机制优化掉的（FlashAttention 覆盖的部分）

**实操方法**：
1. 先全不开，看 OOM 在哪、激活占多少（\`torch.cuda.memory_summary()\` 或 profiler 的 memory timeline）
2. 按「激活大小 ÷ 重算 FLOPs」排序，从高到低逐步开，直到刚好不 OOM
3. 测 MFU 确认时间代价符合预期（不该超过 35%）

**和其他手段的取舍顺序**：
显存不够时的优先级通常是 **ZeRO-1（免费）→ 序列并行（省激活且几乎免费）→ 选择性梯度检查点 → ZeRO-2/3 → offload（最后手段）**。梯度检查点排在中间，因为它的时间代价是确定且可观的。

**面试要点**：能报出「+30% 时间换 60-70% 激活显存」这个量级，并说出选择性开启的判据，比只说概念强很多。`,
ask:"训练显存不够时的标准追问。"},

{id:"t5",cat:"dtrain",level:3,tags:["故障"],q:"千卡训练的故障有哪几类？checkpoint 频率怎么定最优？",
a:`**千卡规模下硬件故障是常态** —— 几千卡的训练平均几小时就会有卡或链路出问题。

**故障分类与检测**：

**① 硬故障（fail-stop）** —— GPU ECC 不可纠错、XID 错误、节点掉线、NCCL 超时。
检测：心跳 + \`NCCL_TIMEOUT\`（注意 NCCL 成员挂掉时其他成员会 **hang 而不报错**，必须靠超时把 hang 转成可处理的故障信号）。
处理：隔离坏节点 → 从最近 checkpoint 恢复，或用 torchelastic 弹性降级继续。

**② 静默数据损坏（SDC）** —— **最可怕的一类**。某张卡算错了但不报错，表现为 loss 突然发散或缓慢变差。
检测：定期跨副本比对（同样输入在不同 DP rank 上算，结果应一致）、loss spike 自动告警、定期用已知输入做数值校验。
处理：回滚到 spike 之前的 checkpoint，隔离可疑节点后重启。

**③ 慢节点（straggler）** —— 一张卡因为降频、风扇故障、或网络劣化变慢，因为集合通信是同步的，**整个集群被它拖住**。
检测：监控 **per-rank 的 step 时间分布**，离群值告警。这个监控很多团队缺，是个高价值的改进点。
处理：自动驱逐并用备用节点替换。

**④ 网络劣化** —— 某条 IB 链路错误率上升，带宽掉但不断。检测靠 \`perfquery\` 类工具监控链路错误计数。

**Checkpoint 频率的最优点**：

设 checkpoint 写入耗时 \`C\`、平均故障间隔 \`MTBF\`、checkpoint 间隔 \`T\`。
- 每次故障平均损失重算时间 \`T/2\`
- 单位时间的总开销 ≈ \`C/T\`（写入开销占比）\`+ (T/2)/MTBF\`（重算开销占比）
- 对 T 求导置零 → **\`T_opt = √(2 × C × MTBF)\`**（Young/Daly 公式）

**举例**：C=60 秒，MTBF=4 小时（14400 秒）→ \`T_opt = √(2×60×14400) ≈ 1315 秒 ≈ 22 分钟\`。

**降低 C 的手段**（让你能更频繁地存）：
- **异步 checkpoint**：先快速拷到 CPU 内存（几秒），后台再慢慢写到持久存储，训练不阻塞
- **分片保存**：每个 rank 只写自己那份（\`torch.distributed.checkpoint\`），并行写盘
- 只在需要时保存完整的可恢复状态（优化器状态很大），中间的可以只存权重

**面试价值**：这是**资深候选人的区分题**。能讲 SDC 和 straggler 的人明显比只会讲并行策略的更像做过真实大规模训练。你在 AGI 如果碰过训练中断或 loss spike，这是最值钱的故事。`,
ask:"资深区分题。碰过就一定要讲。"},

{id:"t6",cat:"dtrain",level:2,tags:["MFU"],q:"什么是 MFU？怎么算？多少算健康？低了怎么查？",
a:`**MFU（Model FLOPs Utilization）** = 模型理论需要的 FLOPs ÷ (硬件峰值算力 × 时间)。它衡量「你真正用上了硬件多少算力」。

**怎么算**：
Transformer 训练的理论 FLOPs 约为 \`6 × N × D\`（N 参数量，D 训练 token 数）—— 前向 2N、反向 4N。如果开了梯度检查点要按 8N 算（多一次前向）。

\`\`\`
MFU = (6 × N × tokens_per_step) / (step_time × GPU数 × 单卡峰值FLOPS)
\`\`\`

**举例**：70B 模型，512 张 H100（BF16 峰值 989 TFLOPS），每 step 4M token，step 时间 12 秒：
\`(6 × 70e9 × 4e6) / (12 × 512 × 989e12)\` ≈ **27.6%**

**健康区间**：
- 大规模训练 **40-55% 是好成绩**（公开报告里 GPT-3 约 21%，PaLM 约 46%，LLaMA-3 约 40%+）
- **低于 35% 说明有明显瓶颈**，值得查
- 超过 60% 不太可能（通信、数据加载、优化器开销必然存在）

**MFU 低的排查顺序**：

1. **数据加载**：GPU 在等数据吗？看 profiler 里的 dataloader 时间占比。解法：加 worker、预取、\`pin_memory\`、数据 packing、把 tokenization 离线做掉。
2. **通信占比**：Nsight Systems 看 NCCL kernel 占多少、有没有和计算重叠。锯齿状说明没重叠。解法：调 bucket、开 TP 通信重叠、检查 NCCL 是否走对了拓扑。
3. **流水线气泡**：PP 的气泡率算一下，micro_batch 够不够多。
4. **算子效率**：有没有用 FlashAttention、有没有开 \`torch.compile\`、GEMM 形状是不是太瘦（hidden/TP 太小）。
5. **梯度检查点开太多**：它是确定的 ~30% 时间成本，检查是否过度。
6. **负载不均**：per-rank step 时间分布，有没有 straggler。

**为什么面试官在意**：MFU 是训练侧最重要的单一效率指标。能报出公式、健康区间、以及排查顺序，说明你真的关注过效率而不只是"能跑起来"。`,
ask:"训练岗高频。这个指标很多人说不清。"},

/* ══════════ 服务化与平台 ══════════ */
{id:"s1",cat:"serve",level:2,tags:["多租户"],q:"多租户 LLM 服务怎么做隔离和公平调度？",
a:`**这是你在 Bedrock 的主场，一定要答得比别人细。**

**① 准入与配额（第一道闸）**
- 按租户设 **TPM（tokens per minute）+ RPM（requests per minute）** 双限流，token 限流才是真限流（请求数不反映负载）
- 令牌桶允许一定突发，但设突发上限
- 超限返回 429 + \`Retry-After\`，而不是让它进队列拖垮别人

**② 队列与公平调度（第二道）**
- 纯 FIFO 的问题：一个租户灌入几百个长请求，后面所有人饿死
- 做法：**per-tenant 队列 + 加权公平调度（WFQ/DRR）**，权重按套餐等级。每轮从各队列按权重取请求
- 另一个维度：**按 token 数而非请求数计权**，否则长请求的租户占便宜
- 长请求单独限制：\`max_tokens\` 上限、超长 prompt 走专门的低优先级池

**③ 执行层隔离（第三道）**
- **共享池 + 配额** 是默认（利用率最高）；**独占池** 给需要强隔离/合规的大客户（成本高）
- 共享池里的 noisy neighbor 问题主要靠 chunked prefill 控制单次 iteration 的工作量
- 小模型多租户可以用 **MIG** 做硬件隔离
- **LoRA 多适配器** 共享基座，服务大量定制模型

**④ SLO 分层**
把流量分成 **交互式（低延迟优先）** 和 **批处理（吞吐优先）** 两类，走不同的池或不同的调度参数。批处理类可以用更大 batch、更激进的排队，整体吞吐能提升很多。这一条最容易被忽略但收益很大。

**⑤ 安全隔离（容易漏，提了加分）**
- **prefix cache 必须按租户隔离命名空间** —— 否则能通过命中时延推测别的租户的 prompt 内容
- 日志和 trace 里的 prompt/输出要做租户级访问控制

**⑥ 可观测**
所有指标（TTFT/TPOT/错误率/配额命中）都要**按租户打标**，否则出问题无法定位是谁的流量导致的。

**回答结构**：准入 → 队列 → 执行 → SLO 分层 → 安全 → 可观测。每层说清机制和代价。`,
ask:"你的主场题。会被问「怎么防止一个租户打爆共享容量」。"},

{id:"s2",cat:"serve",level:2,tags:["扩缩"],q:"LLM 服务的自动扩缩该用什么指标？为什么 CPU 利用率没用？",
a:`**CPU 利用率完全没用**，因为计算在 GPU 上，CPU 只是喂数据和跑调度，常年低占用。**GPU 利用率（\`nvidia-smi\` 的 util）也不太可靠** —— 它只表示"有 kernel 在跑"的时间占比，decode 阶段 kernel 一直在跑但算力利用率不到 5%，所以它经常显示 90%+ 却毫无信息量。

**该用的指标（按优先级）**：

**① KV Cache 使用率** —— 最好的扩容信号。它直接代表"还能接多少并发"。接近上限（比如 >85%）说明再来请求就要排队或抢占了。
**② 队列等待时长 / 队列长度** —— 直接对应用户等待。比队列长度更好的是**等待时间的 P95**。
**③ TTFT 的 P95/P99** —— 最贴近用户体感的信号，但它是滞后指标（已经变差了才报警），适合做兜底而不是主信号。
**④ 运行中的请求数 / \`max_num_seqs\`** —— 并发饱和度。
**⑤ 抢占率（preemption rate）** —— 如果引擎频繁抢占请求，说明容量明显不足，这是个很干净的过载信号。

**组合策略**：主信号用 KV 使用率 + 队列等待时长，兜底用 TTFT P99。

**缩容要格外小心**：
- 不能直接杀实例，正在生成的请求会断。需要 **graceful drain**：先从 LB 摘除（不再接新请求）→ 等现有请求生成完（或超时）→ 再销毁
- 缩容阈值要比扩容阈值低很多（**滞回/hysteresis**），并加冷却期，否则会在阈值附近反复抖动
- 因为**冷启动慢**（分钟级），缩容要保守 —— 缩错了要几分钟才能补回来

**冷启动是扩缩的核心矛盾**：
扩容决策到实例可服务之间有几分钟延迟，所以要么**提前预测扩容**（按历史流量曲线预热）、要么维持 **warm pool**（预热好的备用实例，成本换响应速度）。
优化冷启动的手段：权重放节点本地 NVMe / 节点级共享缓存、分片并行下载、镜像预拉、跳过不必要的 warmup、CUDA Graph 捕获耗时优化。

**面试要点**：主动指出"GPU util 也不可靠"这一层，比只说"CPU 没用"更深。`,
ask:"系统设计轮常见开场。"},

{id:"s3",cat:"serve",level:2,tags:["K8s"],q:"在 Kubernetes 上跑 GPU 推理和训练，有哪些特殊问题？",
a:`**① GPU 不能超卖。**
CPU 可以 overcommit，GPU 不行 —— \`nvidia.com/gpu\` 是整数资源，一张卡只能分给一个容器（除非用 MIG/MPS 的 time-slicing）。所以 bin-packing 策略和 CPU 服务完全不同，碎片（一台 8 卡机上剩 3 张卡没人要）是真实的浪费。

**② Device Plugin 与驱动。**
需要 NVIDIA device plugin 暴露 GPU 资源、GPU Operator 管理驱动/容器运行时（nvidia-container-toolkit）。驱动版本和容器内 CUDA 版本的兼容性是常见坑（驱动要 ≥ CUDA runtime 要求）。

**③ 拓扑感知调度。**
同一个 TP 组的 pod **必须落在同一台机**（NVLink 域内）；跨机的 PP/DP 组最好在同一个 leaf switch 下（减少跨 spine 流量）。默认调度器不懂这些，需要：
- \`nodeSelector\` / affinity 约束
- Topology Manager + NUMA 对齐（GPU、NIC、CPU 要在同一 NUMA 节点，否则 PCIe 跨 socket 传输）
- 更完整的方案用 **Volcano / Kueue** 做拓扑感知

**④ Gang scheduling（最关键）。**
分布式训练是「全起或不起」的 —— 如果 8 个 pod 只调度起 5 个，这 5 个会占着 GPU 等另外 3 个，形成**资源死锁**。默认 K8s 调度器会这么干。必须用 **Volcano / Kueue / Coscheduling** 插件做 gang scheduling（PodGroup 全部满足才调度）。

**⑤ 网络。**
需要 RDMA/GPUDirect 时要挂 \`hostNetwork\` 或用 SR-IOV/RDMA device plugin，把 IB 设备透传进容器；NCCL 要正确识别网卡（\`NCCL_IB_HCA\`、\`NCCL_SOCKET_IFNAME\`）。这块配错的表现是吞吐掉十几倍而不报错。

**⑥ 存储。**
模型权重几十到几百 GB，不能每次从对象存储拉。方案：节点本地 NVMe 缓存 + DaemonSet 预热、或用高性能共享文件系统（Lustre/FSx/GPFS）、或 CSI 的懒加载镜像（如 SOCI/estargz）。

**⑦ 健康检查与故障域。**
探针要检查 GPU 健康（不只是进程活着）—— ECC 错误、XID、显存泄漏。一个 TP 实例跨多 pod 时，健康单位是整个 PodGroup。

**⑧ 优先级与抢占。**
生产推理 > 生产训练 > 研究任务。用 PriorityClass + 抢占，但抢占训练任务要配合 checkpoint（否则白跑）。

**面试价值**：Anthropic 明确会问 K8s/Terraform/云网络基础。gang scheduling 和拓扑感知是最能体现你懂 GPU 集群特殊性的两点。`,
ask:"Anthropic 等公司明确会问。gang scheduling 是甄别点。"},

{id:"s4",cat:"serve",level:2,tags:["可观测"],q:"线上 LLM 服务该监控哪些指标？怎么做全链路排查？",
a:`**性能（都要 P50/P95/P99，绝不看平均）**
- **TTFT**（首 token 延迟）、**TPOT**（每 token 延迟）、端到端延迟
- 吞吐：请求/秒、**输入 token/秒 与 输出 token/秒分开统计**（成本和负载特性都不同）
- 排队等待时长、batch size 分布

**资源**
- GPU：SM 利用率 与 显存带宽利用率（**这是两回事**，都要看）、显存占用
- **KV Cache 使用率**（最关键的容量信号）
- **抢占率 / 重算率**
- 温度与功耗（降频会导致神秘的性能下降）

**质量**
- 空回复率、截断率（撞 \`max_tokens\`）、拒答率
- 工具调用/JSON 解析成功率
- **prefix cache 命中率**（直接对应成本）
- 抽样送 LLM-Judge 的日常质量回归

**业务与成本**
- 每请求 token 成本、按租户/按模型的成本分摊
- 重试率、用户点踩率

**安全**
- 命中安全策略比例、提示注入检测告警

**全链路排查方法论**：
必须有 **trace_id 串起整条链路**：网关 → 鉴权 → 路由 → 队列 → 引擎调度 → 各 GPU 前向 → 流式返回。每段打时间戳，能回答"这 3 秒花在哪"。

**延迟拆解的标准做法**：
\`\`\`
端到端 = 网络 + 鉴权/限流 + 排队 + prefill + N×decode + 流式传输
\`\`\`
逐段测量，定位到段之后再深入。比如排队长 → 容量问题；prefill 长 → prompt 太长或 chunked prefill 没生效；decode 慢 → batch 太大或 KV 打满导致抢占。

**还要能回放**：把请求（脱敏后）落盘，线上出问题能用原始输入在测试环境复现。这是 oncall 的基本功。

**一个高价值但常缺的指标**：**per-rank / per-instance 的指标分布**。全局平均正常但某个实例异常，是最常见的"部分用户受影响"根因。`,
ask:"系统设计轮收尾必问。"},

{id:"s5",cat:"serve",level:3,tags:["发布"],q:"模型或引擎版本升级怎么做灰度和回滚？",
a:`**LLM 服务升级比普通服务危险，因为输出是生成式的，回归难以自动判定。**

**发布前（离线门禁）**
- 多维评测集跑分：目标任务 + 通用能力回归 + 安全性
- **性能门禁**：TTFT/TPOT/吞吐不得劣化超过阈值
- 输出分布对比：同一批 prompt 在新旧版本上的 top-1 一致率、KL 散度 —— 能快速发现"悄悄变了"
- 格式合法率（JSON、工具调用）

**灰度（线上）**
1. **影子流量（shadow）**：把真实请求复制一份给新版本，**不返回给用户**，只比对输出和性能。零风险，能发现离线测不出的问题（真实 prompt 分布、长尾输入）。
2. **1% 金丝雀**：看真实业务指标 —— 点踩率、重试率、转人工率、会话轮数。这些比任何离线指标都准。
3. **按维度放量**：先内部用户 → 低风险租户 → 全量。每一级观察足够长（至少覆盖一个流量周期，通常 24 小时）。
4. **自动回滚条件**：预设阈值（错误率、P99、点踩率），超过自动回滚，不依赖人判断。

**回滚要能一键且快**
- 旧版本实例**保留而不销毁**（至少在灰度期间），回滚就是切流量权重，秒级
- 如果旧版本已缩容，回滚要等冷启动几分钟 —— 这是很多事故的放大器。所以**灰度期间不要缩容旧版本**
- 配置和权重要版本化，能精确回到某个组合（模型版本 + 引擎版本 + 配置 + prompt 模板）

**容易踩的坑**
- **prompt 模板和模型版本耦合**：只回滚模型没回滚模板，行为更奇怪。要一起版本化、一起回滚
- **prefix cache 污染**：新旧版本共用缓存会串味，升级时要清缓存或按版本分命名空间
- **KV Cache 格式变化**：引擎升级可能改 KV 布局，PD 分离场景下新旧实例间传 KV 会崩 —— 升级要么全量同步，要么按版本亲和路由

**面试要点**：主动提「影子流量」和「灰度期间不缩容旧版本」这两条，都是吃过亏才知道的细节。`,
ask:"平台岗深度题。"},

/* ══════════ 成本与容量 ══════════ */
{id:"c1",cat:"cost",level:2,tags:["容量"],q:"给定 QPS 和延迟 SLO，怎么推算需要多少 GPU？",
a:`**自顶向下推导，每一步都要落到数字。**

**① 明确输入**
- 峰值 QPS、峰谷比
- 输入/输出 token 长度分布（**要分位数，不是平均值** —— P95 的长请求决定容量）
- SLO：TTFT P95 ≤ ?、TPOT P95 ≤ ?
- 模型大小与精度

**② 算单实例的并发上限（KV 约束）**
\`\`\`
可用KV显存 = 卡显存 × 利用率 − 权重 − 激活/开销
单请求KV = 2 × L × H_kv × D × 平均总长 × bytes
并发上限 ≈ 可用KV显存 / 单请求KV
\`\`\`
例：70B FP8（权重 70GB）、TP=4（每卡 17.5GB 权重）、80GB 卡 × 0.9 = 72GB 可用 → 每卡剩 54.5GB，4 卡共 218GB KV。单请求（4k 总长、GQA 8 组、FP16 KV）约 1.3GB → **并发约 160**。

**③ 算单实例吞吐**
实测（不要估）在目标 SLO 下的稳定输出 token/秒。假设实测在并发 64 时 TPOT P95 刚好达标，输出 **1200 token/s**。
> 注意：并发上限不等于最优并发。**最优并发是 SLO 约束下的那个点**，通常远低于 KV 上限。这是关键 —— 很多人只算显存不算延迟。

**④ 算需求**
\`\`\`
需要的输出token/s = QPS × 平均输出长度
实例数 = 需要的token/s ÷ 单实例吞吐
\`\`\`
例：QPS=20，平均输出 500 token → 需要 10000 token/s → \`10000/1200 ≈ 9\` 个实例 → 9 × 4 = **36 张卡**

**⑤ 加余量**
- 冗余：+1 个实例（N+1）
- 峰值缓冲：按峰谷比和扩缩响应时间留 20-30%
- 故障域分散
→ 实际 **约 48 张卡**

**⑥ 验证**
用真实流量分布做压测（不是固定长度的合成负载 —— 长度分布对 KV 和调度影响极大），画并发-吞吐-延迟三条曲线，找到 SLO 拐点，反推容量。

**面试要点**：主动强调「并发上限 ≠ 最优并发」和「要用真实长度分布压测」，这两条是纸上算和真做过的分界线。`,
ask:"系统设计轮必算。不算数字会被判定没经验。"},

{id:"c2",cat:"cost",level:2,tags:["降本"],q:"一个 LLM 服务每月烧 50 万，怎么降到 20 万？按优先级说。",
a:`**先问清楚再动手**：成本构成（自建 GPU 还是 API 计费）？流量特征（重复率、system prompt 长度、输入输出比）？质量红线在哪？有多少工程时间？

**然后按性价比排序 —— 这个顺序本身就是答案：**

**① Prefix / Prompt Caching（先做，零质量损失）**
固定 system prompt、长文档、多轮对话历史命中缓存。输入侧成本可降 **50-90%**。如果你的 system prompt 有几千 token 而对话很多轮，这一项可能单独就砍掉三成成本。

**② 精简 prompt + 限制输出长度**
- few-shot 示例从 8 个减到 3 个往往效果一样
- 输出 token 通常比输入贵 **3-5 倍**，让模型简洁作答收益明显
- 设合理的 \`max_tokens\`，避免跑飞

**③ 语义缓存**
相似问题直接返回。能挡掉重复流量，但要注意缓存命中的质量风险（相似不等于相同），设高相似度阈值。

**④ 模型路由**
小模型接住简单请求，难的才升级。需要一个分类器或置信度判断 + 兜底机制。典型能把 60-80% 的流量导到便宜模型。

**⑤ 量化 + 提升批处理并发（自建场景）**
FP8/AWQ + 调 \`max_num_seqs\` 和 \`max_num_batched_tokens\`，单卡吞吐提升 2-3 倍 = 单位 token 成本降 50-65%。

**⑥ 部署形态优化**
PD 分离提升 GPU 利用率；把批处理类流量单独调度用大 batch；用 spot/预留实例组合降低单卡成本。

**⑦ 蒸馏小模型（长期最优，投入最大）**
用大模型造数据微调 7B。垂直任务上常能追平，成本降一个数量级。但要几周工程投入 + 持续维护。

**每一步都必须说三件事**：预期收益量级、质量风险、**怎么验证**（A/B + 质量回归门禁）。

**要避免的答法**：
- 罗列一堆手段不排序
- 一上来说"换更小的模型" —— 那是牺牲质量最直接的做法，应该排在后面
- 不提验证 —— 降本不量化质量损失就是耍流氓

**加分收尾**：「我会先做成本归因，按 租户 × 模型 × 场景 拆出 top 消耗，往往 20% 的场景占 80% 的成本，优化就从那里开始。」`,
ask:"开放题高频。按优先级答是关键。"},

{id:"c3",cat:"cost",level:2,tags:["成本"],q:"怎么核算单 token 成本？自建和用 API 怎么比？",
a:`**自建的单 token 成本**：
\`\`\`
每小时成本 = GPU数 × 单卡小时价 ÷ 利用率
每小时产出 = 实测 token/s × 3600
单token成本 = 每小时成本 ÷ 每小时产出
\`\`\`

**关键在分母的"实测"和分子的"利用率"** —— 这是最容易算错的地方：
- 很多人用峰值吞吐算，但真实利用率可能只有 30-50%（峰谷波动、扩缩滞后、冗余实例空转）
- **有效成本 = 理论成本 / 实际利用率**。利用率 40% 意味着真实单价是理论的 2.5 倍

**要算进去的隐性成本**：
- 冗余实例（N+1）和 warm pool 的空转
- 存储（模型权重多版本、KV 缓存的二级存储）
- 网络出口流量
- **工程人力**（自建的运维、升级、oncall 成本，这项最容易被忽略且往往最贵）
- 峰值容量必须按峰值配，但按平均计费 → 峰谷比越大越亏

**和 API 比的正确方法**：
1. 用**同一批真实流量**分别跑，比较总账单，不要用合成负载
2. API 按 token 计费、天然弹性、零运维；自建的优势在高且稳定的负载 + 数据合规
3. **盈亏平衡点**大致在"能把自建利用率做到 60% 以上"的规模。低于这个，API 通常更便宜
4. 混合策略常见：基线负载自建（利用率高），突发溢出到 API（弹性）

**决策不只看钱**：
- 数据合规/驻留要求 → 必须自建
- 需要定制模型、特殊解码、自研 kernel → 自建
- 负载波动极大、规模小 → API
- 需要最强模型能力 → API（自建追不上前沿模型）

**面试要点**：主动把**利用率**和**工程人力**摆到台面上。只算 GPU 租金除以峰值吞吐的人，一定没真做过成本核算。`,
ask:"「自建还是买 API」的标准题。"},

/* ══════════ 性能调试 ══════════ */
{id:"d1",cat:"debug",level:2,tags:["排查"],q:"线上 TTFT P99 突然涨了 3 倍，你怎么查？",
a:`**先分层缩小范围，不要猜原因。**

**① 界定影响面（2 分钟内要有答案）**
- 所有请求还是某一类？某个模型 / 租户 / 区域 / 实例？
- 什么时间点开始？是阶跃还是渐变？
- **有没有伴随变更**？发版、配置改动、模型切换、流量结构变化 —— 变更是第一嫌疑，先看变更日志

**② 顺着延迟链拆解**
\`\`\`
TTFT = 网络 + 鉴权限流 + 排队 + prefill + 首token返回
\`\`\`
逐段看指标，定位到哪一段：

- **排队时长涨了** → 容量不足或扩缩失效。看 KV 使用率、运行中请求数、抢占率。是流量涨了还是实例少了？
- **排队正常但 prefill 慢** → 看 **prompt 长度分布是否变了**。最常见的真因：某个租户开始发超长 prompt，而 chunked prefill 没配或预算太大。
- **GPU 利用率低但延迟高** → CPU 侧瓶颈（tokenize、调度、序列化）、锁竞争、或 CUDA Graph 失效退回 eager
- **KV Cache 打满** → 请求被抢占后重算，延迟翻倍
- **单节点异常** → 看 **per-instance 指标分布**。一个实例降频/坏卡会拖高整体 P99（P99 本来就反映最差的那部分）

**③ 常见真因清单**（按概率）
1. 流量结构变化（长 prompt 变多）
2. prefix cache 命中率骤降（缓存被清、路由亲和性失效、租户变化）
3. 某节点变慢（ECC、降频、网络劣化）
4. 扩缩容失效或冷启动被卡住（镜像拉取慢、权重下载慢）
5. 上游依赖变慢（鉴权、日志、向量库）
6. 模型热加载抢占资源

**④ 动作顺序：先止血，再定位，最后补防线**
- 止血：限流压住异常租户、隔离坏节点、手动扩容、回滚变更
- 定位：拿 trace 采样、复现
- 补防线：加缺失的监控、加自动化防护（比如超长 prompt 自动降级）、写 postmortem

**面试要点**：面试官要看的是**方法论**，不是你猜得对不对。**直接说"可能是缓存没命中"会扣分，说"我会先分层缩小范围，第一步看影响面和变更"会加分。**`,
ask:"故障排查题越来越常见，因为最能区分真实经验。你在 Bedrock oncall 的经历是现成的弹药。"},

{id:"d2",cat:"debug",level:2,tags:["压测"],q:"怎么设计一个靠谱的 LLM 推理压测？常见的错误有哪些？",
a:`**负载模型必须贴近真实，这是最重要的一条。**

**① 输入要用真实分布，不是固定长度**
- 从线上采样 prompt 长度和输出长度的**联合分布**（两者相关：长 prompt 常伴长输出）
- 固定 512-in/128-out 的合成负载会给出**严重失真**的结论 —— 因为 KV 占用、调度行为、chunked prefill 的表现全都依赖长度分布
- 要包含长尾（P99 的超长请求），它们对 P99 延迟影响巨大

**② 到达模式要真实**
- 恒定速率（closed-loop 固定并发）和泊松到达（open-loop 固定 QPS）测出来的结果不同
- **要用 open-loop 按目标 QPS 打**，才能测出排队行为。closed-loop 固定并发数会掩盖过载
- 要测突发（burst）场景

**③ 必须 warmup 并跳过前段**
- CUDA Graph 捕获、cuBLAS autotune、JIT 编译、缓存预热都在前几十秒
- 丢掉前 30-60 秒的数据，否则 P99 全是启动噪声

**④ 报告完整的曲线，不是单点**
- 横轴并发或 QPS，纵轴同时画 **吞吐、TTFT P50/P95/P99、TPOT P50/P95/P99**
- 目标是找到 **SLO 约束下的最大吞吐点**，这才是容量规划的依据
- 单独报一个"吞吐 3000 token/s"没有意义，必须附带当时的延迟和并发

**常见错误清单**：
1. **用平均值而不是分位数** —— LLM 延迟分布长尾极重，平均值毫无意义
2. 固定长度合成负载
3. 不 warmup
4. closed-loop 测过载
5. 客户端成为瓶颈（Python 单进程压不上去，要多进程/多机）
6. **只测吞吐不测延迟**，或反之
7. 忽略 token 计数方式（输入输出要分开，且用模型自己的 tokenizer 算）
8. 测的时候后台有其他负载干扰
9. 只跑一次不看方差 —— 要多次取中位数

**工具**：vLLM 自带 \`benchmark_serving.py\`（支持真实数据集如 ShareGPT）、locust/k6 做自定义场景、genai-perf（NVIDIA）。

**面试要点**：能说出「固定长度合成负载会失真」和「open-loop vs closed-loop」，说明你真做过而不只是跑过一遍脚本。`,
ask:"「你怎么验证优化有效」的必答部分。"},

{id:"d3",cat:"debug",level:3,tags:["排查"],q:"GPU 利用率显示 95% 但吞吐很低，可能是什么原因？",
a:`**关键认知：\`nvidia-smi\` 的 utilization 是个陷阱。** 它只表示"过去采样周期内有至少一个 kernel 在执行"的时间占比，**不代表算力被用满**。一个只用了 2% SM 的小 kernel 持续跑，也会显示 100%。

**可能原因（按排查顺序）**：

**① 本来就是 memory-bound（最常见，且不是问题）**
decode 阶段读权重就是瓶颈，SM 在等数据。看 Nsight 的 \`sm__throughput\` vs \`dram__throughput\` —— 如果 DRAM 接近峰值而 SM 很低，这就是正常的 decode 行为。解法不是"提高利用率"而是增大 batch 摊薄权重读取。

**② 大量小 kernel + launch 开销**
decode 一步几百个小 kernel，每个几十微秒。GPU 一直"忙"但都在做琐碎工作，且 CPU launch 成为瓶颈。
- 诊断：Nsight Systems 看 kernel 之间有无空隙、CPU 侧 launch 是否密集
- 解法：CUDA Graph + 算子融合

**③ occupancy 太低，延迟掩盖不足**
kernel 用了太多寄存器或 SMEM，导致每个 SM 上驻留的 warp 太少，访存延迟无法被其他 warp 掩盖。
- 诊断：Nsight Compute 的 achieved occupancy 和 stall reasons
- 解法：减少寄存器用量、调整 block size

**④ Tensor Core 没用上**
dtype 不对（FP32 而非 BF16）、形状不满足对齐要求（维度不是 8/16 的倍数）、或走了不支持 TC 的 kernel 路径。
- 诊断：\`sm__pipe_tensor_op_hmma_cycles_active\` 为 0
- 解法：改 dtype、pad 维度到对齐

**⑤ 同步点太多**
频繁的 \`.item()\`、\`.cpu()\`、\`torch.cuda.synchronize()\` 或 print 张量，每次都强制 CPU 等 GPU，把流水线打断。
- 诊断：时间线上规律性的空隙
- 解法：把标量取值批量化、延后到必要时

**⑥ 通信占了大头**
多卡时 NCCL kernel 也算"GPU 忙"。如果通信没和计算重叠，利用率高但有效算力低。
- 诊断：Nsight 看 NCCL kernel 占比
- 解法：见通信重叠那题

**⑦ 数据供给不足**
GPU 跑完一批在等下一批数据（dataloader 慢）。此时 util 可能是间歇的 95%。

**回答要点**：先纠正指标本身的误导性，再给分层排查。**「我不信 nvidia-smi 的 util，我看 SM throughput 和 DRAM throughput」** 这句话本身就是很强的信号。`,
ask:"高区分度题。能指出 util 指标不可靠是关键。"},

{id:"d4",cat:"debug",level:2,tags:["工具"],q:"Nsight Systems 和 Nsight Compute 分别什么时候用？PyTorch Profiler 呢？",
a:`**三个工具的分工，用错了会浪费大量时间。**

**Nsight Systems（nsys）—— 看时间线，找"哪里慢"**
- 作用域：整个进程/系统，CPU 线程 + CUDA stream + NCCL + 内存拷贝的统一时间线
- 能回答：GPU 有空隙吗？CPU 在喂得过来吗？通信和计算重叠了吗？哪个 kernel 占总时间最多？有同步点吗？
- **这是第一个该用的工具**。先用它定位到"哪一段/哪个 kernel 值得深挖"
- 用法：\`nsys profile -t cuda,nvtx,osrt,cudnn,cublas -o out python train.py\`
- 配合 **NVTX 标注**（\`torch.cuda.nvtx.range_push/pop\`）给自己的代码段打标签，时间线立刻可读

**Nsight Compute（ncu）—— 钻单个 kernel，找"为什么慢"**
- 作用域：单个 kernel 的硬件计数器（throughput、occupancy、访存模式、stall reasons）
- 能回答：这个 kernel 是 compute/memory/latency bound？访存合并吗？用上 Tensor Core 了吗？
- 开销大（会重放 kernel），**只在已经知道要看哪个 kernel 时用**
- 用法：\`ncu --set full -k kernel_name -c 10 python x.py\`

**PyTorch Profiler —— 框架视角，最方便的起点**
- 作用域：按 PyTorch 算子聚合的耗时、显存分配、以及 Chrome trace 导出
- 能回答：哪个 **算子**（不是 kernel）最贵？显存峰值在哪个算子？CPU 和 GPU 时间各多少？
- 优势：改几行代码就能用，和 Python 代码直接对应，**memory timeline 对查 OOM 特别好用**
- 劣势：看不到硬件级细节、对多卡通信的可见性不如 nsys

**推荐工作流**：
1. **PyTorch Profiler** 快速看算子级热点和显存曲线（几分钟，零门槛）
2. 怀疑是调度/通信/CPU 问题 → **nsys** 看时间线
3. 锁定某个 kernel 效率低 → **ncu** 钻硬件指标
4. 推理服务的端到端问题 → 先看服务侧 trace 和指标，再决定要不要下到这三个工具

**别忘了**：\`torch.compile\` 之后算子被融合重写，profiler 里看到的名字会变成 Triton kernel。这时要看生成的代码（\`TORCH_COMPILE_DEBUG=1\` 会 dump 出来）。

**面试要点**：说清「先 nsys 定位、再 ncu 深挖」这个顺序，比列举一堆 flag 更能显示你真用过。`,
ask:"NVIDIA 必问。其他家问「你怎么 profile」时能说出分工就够。"}
]);
