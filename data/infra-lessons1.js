/* Infra 主攻线 · 课程（一）：GPU 性能模型 / CUDA 与 kernel / 推理引擎内部
   结构和阶段 0 的课程完全一致：sections 讲课 + quiz 自测（不直接给答案）
   ⚠ 约定：字符串里的中文引号一律用「」，不要用 ASCII 双引号；反引号写成 \`
*/
window.LESSONS = window.LESSONS || {};

window.LESSONS.inf1 = {
track: "infra", crumb: "Infra 主攻线 · 第 1 课",
title: "GPU 性能模型：为什么大模型推理这么慢",
sub: "这一课回答一个问题：一张算力几百 TFLOPS 的卡，为什么生成一个字要几十毫秒？答案不在算力，在搬数据。",
est: "读 80 分钟 + 做题 40 分钟",
sections: [
{h:"1. 先看一个反常识的现象",
b:`H100 的算力是 **989 TFLOPS**（BF16），意思是每秒能做将近一千万亿次浮点运算。

现在让它跑一个 7B 模型，一次生成一个 token。这一步需要多少次运算？

每个参数参与一次乘法和一次加法，所以约 \`2 × 7e9 = 1.4e10\` 次浮点运算，也就是 **14 GFLOP**。

按 989 TFLOPS 算，理论耗时：\`14e9 / 989e12\` ≈ **0.014 毫秒**。

但实测单请求的 decode 大约是 **10-30 毫秒**一个 token —— **慢了一千倍。**

算力没被用上。那时间花在哪了？

**花在把权重从显存搬到计算单元上。**

7B 模型用 FP16 存，权重是 14 GB。生成**每一个** token，都要把这 14 GB 完整读一遍。H100 的显存带宽是 3.35 TB/s，读 14 GB 需要：

$$14 / 3350 × 1000 ≈ 4.2 毫秒$$

这个数字和实测就是一个量级了。

> **结论先摆在这里**：LLM 推理的瓶颈不是「算得慢」，是「读得慢」。理解这一点，后面所有的优化手段（增大 batch、GQA、量化、投机解码、PagedAttention）都会变成同一个逻辑的不同实现。`},

{h:"2. 一张卡只有两个数字重要",
b:`描述一张 GPU 的性能，**只需要两个数**：

**峰值算力**（FLOPS）—— 每秒能做多少次浮点运算。
**峰值带宽**（Byte/s）—— 每秒能从显存搬多少字节。

以 H100 SXM 为例：
- 算力：BF16 约 **989 TFLOPS**（用上 Tensor Core）
- 带宽：HBM3 约 **3.35 TB/s**

注意这两个数的**比例**：

$$989e12 ÷ 3.35e12 ≈ 295$$

意思是：**这张卡每从显存搬 1 个字节，就有能力做 295 次浮点运算。**

如果你的计算只搬 1 字节却只做 1 次运算，那 294 次的算力就闲着 —— 卡在等数据。

这个比值有个名字，叫 **ridge point（拐点）**，下一节展开。

**顺带记住存储的层级**，这解释了为什么「搬数据」这么贵：

| 层级 | 带宽量级 | 容量量级 |
|---|---|---|
| 寄存器 | ~20 TB/s | KB |
| Shared Memory / L1 | ~10 TB/s | 228 KB / SM |
| L2 Cache | 几 TB/s | 50 MB |
| **HBM（显存）** | **3.35 TB/s** | 80 GB |
| PCIe 5.0 x16 | 64 GB/s | 主机内存 |

**每往下一层慢一个量级。** 所以几乎所有 kernel 优化的本质都是同一句话：**减少对 HBM 的访问次数**。

这也解释了为什么「把 KV Cache 放到 CPU 内存」这种想法在 decode 热路径上行不通 —— PCIe 比 HBM 慢 50 倍。`},

{h:"3. 算术强度：衡量一个计算划不划算",
b:`**算术强度（Arithmetic Intensity）= 浮点运算次数 ÷ 必须搬动的字节数**，单位 FLOP/Byte。

它回答的问题是：**我搬来的每一个字节，被用来算了几次？**

算几个例子，你会立刻有感觉。

**例 1：逐元素加法 \`c = a + b\`**
- 运算：每个元素 1 次加法
- 访存：读 a 读 b 写 c，FP32 下 12 字节
- 算术强度 = \`1 / 12\` ≈ **0.083**

搬了 12 个字节只算了 1 次。极度浪费。**任何逐元素操作都是这个量级。**

**例 2：矩阵乘法 \`C = A × B\`，都是 n×n**
- 运算：\`2n³\`（每个输出元素做 n 次乘加）
- 访存：读 A、读 B、写 C，共 \`3n²\` 个元素，FP32 下 \`12n²\` 字节
- 算术强度 = \`2n³ / 12n² = n/6\`

n=4096 时约 **683**。搬一个字节能算 683 次 —— 非常划算。

> **这里藏着一个关键洞察**：矩阵乘法的算术强度**随 n 增长**。矩阵越大越划算。反过来说，**矩阵越瘦越亏**。

**例 3：LLM decode，batch=1，FP16 权重**
- 每个权重参与 1 次乘加 = 2 FLOP
- 每个权重占 2 字节
- 算术强度 = \`2 / 2\` = **1**

搬 1 个字节只算 1 次。和例 1 一个惨状。

**例 4：LLM decode，batch=32**
- 权重只读一遍，却服务了 32 个请求
- 算术强度 = \`2 × 32 / 2\` = **32**

一下涨了 32 倍。**这就是为什么增大 batch 能大幅提升吞吐 —— 不是算得更快了，是同一份数据被复用了更多次。**`},

{h:"4. Roofline：把两件事画在一张图上",
b:`现在把算力和带宽放在一起。

对一个算术强度为 \`I\` 的算子，它能达到的性能上限是两者的较小值：

$$可达性能 = min(峰值算力, 带宽 × I)$$

画成图就是一条屋顶：左边是一条斜线（受带宽限制，强度越高性能越高），右边是一条水平线（受算力限制，再高也上不去）。两条线的交点就是 **ridge point**：

$$ridge = 峰值算力 ÷ 峰值带宽$$

H100 约 **295 FLOP/Byte**。

**判据非常简单**：
- 算子的算术强度 **< ridge** → **memory-bound**，优化访存
- **> ridge** → **compute-bound**，优化算力

把前面四个例子对照一下（H100，ridge=295）：

| 算子 | 算术强度 | 判定 |
|---|---|---|
| 逐元素加法 | 0.083 | memory-bound（差 3500 倍）|
| decode batch=1 | 1 | memory-bound（差 295 倍）|
| decode batch=32 | 32 | memory-bound（还差 9 倍）|
| decode batch=512 | 512 | compute-bound |
| GEMM n=4096 | 683 | compute-bound |

> **一个必须记住的陷阱**：ridge point 是**硬件属性**，不同卡差别巨大。
> 一张没有 Tensor Core 的消费级卡（比如 GTX 1650），算力只有 1.84 TFLOPS、带宽 112 GB/s，ridge point 只有 **16**。
> 同一个 batch=32 的 decode，在 H100 上是 memory-bound，在这张卡上已经 **compute-bound** 了。
>
> 所以「LLM 推理是访存瓶颈」这句话**必须连着硬件一起说**。面试里只会背结论、被追问「那什么时候不是」就卡住的人很多，这是个很好的区分点。`},

{h:"5. 把 LLM 推理拆成两半看",
b:`理解了算术强度，就能解释为什么 LLM 推理要分成两个阶段看。

**Prefill（处理输入的 prompt）**
一次性处理整个 prompt，假设 2000 个 token。这时矩阵乘法的形状是 \`(2000, hidden) × (hidden, out)\` —— **又高又厚**，算术强度高。

- 权重只读一遍，却服务了 2000 个 token 的计算
- 算术强度约 2000 量级 → **compute-bound**
- GPU 算力被充分利用
- 它决定 **TTFT（首个 token 的延迟）**

**Decode（逐个生成）**
每一步只处理 1 个新 token。矩阵形状变成 \`(1, hidden) × (hidden, out)\` —— **极度瘦长**，退化成矩阵向量乘（GEMV）。

- 权重读一遍只服务 1 个 token
- 算术强度约 1 → **彻底 memory-bound**
- 算力利用率常常**低于 5%**
- 它决定 **TPOT（每个后续 token 的延迟）**

**这个差异带来两个重要后果：**

**后果一：增大 batch 对 decode 几乎是白赚。**
batch 从 1 到 32，权重还是只读一遍，但服务了 32 个请求。吞吐涨 32 倍，而单步耗时几乎不变（因为瓶颈是读权重，不是算）。这就是 **continuous batching** 价值的来源。

**后果二：两个阶段混在一起调度会互相伤害。**
一个 2000 token 的 prefill 要算很久，如果它和正在 decode 的请求排在同一个批次里，那些 decode 请求就得干等 —— 用户体感是「打字突然卡住」。
解法有两个：把 prefill 切成小块和 decode 混着跑（**chunked prefill**），或者让 prefill 和 decode 跑在不同的 GPU 池（**PD 分离**）。

> 你现在应该能自己推出来：**为什么投机解码有效？**
> 因为 decode 阶段算力大量空闲，让大模型一次验证 5 个 token 和验证 1 个 token 的耗时几乎一样（都要完整读一遍权重）。多验证的那 4 个是白赚的。`},

{h:"6. 显存账：必须能当场算的三个数",
b:`面试里极高频：「70B 模型推理要几张卡」「为什么全参微调 7B 要 8 卡」。你要能在一分钟内口算。

**① 推理的权重显存**
$$权重 = 参数量 × 每参数字节数$$
- FP16/BF16：\`2N\`。7B → 14 GB，70B → 140 GB
- FP8：\`N\`。70B → 70 GB
- INT4：\`0.5N\`。70B → 35 GB

**② KV Cache（最容易被忽略，但常常是真正的瓶颈）**
$$KV = 2 × 层数 × KV头数 × 头维度 × 序列长 × batch × 字节数$$

开头的 2 是 K 和 V 各存一份。注意是 **KV 头数**，不是 Q 头数 —— 用了 GQA 的模型这两个不一样。

算 LLaMA-2 7B（32 层、32 头、每头 128 维、FP16）：
\`2 × 32 × 32 × 128 × 2\` = **512 KB / token**

一个 4096 长度的请求就要 2 GB。一张 80GB 的卡装完 14GB 权重，剩下的只够放 **33 个并发**。

如果用 GQA 把 KV 头数降到 8，变成 **128 KB/token**，并发直接翻 4 倍。

**③ 训练的显存**
用 AdamW 混合精度，每个参数需要：
- 参数副本 FP32：4 字节
- 梯度：4 字节
- 动量 m：4 字节
- 二阶矩 v：4 字节

$$训练状态 = 16N 字节$$

7B → **112 GB**，单卡放不下。这就是 ZeRO、FSDP、LoRA 存在的全部理由。

再加上激活值（长序列时可能超过权重本身），所以 70B 全参训练至少要十几到几十张卡。

> **口算模板，背下来**：
> 推理 ≈ \`2N + KV\`
> 训练 ≈ \`16N + 激活\``},

{h:"7. 动手：测出你自己那张卡",
b:`理论讲完了，但**没有自己测过的数字，这些都还只是别人的结论**。

仓库里有一个现成的脚本：

\`\`\`bash
python tools/gpu_bench.py
\`\`\`

它做三件事：
1. 用逐元素加法测**实际带宽**（纯 memory-bound 负载）
2. 用大矩阵乘测**实际算力**（纯 compute-bound 负载）
3. 两者相除给出**你这张卡的 ridge point**，并对照几个典型算子

**实测值通常只有标称的 70-90%**，原因是 DRAM 刷新、行激活开销、tile 边界效应、以及温度墙降频。85% 左右算正常。

**一个我在 GTX 1650 上跑出来的有趣结果**：

\`\`\`
实测带宽   112.5 GB/s
FP32       1.84 TFLOPS
FP16       0.43 TFLOPS   ← 比 FP32 还慢
Ridge      16.3 FLOP/Byte
\`\`\`

FP16 比 FP32 慢了四倍多。为什么？因为 **GTX 16 系列砍掉了 Tensor Core**，半精度只能走普通 CUDA core，还多了格式转换的开销。

这个结果很有教学意义：它说明**「低精度一定更快」是错的，它依赖硬件支持**。在有 Tensor Core 的卡上 FP16 通常快 8-16 倍，在没有的卡上反而更慢。

**动手清单**：
1. 本地跑一遍脚本，记下你的三个数
2. 用这三个数手算：你的卡上 batch 多大时 decode 会从 memory-bound 转成 compute-bound
3. 租一张 A100 或 H100 再跑一遍，把两组数并排 —— 这张对照表本身就能写进简历`}
],
quiz: [
{id:"if1q1", type:"num", q:"某张卡的峰值算力是 312 TFLOPS，显存带宽是 2039 GB/s。它的 ridge point 是多少 FLOP/Byte？（保留整数）",
hint:"ridge point = 峰值算力 ÷ 峰值带宽。注意单位：TFLOPS 是 1e12，GB/s 是 1e9，换算时差 1000 倍。", ans:153, tol:3,
why:"\`312e12 / 2039e9\` ≈ **153 FLOP/Byte**。\n\n这是 A100 80GB 的参数。对比 H100 的 295 —— A100 的 ridge point 更低，意味着它相对「更不容易被算力卡住」，同一个算子在 A100 上更容易落在 compute-bound 一侧。\n\n**单位换算是这题唯一的坑**：TFLOPS 的 T 是 1e12，GB/s 的 G 是 1e9，所以分子分母差三个数量级，别算成 0.153。"},

{id:"if1q2", type:"num", q:"计算逐元素乘法 c = a * b 在 FP16 下的算术强度（FLOP/Byte，保留两位小数）。",
hint:"每个元素做 1 次乘法。访存是读 a、读 b、写 c 三次，每次 FP16 占 2 字节。", ans:0.17, tol:0.02,
why:"运算：1 次乘法。\n访存：\`3 × 2 = 6\` 字节。\n算术强度 = \`1/6\` ≈ **0.17 FLOP/Byte**。\n\n对比 FP32 的 \`1/12 ≈ 0.083\` —— 用低精度**反而让算术强度翻倍了**，因为搬的字节少了一半而计算量不变。\n\n这解释了一个容易混淆的点：**量化对 memory-bound 算子的收益，来自搬得更少，而不是算得更快。**"},

{id:"if1q3", type:"mc", q:"一个算子的算术强度是 50，运行在 ridge point 为 295 的 H100 上。下面哪个优化最可能有效？",
choices:["用更大的 batch 提高数据复用","换用算力更强的卡","增加 SM 的占用率（occupancy）","把 FP32 换成 FP64 提高精度"], ans:0,
hint:"50 < 295，说明它落在拐点哪一侧？那一侧的瓶颈是什么？针对那个瓶颈该做什么？",
why:"50 < 295 → **memory-bound**，瓶颈是搬数据，不是算。\n\n**正确答案是提高数据复用**（增大 batch）—— 让搬来的每个字节被算更多次，把算术强度推高。\n\n其余为什么错：\n- **换算力更强的卡**：算力本来就没用满，换了也是闲着。这是最常见的错误直觉。\n- **提高 occupancy**：occupancy 帮助掩盖延迟，但当带宽已经打满时，再多 warp 也只是一起排队等数据。\n- **FP64**：字节数翻倍，算术强度**减半**，只会更糟。\n\n记住这个映射：**memory-bound → 减少搬运或增加复用；compute-bound → 提高算力利用（Tensor Core、更好的 tiling）。**"},

{id:"if1q4", type:"num", q:"一个模型有 60 层、8 个 KV 头、每头 128 维，KV Cache 用 FP16 存。每个 token 的 KV Cache 占多少 KB？",
hint:"公式：2 × 层数 × KV头数 × 头维度 × 字节数。算出来是字节，再除以 1024 换成 KB。", ans:240, tol:2,
why:"\`2 × 60 × 8 × 128 × 2\` = 245760 字节 = **240 KB / token**。\n\n注意开头那个 2 是 **K 和 V 各存一份**，不要漏。\n\n**接着往下想一步**：如果要支持 32 个并发、每个 8000 token 的上下文，KV Cache 需要\n\`240KB × 8000 × 32\` ≈ **61 GB**。\n\n这已经超过一张 80GB 卡在装完权重后的剩余空间了。**这就是为什么长上下文 + 高并发必须做 KV 量化或多卡切分。**"},

{id:"if1q5", type:"text", q:"某模型 decode 阶段 batch=1 时算术强度为 1。要让它在 ridge point 为 295 的卡上变成 compute-bound，batch 至少要多大？（填数字）",
ans:["295","296","约295","大于295",">295"],
hint:"decode 的算术强度约等于 batch（FP16 权重下，每 2 字节做 2×batch 次运算）。要超过 ridge point，batch 需要达到多少？",
why:"decode 的算术强度约等于 batch，所以需要 **batch ≥ 295**。\n\n**但这里有个现实问题，是这道题真正想让你想的**：你几乎永远到不了 batch=295。\n\n因为在此之前 **KV Cache 就把显存吃光了**。用上一题的数字，batch=295 时 KV Cache 需要几百 GB —— 根本放不下。\n\n**所以真实的优化路径不是「把 batch 拉到 295」，而是「先想办法压缩 KV Cache，让 batch 能尽可能大」**：GQA/MLA 减少 KV 头、KV 量化降精度、PagedAttention 消除碎片、prefix caching 复用共享部分。\n\n这个推理链条（想提吞吐 → 要大 batch → 受限于 KV → 所以优化 KV）是 LLM 推理优化的主线逻辑，能自己走通就说明这一课学到位了。"},

{id:"if1q6", type:"mc", q:"关于 prefill 和 decode，下面哪个说法是错的？",
choices:[
"prefill 是 compute-bound，decode 是 memory-bound",
"prefill 决定 TTFT，decode 决定 TPOT",
"把一个长 prefill 和多个 decode 请求放进同一批次，能同时改善两者的延迟",
"decode 阶段的矩阵乘法退化成了矩阵向量乘，Tensor Core 利用率很低"], ans:2,
hint:"想想一个 2000 token 的 prefill 要算多久。如果它和 decode 请求在同一个批次里，那些 decode 请求在这段时间里能做什么？",
why:"**第三个说法是错的，而且正好说反了。**\n\n长 prefill 和 decode 混在同一批次会**互相伤害**：这一次 iteration 被 prefill 拖到几百毫秒，同批所有 decode 请求都得干等，造成 **TPOT 尖刺**（用户感觉打字突然卡住）。\n\n正确的做法是把它们**分开或切碎**：\n- **Chunked Prefill**：把长 prefill 切成固定 token 预算的小块，和 decode 混着跑，平滑延迟\n- **PD 分离**：prefill 和 decode 跑在不同的 GPU 池，各自独立扩缩\n\n其余三个说法都对。第四条是对第一条的解释 —— decode 时矩阵形状是 \`(1, hidden)\`，M 维只有 1，Tensor Core 要求最小 16×16 的形状，所以大部分计算单元空转。"},

{id:"if1q7", type:"num", q:"一个 13B 模型用 BF16 做全参数训练（AdamW 混合精度），光是参数、梯度、优化器状态需要多少 GB？（不含激活）",
hint:"每个参数需要：FP32 参数副本 4 字节 + 梯度 4 字节 + 动量 m 4 字节 + 二阶矩 v 4 字节。", ans:208, tol:5,
why:"\`16 × 13e9\` = 208e9 字节 = **208 GB**。\n\n一张 80GB 的卡放不下，所以 13B 全参训练至少需要 **3-4 张卡**，而且还没算激活值。\n\n**这个 16 字节/参数的系数要记死**，它是理解以下所有技术的前提：\n- **ZeRO-1** 切优化器状态（省掉 m 和 v 的 8 字节）→ 降到约 8N\n- **ZeRO-2** 再切梯度 → 约 4N\n- **ZeRO-3 / FSDP** 再切参数 → 约 16N/DP\n- **LoRA** 冻结主干，只有适配器需要梯度和优化器状态 → 优化器部分几乎归零\n\n顺带对比：同样 13B 做**推理**只要 \`2N\` = 26 GB，单卡轻松装下。**训练和推理的显存需求差了 8 倍**，这是为什么很多人能跑推理但跑不了训练。"},

{id:"if1q8", type:"open", q:"用你自己的话解释：为什么「用更小的模型」和「用更大的 batch」都能提高吞吐，但它们改善的机制完全不同？",
hint:"分别想：换小模型改变了公式里的哪一项？增大 batch 又改变了哪一项？一个是减少分子还是分母，另一个呢？",
ref:"**两者都在提高「每单位时间能服务多少 token」，但作用在不同的地方。**\n\n**换更小的模型 —— 减少了每次必须搬的数据量。**\n7B 换成 3B，权重从 14 GB 降到 6 GB，每生成一个 token 要读的字节数少了一半多，所以单步耗时直接缩短。算术强度**没有变**（还是每 2 字节做 2 次运算），它仍然是 memory-bound，只是要搬的总量变少了。\n\n**增大 batch —— 提高了数据的复用次数。**\n权重还是那 14 GB，读一遍的时间没变。但这一遍读取现在同时服务了 32 个请求而不是 1 个。所以**单步耗时几乎不变，但每步产出翻了 32 倍**。算术强度从 1 涨到 32，算子在向 compute-bound 移动。\n\n**用公式看更清楚**：\n\`吞吐 ≈ batch / 单步耗时\`，而 \`单步耗时 ≈ 权重字节数 / 带宽\`\n- 换小模型：缩小分母\n- 增大 batch：放大分子\n\n**实践含义（这部分是加分项）**：\n- 增大 batch 几乎免费，但**受 KV Cache 显存限制**，而且会让单请求的 TPOT 变差（要排队等整批）\n- 换小模型必然损失质量，但能同时降低延迟和显存\n- **两者可以叠加**：小模型占显存少 → 能放更多 KV → batch 能开更大 → 吞吐是乘法关系提升\n- 所以生产上常见的组合是「蒸馏出小模型 + 量化 + 大 batch」，三者的收益是相乘而不是相加"}
]
};

window.LESSONS.inf2 = {
track: "infra", crumb: "Infra 主攻线 · 第 2 课",
title: "CUDA 执行模型：GPU 到底怎么跑代码",
sub: "不要求你成为 kernel 工程师，但要能读懂一段 kernel、判断它为什么慢。这是 NVIDIA 的核心筛子，也是其他家判断你深度的标尺。",
est: "读 90 分钟 + 做题 45 分钟",
sections: [
{h:"1. GPU 和 CPU 的根本分工不同",
b:`CPU 的设计目标是**让单个任务尽快完成**：大缓存、复杂的分支预测、乱序执行，几个核心但每个都很强。

GPU 的设计目标是**让大量任务总体吞吐最大**：几千个简单核心，缓存很小，没有复杂的预测逻辑。

这导致一个关键差异：**GPU 不怕延迟，靠并发来掩盖延迟。**

CPU 遇到一次内存访问（几百个时钟周期）会想尽办法预测和预取。GPU 的做法是：这个线程组要等数据？没关系，切换到另一个不用等的线程组继续算。只要同时有足够多的线程组，访存延迟就被藏起来了。

**这个「靠并发掩盖延迟」的机制，是理解后面所有概念（occupancy、warp 调度）的钥匙。**

所以 GPU 编程的思路和 CPU 完全不同：
- CPU：想办法让这一条指令流更快
- GPU：想办法让成千上万条指令流同时有活干，且访存规整`},

{h:"2. 线程的四层组织：thread / warp / block / grid",
b:`写 CUDA 时你要同时在四个层级上思考。

**thread（线程）** —— 最小单位，执行你写的那段代码。

**warp（线程束）** —— **32 个线程**捆成一束。这是硬件**真正的调度单位**。

> 这一条最重要：**你以为你在写单线程代码，实际上硬件是 32 个一起走的。**

**block（线程块）** —— 若干个 warp 组成一个 block。同一 block 内的线程：
- 跑在**同一个 SM** 上
- 可以共享 **Shared Memory**（片上高速内存）
- 可以用 \`__syncthreads()\` 互相同步

**grid（网格）** —— 所有 block 的集合，就是你这次 kernel 启动的全部。

\`\`\`
grid
 ├── block 0  ── warp 0 (32线程) ── warp 1 ── ...
 ├── block 1  ── ...
 └── ...
\`\`\`

**SM（Streaming Multiprocessor）** 是硬件上的物理单元。H100 有 132 个 SM。一个 block 被分配到一个 SM 上执行，一个 SM 可以同时驻留多个 block。

启动 kernel 时你指定 grid 和 block 的大小：
\`\`\`
kernel<<<gridDim, blockDim>>>(args);
\`\`\`
\`blockDim\` 通常取 128、256 或 512（**必须是 32 的倍数**，否则最后一个 warp 有线程闲置）。`},

{h:"3. warp 是真正的执行单位（以及分支的代价）",
b:`一个 warp 里的 32 个线程**共用一个程序计数器** —— 它们在同一时刻执行**同一条指令**，只是作用在各自的数据上。这叫 **SIMT（Single Instruction, Multiple Thread）**。

那如果代码里有 \`if\` 呢？

\`\`\`c
if (tid % 2 == 0) {
    do_A();     // 16 个线程要走这里
} else {
    do_B();     // 另外 16 个要走这里
}
\`\`\`

硬件做不到让半个 warp 走一条路、另外半个走另一条路。它的做法是：

1. 先执行 \`do_A()\`，让走 else 分支的 16 个线程**闲置**（掩码关闭）
2. 再执行 \`do_B()\`，让走 if 分支的 16 个线程闲置

**两条分支串行执行，总耗时是两者之和。** 这叫 **warp divergence（束内分歧）**。

**代价有多大**：最坏情况下，如果 32 个线程走 32 条不同的路径，性能是理想情况的 **1/32**。

**怎么避免**：
- 让分支条件**按 warp 对齐**。比如 \`if (tid / 32 == 0)\` 就不会分歧（整个 warp 走同一边），而 \`if (tid % 2 == 0)\` 会。
- 用算术代替分支：\`x = cond ? a : b\` 有时能被编译成无分支的 select 指令
- 把不同类型的工作**分到不同的 kernel 或不同的 block**，而不是在 warp 内部分叉

> **一个实际例子**：处理变长序列时，如果每个线程处理一个序列、而序列长度差异很大，那 warp 内就会有大量分歧（短的早早算完在等长的）。所以实践中要么做长度排序分桶，要么改成按 token 而非按序列分配线程。`},

{h:"4. 访存合并：最常见的性能杀手",
b:`这一节讲的东西，是实际工作中**最容易犯也最容易修**的性能问题。

当一个 warp 的 32 个线程同时访问显存时，硬件会检查它们的地址：

- **如果地址连续且对齐**（落在同一个 128 字节的缓存行范围内）→ 合并成 **1 次**内存事务
- **如果地址分散** → 最多退化成 **32 次**独立事务

**差距高达 32 倍。**

**看一个具体例子。** 假设有个二维数组 \`A[M][N]\`，按行优先存储（同一行的元素在内存里连续）。

\`\`\`c
// ✗ 按列访问：线程 i 读 A[i][col]
// 相邻线程的地址间隔是 N 个元素 —— 完全不合并
float v = A[tid * N + col];

// ✓ 按行访问：线程 i 读 A[row][i]
// 相邻线程的地址连续 —— 完美合并
float v = A[row * N + tid];
\`\`\`

同样的计算量，第一种可能慢十几倍。

**怎么解决**：
1. **调整数据布局**：从 AoS（结构体数组）改成 SoA（数组的结构体），让同类数据连续
2. **借道 Shared Memory**：先用合并的方式把数据读进 SMEM，再在 SMEM 里做不规则访问（SMEM 没有合并要求）
3. **分块转置**：矩阵转置这类天然不合并的操作，用 SMEM 做分块中转

**注意区分两个概念**（面试常混）：
- **HBM 要求访存合并**（地址连续）
- **Shared Memory 要求避免 bank conflict**（下一节）

这是两个不同层级、不同机制的问题。`},

{h:"5. Shared Memory 与 bank conflict",
b:`**Shared Memory（SMEM）** 是每个 SM 上的一块片上内存（H100 每 SM 228 KB），速度接近 L1 缓存，比 HBM 快一个量级。

它的用法是：**把要反复使用的数据先搬进来，在片上复用，减少对 HBM 的访问。** 这是几乎所有高性能 kernel（GEMM、FlashAttention、卷积）的核心手法。

**但 SMEM 有自己的坑：bank conflict。**

SMEM 被划分成 **32 个 bank**（正好对应一个 warp 的 32 个线程），按 4 字节交错分布：
- 地址 0、128、256… 属于 bank 0
- 地址 4、132、260… 属于 bank 1
- 以此类推

**规则**：一个 warp 内，如果多个线程访问**同一个 bank 的不同地址**，这些访问会**串行化**。

（例外：如果访问的是**同一个地址**，硬件会广播，不算冲突。）

**经典的冲突场景**：
\`\`\`c
__shared__ float tile[32][32];
// 线程 i 访问 tile[i][0]
// 地址 = i * 32 * 4 字节 = i * 128 字节
// 全部落在 bank 0 —— 32 路冲突，性能降到 1/32
float v = tile[tid][0];
\`\`\`

**经典的解法：padding**
\`\`\`c
__shared__ float tile[32][33];   // 行宽改成 33
// 现在 tile[i][0] 的地址 = i * 33 * 4
// 相邻 i 落在不同 bank —— 冲突消除
\`\`\`

多加一列看起来浪费，但换来 32 倍的访问效率。**这是 CUDA 优化里最经典的小技巧之一。**`},

{h:"6. Occupancy：让 SM 有足够的活可切换",
b:`回到最开始那句话：**GPU 靠并发掩盖延迟。**

**Occupancy（占用率）** = 一个 SM 上实际驻留的 warp 数 ÷ 硬件支持的最大 warp 数。

它衡量的是：**当某些 warp 在等数据时，有没有别的 warp 可以顶上。**

occupancy 太低 → 所有 warp 都在等访存 → SM 空转。

**什么限制了 occupancy**：
1. **寄存器用量**。每个 SM 的寄存器总数固定（H100 是 64K 个）。如果你的 kernel 每线程用 64 个寄存器，那一个 SM 最多驻留 \`65536/64 = 1024\` 个线程 = 32 个 warp。用得越多，能驻留的越少。
2. **Shared Memory 用量**。同理，SMEM 总量固定，每个 block 用得越多，能同时驻留的 block 越少。
3. **block 大小**。太小则调度开销占比高，太大则灵活性差。

**但有个重要的认知纠正**：

> **occupancy 高不等于快，低也不一定慢。**
>
> 如果 kernel 已经**打满了带宽**，再多 warp 也只是一起排队等数据，提高 occupancy 毫无帮助。
> 反过来，有些高度优化的 GEMM kernel 故意用大量寄存器做数据复用（导致 occupancy 只有 25%），但因为每个线程干的活多、访存少，反而更快。

所以正确的用法是：**当 profiler 显示 latency-bound（算力和带宽都没打满）时，才去查 occupancy。**

**怎么调**：
- 用 \`__launch_bounds__\` 提示编译器限制寄存器用量
- 减小 block 大小或 SMEM 用量
- 用 Nsight Compute 的 occupancy calculator 看瓶颈是寄存器还是 SMEM`},

{h:"7. 两个不写 kernel 也能拿到的优化",
b:`即使你不手写 CUDA，有两个优化手段是必须知道的，因为它们收益大、门槛低。

**① 算子融合（kernel fusion）**

每启动一个 kernel 都要：读 HBM → 算 → 写回 HBM。

逐元素算子（bias、激活、残差、LayerNorm）计算量极小但访存量大，单独跑纯属浪费带宽。把连续的几个融成一个 kernel，中间结果留在寄存器里，**只读写一次 HBM**。

把 \`Linear → Bias → GELU\` 融合，能省掉两次完整的 HBM 往返。因为这些算子是纯 memory-bound，**收益接近于融合的算子个数倍**。

怎么做（从易到难）：
- \`torch.compile\` —— 一行代码，Inductor 自动融合并生成 Triton kernel
- 用现成的融合算子库（FlashAttention、liger-kernel、apex）
- 自己用 Triton 写

**② CUDA Graph**

每次 \`cudaLaunchKernel\` 有几微秒的 CPU 开销。decode 一步要跑几百个 kernel，而每个 kernel 本身可能只要几十微秒 —— **小 batch 时 CPU 的 launch 开销能占总时间的 30% 以上**，GPU 在等 CPU 喂活。

CUDA Graph 把整个 decode step 的 kernel 序列**录制成一张图**，之后一次提交、由 GPU 按图执行，CPU 只需一次调用。

**限制**：图是静态的，形状必须固定。所以实现上要为不同 batch size 预先捕获多张图（vLLM 就是这么做的，把 batch size 向上取整到预设档位）。

**怎么判断该用哪个**（这是面试里的实际判断题）：
- Nsight Systems 里看到 **GPU 时间线有大量空隙、CPU 侧 launch 密集** → CUDA Graph
- 看到很多**短小的 memory-bound kernel 连续排列** → 融合
- 两者叠加用，vLLM 同时做了这两件事`}
],
quiz: [
{id:"if2q1", type:"num", q:"一个 warp 有多少个线程？", hint:"这是 CUDA 里最基本的常数，所有 NVIDIA GPU 上都一样。", ans:32,
why:"**32**。这是所有 NVIDIA GPU 的固定值，是硬件真正的调度单位。\n\n由此推出几条实践规则：\n- **block 大小应该是 32 的倍数**，否则最后一个 warp 有线程闲置\n- 分支条件最好**按 warp 对齐**，避免 divergence\n- Shared Memory 恰好分 32 个 bank，对应一个 warp 的宽度\n- warp 内可以用 shuffle 指令（\`__shfl_down_sync\`）直接交换数据，不需要经过 SMEM"},

{id:"if2q2", type:"mc", q:"下面哪段代码会造成 warp divergence？（tid 是线程在 block 内的编号）",
choices:["if (tid / 32 == 0) { A(); } else { B(); }",
         "if (tid % 2 == 0) { A(); } else { B(); }",
         "if (blockIdx.x == 0) { A(); } else { B(); }",
         "以上都不会"], ans:1,
hint:"divergence 发生在同一个 warp 内部（连续的 32 个线程）走了不同分支。逐个检查：每个条件下，tid 为 0~31 的这 32 个线程会不会分开走？",
why:"**第二个会造成 divergence。**\n\n\`tid % 2 == 0\` 让偶数线程走 A、奇数线程走 B —— 同一个 warp 内的 32 个线程**一半一半分开了**，两条分支串行执行，耗时翻倍。\n\n其余为什么不会：\n- \`tid / 32 == 0\`：**按 warp 对齐**。warp 0（tid 0-31）整体走 A，其他 warp 整体走 B，每个 warp 内部是一致的。\n- \`blockIdx.x == 0\`：block 级别的判断，整个 block（所有 warp）走同一边。\n\n**记住判据：只要同一个 warp 里的 32 个线程走向一致，就没有 divergence。** 优化分歧的思路就是把条件的粒度从线程级提升到 warp 级或 block 级。"},

{id:"if2q3", type:"mc", q:"数组 A 是 M×N 的行优先二维数组。哪种访问模式能实现访存合并？",
choices:["float v = A[tid * N + col];  每个线程读一列里的不同行",
         "float v = A[row * N + tid];  每个线程读一行里的不同列",
         "float v = A[tid * tid];      平方索引",
         "三种都可以"], ans:1,
hint:"行优先意味着同一行的元素在内存里是连续的。哪种写法让相邻的 tid 对应相邻的内存地址？",
why:"**第二个。** \`A[row * N + tid]\` 中，相邻线程的 tid 差 1，地址也差 1 个元素 —— **连续且对齐**，一个 warp 的 32 次访问被合并成 1 次内存事务。\n\n第一个 \`A[tid * N + col]\`：相邻线程的地址差 **N 个元素**（跨越一整行），完全不合并，退化成 32 次独立事务，**慢最多 32 倍**。\n\n第三个更糟，地址间隔还在变化。\n\n**这是实际工作中最高频的性能 bug。** 典型场景是矩阵按列处理、或者用了 AoS（结构体数组）布局。\n\n**解法**：改数据布局（AoS→SoA）、或者先用合并的方式读进 Shared Memory 再在片上做不规则访问。"},

{id:"if2q4", type:"num", q:"__shared__ float tile[32][32] 中，一个 warp 的 32 个线程分别访问 tile[tid][0]。这会造成几路 bank conflict？",
hint:"SMEM 有 32 个 bank，按 4 字节交错。算出 tile[tid][0] 的字节地址，看它落在哪个 bank（地址除以 4 再对 32 取模）。", ans:32,
why:"\`tile[tid][0]\` 的地址 = \`tid × 32 × 4\` = \`tid × 128\` 字节。\n\nbank 编号 = \`(地址/4) % 32\` = \`(tid × 32) % 32\` = **0**。\n\n所有 32 个线程都落在 **bank 0** → **32 路冲突**，访问被完全串行化，性能降到 1/32。\n\n**经典解法是 padding**：\n\`\`\`c\n__shared__ float tile[32][33];   // 行宽 32 → 33\n\`\`\`\n现在地址 = \`tid × 33 × 4\`，bank = \`(tid × 33) % 32\` = \`tid % 32\` —— 32 个线程落在 32 个不同 bank，**冲突完全消除**。\n\n多用了 32 个 float 的空间，换来 32 倍的访问效率。这是 CUDA 优化里性价比最高的小技巧。"},

{id:"if2q5", type:"mc", q:"Nsight Compute 显示某 kernel 的 SM throughput 是 12%、DRAM throughput 是 15%、achieved occupancy 是 8%。最该做什么？",
choices:["优化访存合并，因为带宽没打满",
         "提高 occupancy —— 算力和带宽都很低，说明是 latency-bound",
         "换用 Tensor Core",
         "减小 block size"], ans:1,
hint:"算力和带宽都很低，说明硬件的两种资源都没被用满。那时间到底花在哪了？什么情况下 GPU 既不在算也不在搬，而是在等？",
why:"**两个 throughput 都很低 = latency-bound**（延迟受限）。\n\n意思是 GPU 既没在满负荷计算，也没在满负荷搬数据 —— 它在**等**。等访存返回、等同步、等依赖。\n\n而 GPU 掩盖延迟的唯一手段就是**切换到别的 warp**。occupancy 只有 8% 意味着 SM 上驻留的 warp 太少，没有足够的备胎可切换，只能干等。\n\n**所以正确动作是提高 occupancy**：减少每线程的寄存器用量（\`__launch_bounds__\`）、减少 SMEM 用量、或调整 block 大小。\n\n其余为什么不对：\n- **优化访存合并**：带宽才 15%，说明问题不在搬得不够快\n- **Tensor Core**：算力才 12%，本来就没算力压力\n- **减小 block size**：方向不一定对，要看瓶颈是寄存器还是 SMEM\n\n**这道题的价值在于那个判断表**：\n- SM 高 → compute-bound\n- DRAM 高 → memory-bound\n- **都低 → latency-bound，查 occupancy 和 stall reasons**"},

{id:"if2q6", type:"mc", q:"关于算子融合和 CUDA Graph，哪个说法正确？",
choices:["两者都是为了减少 HBM 访问",
         "融合减少 HBM 往返，CUDA Graph 减少 CPU 的 kernel 启动开销",
         "CUDA Graph 能自动完成算子融合",
         "两者只对 compute-bound 的算子有效"], ans:1,
hint:"分别想：融合把几个 kernel 变成一个，省掉了什么？CUDA Graph 把几百次 launch 变成一次提交，省掉的又是什么？这两样东西在同一个地方吗？",
why:"**第二个正确。这两个优化解决的是完全不同层面的开销。**\n\n**算子融合** → 省的是 **GPU 侧的 HBM 往返**。把 \`Linear→Bias→GELU\` 融成一个 kernel，中间结果留在寄存器，不用写回显存再读出来。\n\n**CUDA Graph** → 省的是 **CPU 侧的 launch 开销**。decode 一步几百个 kernel，每次启动有几微秒 CPU 开销，小 batch 时能占总时间三成以上。录成图后一次提交。\n\n其余为什么错：\n- 第一个：只说对了融合那一半\n- 第三个：CUDA Graph 只是**记录并重放**已有的 kernel 序列，不会改变或合并 kernel 本身\n- 第四个：**正好说反了**。这两个优化主要惠及 **memory-bound 和小 kernel 密集**的场景，也就是 decode 阶段\n\n**怎么判断用哪个**：Nsight Systems 里看到 GPU 有大量空隙、CPU launch 密集 → CUDA Graph；看到很多短小的 memory-bound kernel 排排站 → 融合。两者可以叠加。"},

{id:"if2q7", type:"open", q:"解释一下：为什么有些高度优化的 GEMM kernel 故意把 occupancy 降到 25% 左右，反而更快？",
hint:"occupancy 低是因为每个线程用了很多寄存器。那些寄存器被用来做什么了？如果一个线程在寄存器里攒了很多数据，它需要多频繁地去访问显存？",
ref:"**因为它们用寄存器换取了数据复用，把自己从 memory-bound 变成了 compute-bound。**\n\n高性能 GEMM 的核心手法是**多级 tiling**：每个线程不是只算一个输出元素，而是在寄存器里维护一小块（比如 8×8）的累加结果。\n\n这样做的后果：\n- **寄存器用量大增**（要存 64 个累加值 + 输入的缓存）→ 每个 SM 能驻留的线程数减少 → **occupancy 下降**\n- 但**每从 SMEM/HBM 读一次数据，能参与的计算次数大幅增加** → **算术强度大幅提高**\n\n举例：如果每个线程只算 1 个输出，读 2 个输入才做 1 次乘加，算术强度约 0.25；如果每个线程算 8×8=64 个输出，读 8+8=16 个输入就能做 64 次乘加，算术强度跳到 4 —— 提高了 16 倍。\n\n**关键在于靠什么掩盖延迟变了**：\n- 低算术强度的 kernel 靠**大量 warp 轮换**掩盖访存延迟 → 需要高 occupancy\n- 高算术强度的 kernel 每个 warp 自己就有大量独立的计算指令可以填满流水线（**指令级并行 ILP**）→ 不需要那么多 warp\n\n**所以结论是**：occupancy 是手段不是目标。真正的目标是**让计算单元一直有活干**，途径有两条 —— 要么多找几个 warp 轮着来，要么让单个 warp 自己就有足够多的独立工作。\n\n**实践判断**：只有当 profiler 显示 latency-bound（SM 和 DRAM throughput 都低）时，提高 occupancy 才是正确动作。如果已经是 compute-bound，降低 occupancy 换取更高的数据复用反而是对的。"}
]
};

window.LESSONS.inf3 = {
track: "infra", crumb: "Infra 主攻线 · 第 3 课",
title: "推理引擎内部：vLLM 到底做了什么",
sub: "从「为什么需要 KV Cache」一路推到 continuous batching 和 PagedAttention。学完你能画出一次调度 iteration 的完整决策流程。",
est: "读 90 分钟 + 做题 45 分钟",
sections: [
{h:"1. 自回归生成的朴素做法有多浪费",
b:`语言模型生成文本是**自回归**的：先根据 prompt 生成第 1 个 token，再把它拼回输入生成第 2 个，如此往复。

朴素实现是这样：
\`\`\`
输入 今天天气       → 前向 → 输出 真
输入 今天天气真     → 前向 → 输出 好
输入 今天天气真好   → 前向 → 输出 。
\`\`\`

问题很明显：**每一步都把整个序列重新算了一遍。**

生成第 n 个 token 时，前面 n-1 个 token 的计算被完整重复。总计算量是 \`O(n²)\`。

但仔细想想：在计算注意力时，因为有**因果掩码**（每个位置只能看到自己和之前的位置），第 i 个 token 的 K 和 V 向量**永远不会改变** —— 不管后面又来了多少新 token。

既然不变，为什么每次都重算？

**存下来。** 这就是 KV Cache。

有了 KV Cache，每一步只需要：
1. 为新 token 算出它的 Q、K、V
2. 把新的 K、V 追加到缓存里
3. 用新的 Q 和**全部缓存的 K、V** 做注意力

每步的计算量从 \`O(n)\` 降到 \`O(1)\`（相对于序列长度），总计算量从 \`O(n²)\` 降到 \`O(n)\`。`},

{h:"2. KV Cache 的代价：显存",
b:`省了计算，代价是显存。而且这个代价常常比你想的大。

$$KV = 2 × 层数 × KV头数 × 头维度 × 序列长 × batch × 字节数$$

拿 LLaMA-2 7B 算（32 层、32 个 KV 头、每头 128 维、FP16）：

\`2 × 32 × 32 × 128 × 2\` = **512 KB / token**

看起来不多？往下算：
- 一个 4096 token 的请求：**2 GB**
- 32 个这样的并发：**64 GB**

而模型权重才 14 GB。**KV Cache 比模型本身还大。**

一张 80GB 的 H100，装完权重剩 66GB，只能支撑 **33 个并发**。

**这就是 LLM 服务的核心矛盾**：你想提高吞吐就得加大 batch，但 batch 受 KV Cache 显存限制。

**所以压缩 KV Cache 是提高并发最直接的手段**，主要有四条路：

**① 减少 KV 头数（GQA / MQA）**
让多个 Q 头共享一组 KV。32 头分 8 组 → KV 直接省 4 倍。质量几乎无损，LLaMA-2 70B 之后成为标配。

**② 低秩压缩（MLA）**
DeepSeek 的做法：把 KV 投影到一个低维潜向量存储，用时再升维。压缩比 GQA 更狠，论文报告效果还反超。

**③ 降低精度（KV 量化）**
FP16 → FP8 直接省一半。可以和上面两条叠加。

**④ 消除浪费（PagedAttention）**
不改变 KV 的内容，而是改变**存储方式**，把碎片浪费掉的 60-80% 拿回来。这是下面要讲的重点。`},

{h:"3. 静态批处理的浪费",
b:`知道了要加大 batch，那就把请求攒起来一批批跑。这叫**静态批处理**。

问题在于：**不同请求的生成长度差异巨大。**

假设一批 4 个请求，生成长度分别是 20、50、200、30 个 token。

静态批处理必须等**最长的那个（200）**跑完，整批才能返回。于是：
- 请求 1 在第 20 步就生成完了，但它的槽位要**空转 180 步**
- 请求 2、4 同理

实际有效利用率可能只有 **30-40%**。而且用户体验也差 —— 明明第 20 步就有答案了，却要等到第 200 步才拿到。

**Continuous Batching（连续批处理）的解法**：

把调度单位从「一批请求」改成「**一次 iteration（一步 decode）**」。

每一步结束后：
- 检查有没有请求生成完了（出 EOS 或达到 max_tokens）→ **立刻踢出、立刻返回给用户**
- 空出来的槽位 → **立刻从等待队列补新请求进来**

这样 GPU 的批次里始终塞满了活跃请求，没有空转。

**收益**：吞吐提升通常是**数倍**，同时短请求的延迟大幅改善（不用陪跑）。

这个机制也叫 in-flight batching（TensorRT-LLM 的叫法）或 iteration-level scheduling。`},

{h:"4. PagedAttention：把显存碎片拿回来",
b:`Continuous batching 解决了调度问题，但还有一个显存问题。

**传统实现给每个请求预分配一块连续的 KV 显存，大小按 \`max_length\` 算。**

为什么要预分配最大长度？因为不知道它会生成多长，而且要求连续。

于是：
- **内部碎片**：一个请求实际只生成了 100 个 token，却占着 2048 个 token 的空间 —— 浪费 95%
- **外部碎片**：不同长度的请求进进出出，中间留下的空隙装不下新请求

实测显存浪费达到 **60-80%**。

**PagedAttention 的解法，直接借鉴了操作系统的虚拟内存。**

操作系统怎么解决进程内存碎片的？分页：把物理内存切成固定大小的页，用页表把连续的虚拟地址映射到不连续的物理页。

PagedAttention 做同样的事：
- 把 KV Cache 切成固定大小的 **block**（默认 16 个 token）
- 每个请求维护一张 **block table**，记录它的逻辑位置对应哪些物理 block
- **按需分配**：生成到第 17 个 token 时才申请第 2 个 block
- **物理上不必连续**

注意力 kernel 要改造成能按 block table 去 gather 数据。

**收益**：
- 显存利用率接近 **100%**（唯一浪费是最后一个 block 的零头，平均半个 block）
- 同样显存能放下 **2-4 倍**的并发 → 吞吐成倍提升

**额外的大收益：前缀共享。**

如果多个请求共用同一段 system prompt，它们的前几个 block 内容完全相同 —— 那就**只存一份**，多个 block table 指向同一个物理 block，用引用计数管理，需要修改时才复制（copy-on-write）。

这就是 **prefix caching** 的实现基础，也让「一个 prompt 生成 n 个候选」这种并行采样几乎免费。`},

{h:"5. block size 怎么选，抢占怎么做",
b:`**block size 的权衡**：

- **太小**（如 1-4 个 token）：block table 变长，kernel 里的间接寻址开销上升，访存合并变差
- **太大**（如 128）：最后一个 block 的浪费变大（平均浪费 block_size/2 个 token），前缀共享的粒度也变粗
- **16 是经验甜点**，兼顾访存效率和碎片率

**抢占（preemption）**：

显存总会不够。当新请求进不来、或者正在运行的请求需要新 block 但没有空闲时，调度器要挑一个受害者踢出去。

踢出去的请求，它已经算好的 KV 怎么办？两个选择：

**① Swap（换出）**：把它的 KV Cache 拷贝到 CPU 内存，之后重新调度时再拷回来。
- 优点：不用重算
- 缺点：占 PCIe 带宽（比 HBM 慢 50 倍），拷贝本身要时间

**② Recompute（重算）**：直接丢弃 KV，之后重新 prefill。
- 优点：不占传输带宽
- 缺点：要重新算一遍

**怎么选**：
- **短 prompt 用 recompute**（重算很快，传输反而不划算）
- **长 prompt 用 swap**（重算太贵）
- 分界点可以实测标定：比较重算耗时和来回传输耗时

vLLM 默认用 recompute，因为大多数场景下 prompt 不算太长。

**准入控制（admission）**：

不只是显存，还要控制每次 iteration 的**工作量**。参数 \`max_num_batched_tokens\` 限制一次 iteration 能处理的总 token 数（prefill 的 token + decode 的 batch）。

这个参数是 **chunked prefill** 的基础：一个 8000 token 的 prompt 会被切成多个 chunk，每次只处理预算内的一部分，剩下的下次再来 —— 这样它就不会独占一次 iteration 把 decode 请求全卡住。`},

{h:"6. 一次 iteration 的完整决策流程",
b:`把前面的都串起来。调度器每一步做这些事：

**① 回收**
检查上一步的结果，把生成完成的请求（EOS 或 max_tokens）移出 running 集合，**释放它们的 block**，把结果返回给用户。

**② 抢占检查**
如果 running 集合里有请求需要新 block 但显存不足 → 按策略选一个受害者，swap 或 recompute 出去，放回 waiting 队列。

**③ 准入**
从 waiting 队列挑请求加入 running。要同时满足三个约束：
- \`max_num_seqs\`：并发请求数上限
- \`max_num_batched_tokens\`：本次 iteration 的 token 预算
- **能分配到足够的 block**

**④ 组批**
把这一步要处理的请求拼成一个批次。可能同时包含：
- 若干个 decode 请求（每个 1 个 token）
- 若干个 prefill 请求或 prefill chunk

**⑤ 执行**
一次前向传播。注意力 kernel 按各请求的 block table 去读 KV。

**⑥ 采样与更新**
对每个序列采样下一个 token，追加到它的 KV block，更新长度和状态。

然后回到 ①。

> **画出这张图**，你就理解了 vLLM 的核心。面试里被问「continuous batching 怎么实现」，按这六步讲就是满分答案。

**调度策略的选择**（加分点）：
- 纯 **FCFS** 最简单，但长请求会饿死短请求
- 按剩余长度排序能改善 P99，但输出长度**预测不准**
- 生产上常用**分级队列**：交互式和批处理走不同池子，各有 token 预算和优先级`},

{h:"7. 三个引擎怎么选",
b:`**vLLM** —— 默认选择
- PagedAttention 的原创者，社区最活跃，模型支持最广最新
- OpenAI 兼容 API 开箱即用
- 劣势：极致性能不如 TRT-LLM，Python 调度有一定开销
- **适合：绝大多数场景，尤其是模型换得勤的时候**

**TensorRT-LLM** —— 性能天花板
- NVIDIA 官方，深度利用硬件（FP8、自定义 kernel、CUDA Graph）
- 同硬件上通常延迟最低、吞吐最高
- 劣势：需要**预编译 engine**（改模型、改并行度、改精度都要重编，几分钟到几十分钟），新模型支持慢，调试门槛高
- **适合：模型固定、规模大、对成本极度敏感的生产环境**

**SGLang** —— 前缀复用与结构化输出
- **RadixAttention**：用前缀树组织 KV 缓存，能自动找到最长公共前缀，比简单的 block 哈希灵活得多
- 多轮对话、Agent、树状搜索场景优势明显
- 内置高效的结构化输出（XGrammar）
- **适合：Agent、多轮对话、大量前缀复用的场景**

**怎么在面试里答这道题**：不要评优劣，给**决策树**，然后加一句「我会用自己的真实流量做 A/B 压测再定」，并说明你会测什么 —— 不同并发下的 TTFT/TPOT/吞吐曲线、显存占用、以及切换的工程成本。

**这比背结论强得多。**`}
],
quiz: [
{id:"if3q1", type:"mc", q:"为什么可以把 K 和 V 缓存下来，但 Q 不行？",
choices:["Q 占的显存太大",
         "因为因果掩码的存在，历史 token 的 K/V 不会改变，而每步需要的是新 token 的 Q",
         "Q 的计算比 K/V 便宜，缓存没意义",
         "Q 在每一层都不同，K/V 只在第一层计算"], ans:1,
hint:"想想注意力在算什么：新 token 的 Q 去和谁做点积？那些被点积的对象，在下一步会变吗？而 Q 本身，下一步还用得上吗？",
why:"**关键是因果掩码带来的不变性。**\n\n每一步生成时，模型需要：用**新 token 的 Q**，去和**所有历史 token 的 K、V** 做注意力。\n\n- **历史 token 的 K、V 永远不变** —— 因为因果掩码保证每个位置只看到自己和之前的内容，后面来了新 token 不会影响它们。所以值得缓存。\n- **Q 每步都是新算的，而且用完就扔** —— 上一步那个 token 的 Q 在这一步完全用不上了。缓存它没有意义。\n\n**第四个选项是常见误解**：K/V 在**每一层**都要重新计算（每层有自己的投影矩阵），所以 KV Cache 是**按层存储**的。这也是为什么显存公式里有层数这一项。"},

{id:"if3q2", type:"num", q:"某请求的 max_length 设为 2048，但实际只生成了 120 个 token。用传统的预分配方式，这个请求浪费了多少百分比的 KV 显存？（填整数）",
hint:"预分配按 max_length 算，实际只用了 120。浪费的 = (预分配 − 实际) ÷ 预分配。", ans:94, tol:1,
why:"\`(2048 − 120) / 2048\` = **94%** 被浪费。\n\n这就是**内部碎片**，是 PagedAttention 要解决的主要问题。加上不同长度请求进出留下的**外部碎片**，实测总浪费达到 60-80%。\n\n**PagedAttention 之后**：按 16 token 一个 block 按需分配，120 个 token 需要 \`ceil(120/16) = 8\` 个 block，共 128 个 token 的空间。浪费只有 \`8/128 = 6%\`（最后一个 block 的零头）。\n\n**从浪费 94% 到浪费 6%** —— 同样的显存能装下十几倍的请求。这就是为什么 vLLM 的吞吐能碾压朴素实现。"},

{id:"if3q3", type:"mc", q:"静态批处理里，一批 4 个请求的生成长度分别是 20、50、200、30。如果改用 continuous batching，最大的收益是什么？",
choices:["单个请求的生成速度变快了",
         "短请求能立刻返回并让出槽位给新请求，GPU 不再空转",
         "模型的显存占用变小了",
         "每个 token 的计算量减少了"], ans:1,
hint:"静态批处理下，生成 20 个 token 就结束的那个请求，在第 21 步到第 200 步之间在做什么？continuous batching 改变了什么？",
why:"**收益来自消除空转，不是让单步变快。**\n\n静态批处理下，请求 1 在第 20 步就完成了，但它的槽位要**空转 180 步**等最长的那个（200）跑完。整批的有效利用率可能只有 30-40%。\n\nContinuous batching 以 **iteration 为调度单位**：请求 1 一出 EOS 就立刻返回并释放槽位，等待队列里的新请求马上补进来。GPU 的批次始终是满的。\n\n**顺带的好处**：短请求的延迟也大幅改善 —— 不用陪着长请求跑完。\n\n其余为什么错：\n- **单请求速度没变**：每一步的计算量是一样的\n- **显存没变小**：那是 PagedAttention 的功劳，两者是不同的优化\n- **计算量没减少**：总的 token 数没变，只是排布更紧密了\n\n**要分清这两个优化**：continuous batching 解决**调度空转**，PagedAttention 解决**显存碎片**。vLLM 同时做了这两件事，收益是相乘的。"},

{id:"if3q4", type:"num", q:"用 PagedAttention，block size = 16。一个生成了 100 个 token 的请求需要分配几个 block？",
hint:"按需分配，每个 block 装 16 个 token。100 个 token 需要向上取整。", ans:7,
why:"\`ceil(100 / 16)\` = **7 个 block**（能装 112 个 token）。\n\n浪费 12 个 token 的空间，浪费率约 10.7%。\n\n**平均浪费是半个 block**（block_size/2 = 8 个 token），这就是 PagedAttention 唯一的显存开销。对比传统预分配的 94% 浪费，可以忽略不计。\n\n**由此理解 block size 的权衡**：\n- block 越大 → 最后一块的平均浪费越大（block_size/2），但 block table 更短、间接寻址开销更小\n- block 越小 → 浪费小，但 table 长、kernel 里 gather 的开销大、访存合并变差\n- **16 是实践中的甜点**"},

{id:"if3q5", type:"text", q:"调度器要把一个请求踢出去腾显存，这个请求的 prompt 有 8000 个 token。应该用 swap 还是 recompute？（填 swap 或 recompute）",
ans:["swap","Swap","换出"],
hint:"对比两种方案的代价：重新 prefill 8000 个 token 要多久？把它的 KV 拷到 CPU 内存再拷回来要多久？哪个更划算？",
why:"**长 prompt 用 swap。**\n\n重新 prefill 8000 个 token 是一次完整的大规模前向计算，代价很高（可能几百毫秒）。而把 KV 拷到 CPU 内存再拷回来，虽然走 PCIe（比 HBM 慢约 50 倍），但**只是一次数据搬运，不涉及计算**。\n\n对长 prompt 来说，传输代价远小于重算代价。\n\n**反过来，短 prompt 用 recompute**：重新算几十个 token 很快，而 swap 要占 PCIe 带宽、还有拷贝的固定开销，反而不划算。\n\n**分界点可以实测标定**：比较重新 prefill 的耗时和 KV 来回传输的耗时，找到交叉点。\n\n补充：vLLM 默认用 recompute，因为大多数线上场景 prompt 不算特别长，而且 recompute 的实现更简单、不占 CPU 内存。这是个合理的默认值，但长文档场景应该考虑改。"},

{id:"if3q6", type:"mc", q:"关于 prefix caching，下面哪个说法不正确？",
choices:["它建立在 PagedAttention 的 block 共享机制之上",
         "多个请求共享同一 system prompt 时，对应的物理 block 只存一份",
         "它能显著降低多轮对话场景的 prefill 成本",
         "开启后所有请求的 decode 速度都会提升"], ans:3,
hint:"prefix caching 复用的是已经算好的 KV，它省掉的是哪个阶段的计算？decode 阶段每一步都要读全部 KV，这一点会因为缓存而改变吗？",
why:"**第四个不正确。**\n\nprefix caching 省的是 **prefill 阶段**的重复计算 —— 共享的前缀不用重新算 KV 了。它对 **TTFT（首 token 延迟）**改善巨大。\n\n但 **decode 阶段完全不受益**：每生成一个 token 仍然要读全部权重和全部 KV，这个过程和缓存没关系。TPOT 不会因为 prefix caching 变快。\n\n其余三个都对：\n- 它确实建立在 PagedAttention 的 block 抽象上（用引用计数 + copy-on-write 共享物理块）\n- 共享 system prompt 只存一份，这是它省显存的来源\n- 多轮对话每一轮都能复用前面所有轮次，第 n 轮只需 prefill 新增部分，收益极大\n\n**这是最高性价比、且零质量损失的优化**，任何降本讨论都该从它开始。\n\n**一个容易漏但很加分的点**：多租户场景下，prefix cache 必须**按租户隔离命名空间** —— 否则能通过缓存命中带来的时延差异推测别人的 prompt 内容，这是真实的信息泄漏风险。"},

{id:"if3q7", type:"open", q:"按顺序写出 continuous batching 调度器在一次 iteration 里要做的主要决策（至少四步），并说明每一步的约束是什么。",
hint:"从上一步刚跑完开始想：先要处理什么？然后显存不够怎么办？新请求什么条件下能进来？最后怎么组成一个批次去执行？",
ref:"**六个步骤**：\n\n**① 回收（retire）**\n检查上一步的输出，把生成完成的请求（出 EOS 或达到 max_tokens）移出 running 集合，**释放它们占用的 KV block**，把结果返回给用户。\n\n**② 抢占（preempt）**\n如果 running 集合里有请求需要新 block 但显存不足，按策略挑一个受害者踢出（swap 到 CPU 或 recompute 丢弃），放回 waiting 队列。约束是**可用 block 数**。\n\n**③ 准入（admit）**\n从 waiting 队列挑请求加入 running。要同时满足三个约束：\n- \`max_num_seqs\`（并发请求数上限）\n- \`max_num_batched_tokens\`（本次 iteration 的 token 预算）\n- **能为它分配到足够的 KV block**\n\n**④ 组批（batch）**\n把这一步要处理的请求拼成一个批次，可能同时包含若干 decode 请求（每个 1 token）和若干 prefill 请求或 prefill chunk。\n\n**⑤ 执行（forward）**\n一次前向传播。注意力 kernel 按各请求的 block table 去 gather 对应的 KV。\n\n**⑥ 采样与更新（sample）**\n对每个序列采样下一个 token，追加到它的 KV block，更新序列长度和状态。\n\n然后回到 ①。\n\n**能补充的加分内容**：\n- **调度策略**：纯 FCFS 会让长请求饿死短请求；按剩余长度排序能改善 P99 但输出长度预测不准；生产上常用分级队列（交互式 vs 批处理各有预算）\n- **chunked prefill** 就是在第 ③④ 步起作用：长 prompt 被 token 预算切成多块，分多次 iteration 处理，避免独占一次前向把 decode 请求全卡住\n- 这六步里最容易被忽略但很关键的是 **②抢占** —— 它决定了过载时系统是优雅降级还是雪崩"}
]
};
