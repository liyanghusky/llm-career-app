/* 阶段 0 课程内容（一）：线性代数 / 概率统计 / 微积分与优化
   面向有高中理科基础的自学者，从零讲起。
   quiz type: num 数值 | mc 单选 | text 文本 | open 开放（自评）
*/
window.LESSONS = {};

window.LESSONS.s0n1 = {
title: "线性代数：从一串数字到 SVD",
sub: "神经网络里 99% 的计算是矩阵乘法。这一课的目标是让「矩阵」从一堆符号变成你脑子里能看见的东西。",
est: "读 90 分钟 + 做题 40 分钟",
sections: [
{h:"1. 向量：一串数字，也是一支箭",
b:`先忘掉课本上「既有大小又有方向的量」这种说法。在机器学习里，**向量就是一串有顺序的数字**：

\`v = [3, 4]\`

这串数字有两种看法，你必须同时具备：

**看法一（代数）**：它就是两个数，第 1 个是 3，第 2 个是 4。

**看法二（几何）**：它是平面上从原点 (0,0) 指向点 (3,4) 的一支箭。

这支箭的长度（记作 \`‖v‖\`）用勾股定理算：

$$‖v‖ = √(3² + 4²) = √25 = 5$$

推广到 n 个数字，就是 n 维向量。三维你还能想象，四维以上就想象不了了 —— **没关系，没人能想象**。你要做的是：**用二维、三维建立直觉，然后相信这些直觉在高维依然成立**（绝大多数时候确实成立）。

这件事为什么重要？因为机器学习干的所有事，本质上就是**把万物变成向量**：

- 一个词 → 4096 个数字（GPT 内部一个 token 的表示）
- 一张 224×224 的彩色图 → 150528 个数字
- 一个用户 → 128 个数字（推荐系统里的用户向量）

一旦变成向量，就能计算「像不像」「离多远」，机器就能处理了。

**两个基本操作：**

加法是对应位置相加，几何上是「首尾相接」：
\`[3,4] + [1,2] = [4,6]\`

数乘是每个数都乘，几何上是「拉伸或翻转」：
\`2 × [3,4] = [6,8]\`（长度变 2 倍，方向不变）
\`-1 × [3,4] = [-3,-4]\`（长度不变，方向掉头）`},

{h:"2. 点积：衡量「方向有多一致」",
b:`这是整门课最重要的一个运算，因为**注意力机制就是它**。

两个向量的点积（dot product）定义为对应位置相乘再求和：

$$a · b = a₁b₁ + a₂b₂ + … + aₙbₙ$$

比如 \`[3,4] · [1,2] = 3×1 + 4×2 = 11\`。

注意：点积的结果是**一个数**，不是向量。

**关键的几何意义**，这个公式你要记住：

$$a · b = ‖a‖ · ‖b‖ · cos θ$$

θ 是两个向量之间的夹角。由此得到三种情况：

- 方向相同（θ=0，cos=1）→ 点积**最大**
- 互相垂直（θ=90°，cos=0）→ 点积 **等于 0**
- 方向相反（θ=180°，cos=-1）→ 点积**最负**

所以：**点积大 = 两个向量指的方向像**。

> **这就是语义检索的全部原理。** 把「小狗」和「幼犬」都变成向量，它们的点积会很大；「小狗」和「财务报表」的点积会很小。向量数据库做的事情就是：给你一个查询向量，从几百万个向量里找出点积最大的那几个。

如果先把两个向量都缩放到长度为 1（叫「归一化」），那么点积就直接等于 cos θ，取值在 -1 到 1 之间 —— 这叫**余弦相似度**，是 embedding 检索的标准度量。

**在 Transformer 里**：注意力机制要判断「第 5 个词应该关注第 2 个词多少」，做法就是把第 5 个词的 query 向量和第 2 个词的 key 向量做点积。点积越大，关注度越高。就这么简单。`},

{h:"3. 矩阵：数字表格，更是一次「变换」",
b:`矩阵就是一个数字的长方形表格。一个 m×n 的矩阵有 m 行 n 列：

\`\`\`
A = [ 2  0 ]     这是一个 2×2 矩阵
    [ 0  3 ]
\`\`\`

**第一种看法**：它就是一张表。神经网络某一层的权重就是这么一张表。

**第二种看法（更重要）**：矩阵是一台**变换机器**。你喂给它一个向量，它吐出另一个向量。

拿上面的 A 试试。矩阵乘向量的算法是「A 的每一行分别和 v 做点积」：

\`\`\`
A · [1]  =  [ 2×1 + 0×2 ]  =  [ 2 ]
    [2]     [ 0×1 + 3×2 ]     [ 6 ]
\`\`\`

输入 (1,2)，输出 (2,6)。再试几个点你会发现：这个矩阵把所有点的横坐标拉伸 2 倍、纵坐标拉伸 3 倍。它是一台「拉伸机」。

> **一个能让你秒懂矩阵的技巧：看它把基向量送到哪里。**
> 基向量就是 \`e₁=[1,0]\` 和 \`e₂=[0,1]\`。
> 你会发现 \`A·e₁\` 恰好等于 A 的第一列，\`A·e₂\` 恰好等于 A 的第二列。
> 所以：**矩阵的每一列，就是对应的基向量变换后落到的位置**。知道基向量去哪了，整个变换就确定了。

再看一个：

\`\`\`
R = [ 0  -1 ]
    [ 1   0 ]
\`\`\`

第一列是 (0,1)，说明 e₁=[1,0] 被送到了 [0,1]；第二列是 (-1,0)，说明 e₂=[0,1] 被送到了 [-1,0]。两个基向量都逆时针转了 90°。所以 **R 是一台旋转机**。

**神经网络的一层是什么？** 就是「一次矩阵变换 + 一次非线性挤压」：

\`\`\`
h = 激活函数(W · x + b)
\`\`\`

W 是矩阵（变换），b 是偏置（平移），激活函数（如 ReLU）提供非线性。深度网络就是把这个过程叠几十层。**如果没有激活函数，多层矩阵相乘等价于一个矩阵，叠多少层都白搭** —— 这是为什么非线性是必须的。`},

{h:"4. 矩阵乘法：算法、形状规则、以及为什么这样定义",
b:`两个矩阵相乘 \`C = A · B\`，规则是：

$$C[i][j] = A 的第 i 行 · B 的第 j 列（做点积）$$

手算一个：

\`\`\`
[ 1  2 ]   [ 5  6 ]     [ 1×5+2×7   1×6+2×8 ]     [ 19  22 ]
[ 3  4 ] · [ 7  8 ]  =  [ 3×5+4×7   3×6+4×8 ]  =  [ 43  50 ]
\`\`\`

**形状规则（务必背下来）**：

$$(m × n) · (n × p) = (m × p)$$

**中间那两个数字必须相等**，否则乘不了。结果的形状是「外面那两个数字」。

> 你未来 90% 的深度学习报错会是形状不匹配。像 \`mat1 and mat2 shapes cannot be multiplied (32x512 and 768x256)\` 这种错误，看一眼就知道是 512 和 768 对不上。养成随时在脑子里跟踪张量形状的习惯，能省掉你一半的调试时间。

**为什么矩阵乘法要这么别扭地定义？**

因为它代表**变换的复合**。如果 B 是「旋转」，A 是「拉伸」，那么 \`A·B\` 这个新矩阵代表「先旋转，再拉伸」。这个定义是为了让「先做 B 再做 A」恰好等于「一次性做 A·B」。

注意顺序：\`A·B\` 是**先 B 后 A**（从右往左读，跟函数嵌套 f(g(x)) 一样）。

**矩阵乘法不满足交换律**：一般来说 \`A·B ≠ B·A\`。这很好理解 —— 「先旋转再拉伸」和「先拉伸再旋转」结果确实不一样。

**几个必知的特殊矩阵：**

- **转置** \`Aᵀ\`：行列互换。\`(m×n)\` 转置后变 \`(n×m)\`。注意力公式里的 \`QKᵀ\` 就是为了让形状能乘起来。
- **单位矩阵** \`I\`：对角线全 1，其余全 0。它是「什么都不做」的变换，\`A·I = A\`。相当于数字里的 1。
- **逆矩阵** \`A⁻¹\`：能把 A 的变换撤销回去，\`A·A⁻¹ = I\`。不是所有矩阵都有逆（比如把三维压扁成平面的变换，信息丢了就回不去了）。
  实践中**几乎从不真的去求逆矩阵** —— n×n 矩阵求逆要 O(n³) 的计算量，模型里的矩阵动辄 4096×4096，成本无法接受。这也是为什么深度学习用梯度下降而不是解析求解。`},

{h:"5. 秩：这个矩阵真正含有多少信息",
b:`先看一组向量：\`[1,2]\`、\`[2,4]\`、\`[3,6]\`。

它们看起来是三个向量，但其实**全都在同一条直线上** —— 后两个只是第一个的倍数。它们提供的「方向信息」只有一个。这叫**线性相关**。

**秩（rank）= 一个矩阵里真正独立的行（或列）有几个。**

举例：

\`\`\`
A = [ 1  2 ]      第二行 = 第一行 × 2，所以 rank(A) = 1
    [ 2  4 ]

B = [ 1  2 ]      两行不成比例，谁也代表不了谁，rank(B) = 2
    [ 3  1 ]
\`\`\`

**几何直觉**：秩 = 变换之后，空间被压缩到了几维。
- rank=2 的 2×2 矩阵：平面还是平面，没丢维度（这种叫「满秩」，有逆矩阵）。
- rank=1 的 2×2 矩阵：整个平面被压扁成一条线。信息丢失了，回不去了，没有逆矩阵。

**为什么这个概念在 LLM 里极其重要？**

想象一个 4096×4096 的权重矩阵，它有 1677 万个数字。但如果它的秩只有 8，意味着这 1677 万个数字里**真正独立的信息只有 8 个方向**，剩下的全是这 8 个方向的线性组合 —— 大量冗余。

而研究发现：**微调时模型权重的变化量 ΔW，本征秩非常低**。也就是说，让模型学会一个新任务，并不需要在 1677 万个数字上各改一点，只需要在少数几个方向上动一动。

这就是 **LoRA** 的全部思想。下一节我们把这笔账算出来。`},

{h:"6. 特征值、特征向量、SVD",
b:`**特征向量**：对某个矩阵 A 来说，绝大多数向量被它变换后会改变方向。但有一些特殊的向量，变换后**方向不变，只是被拉长或缩短**：

$$A · v = λ · v$$

这个 v 叫特征向量，λ（读作 lambda）叫对应的特征值。

用前面那台「拉伸机」\`A = [[2,0],[0,3]]\` 举例：向量 [1,0] 被变换成 [2,0]，方向没变、长度乘了 2。所以 [1,0] 是特征向量，特征值是 2。同理 [0,1] 是特征向量，特征值 3。

**直觉**：特征向量是这个变换的「骨架方向」，特征值告诉你在这个方向上放大了多少倍。

**SVD（奇异值分解）—— 线性代数的终极工具**

特征分解只对方阵好使。SVD 更强：**任何矩阵**（长的、扁的、方的）都可以拆成三个矩阵相乘：

$$A = U · Σ · Vᵀ$$

几何意义非常干净：**任何线性变换，都可以拆解成「旋转 → 沿坐标轴缩放 → 再旋转」这三步**。

- \`V ᵀ\`：第一次旋转
- \`Σ\`：一个对角矩阵，对角线上的数叫**奇异值**，按从大到小排列。它们就是各个方向的缩放倍数。
- \`U\`：第二次旋转

**最有用的性质：低秩近似。**

奇异值是从大到小排的，通常前几个特别大、后面的迅速衰减到接近 0。如果你只保留前 k 个奇异值，把后面的全扔掉，得到的矩阵 \`Aₖ\` 是**所有秩为 k 的矩阵里最接近 A 的那一个**（这叫 Eckart–Young 定理）。

一个直观例子：一张 1000×1000 的灰度图是个矩阵。做 SVD 后只保留前 50 个奇异值，图片看起来几乎没变，但存储量从 100 万个数降到了 \`50×(1000+1000) = 10万\` 个数 —— 压缩了 10 倍。这就是图像压缩的原理之一。

**三个概念串起来：**
- **PCA**（主成分分析）：对数据矩阵做 SVD，最大的那几个奇异值方向就是数据变化最大的方向，用它们降维。
- **LoRA**：假设 ΔW 是低秩的，于是不存完整的 ΔW，只存两个瘦长矩阵 B 和 A，用 \`BA\` 去近似它。
- 两者都是「**低秩结构无处不在**」这一个洞察的不同用法。`},

{h:"7. 算一笔账：LoRA 到底省了多少",
b:`现在你有足够的工具来理解 LoRA 了。

设原权重矩阵 \`W\` 的形状是 \`d × k\`。微调的本质是学一个变化量 ΔW，让新权重变成 \`W + ΔW\`。ΔW 的形状当然也是 \`d × k\`。

**全参数微调**：要学 \`d × k\` 个参数。

**LoRA**：假设 ΔW 的秩很低，于是把它写成两个矩阵的乘积：

$$ΔW ≈ B · A$$

其中 \`B\` 的形状是 \`d × r\`，\`A\` 的形状是 \`r × k\`，r 是一个很小的数（比如 8、16、32）。

先验证形状对不对：\`(d×r) · (r×k) = (d×k)\` ✓ 正好。

**参数量对比**（以 d = k = 4096，r = 8 为例）：

| | 参数量 | 数值 |
|---|---|---|
| 全参数 | d × k | 16,777,216 |
| LoRA | r × (d + k) | 65,536 |
| 比例 | | **0.39%** |

只需要训练原来 0.4% 的参数。而且真正省的大头还不止于此 —— 优化器 Adam 要为每个可训练参数额外存两份状态（动量 m 和二阶矩 v），都是 FP32。所以显存的节省比参数量的节省还要夸张。

> **两个实现细节，面试常问：**
> 1. \`B\` 初始化为**全 0**，\`A\` 用随机高斯初始化。这样训练刚开始时 \`BA = 0\`，模型输出和原模型**完全一致**，不会一上来就把预训练能力搞坏。
> 2. 训练完可以把 \`W' = W + BA\` 直接合并回去，推理时**零额外延迟**。这是 LoRA 相比其他微调方法的巨大优势。

到这里，线性代数够你走很远了。下面做题 —— **一定要动笔算，不要用计算器，更不要跳过**。这些题的计算量都很小，考的是概念有没有真的进脑子。`}
],
quiz: [
{id:"m1q1", type:"num", q:"计算点积：[2, -1, 3] · [4, 5, 1] = ?", hint:"对应位置相乘，然后把三个结果加起来。注意第二项有负号。", ans:6,
why:"2×4 + (-1)×5 + 3×1 = 8 - 5 + 3 = 6。\n\n点积结果是**一个数**，不是向量 —— 这一点初学最容易搞混。"},

{id:"m1q2", type:"mc", q:"两个非零向量的点积等于 0，说明它们的几何关系是：",
choices:["方向相同","互相垂直","方向相反","其中一个长度为 0"], ans:1,
hint:"回忆 a·b = ‖a‖‖b‖cos θ。两个向量都非零，那么要让整个式子等于 0，只能是哪个因子等于 0？",
why:"因为 a·b = ‖a‖‖b‖cos θ，两个向量都非零意味着 ‖a‖ 和 ‖b‖ 都不为 0，所以只能是 **cos θ = 0**，即 θ = 90°，互相垂直。\n\n在 embedding 空间里，点积接近 0 意味着两个概念「毫不相干」—— 既不相似，也不相反。"},

{id:"m1q3", type:"text", q:"一个形状为 (32, 128) 的矩阵，乘以一个形状为 (128, 512) 的矩阵，结果的形状是？（按 32,128 这样的格式填写两个数字）",
ans:["32,512","(32,512)","32 512","32*512","32x512","32×512"],
hint:"规则是 (m×n)·(n×p) = (m×p)。中间的两个数字必须相等（这里都是 128，能乘），结果取外面的两个数字。",
why:"(32×128) · (128×512) = **(32, 512)**。\n\n实际场景里这通常表示：一个 batch 里 32 个样本，每个样本是 128 维，经过一个把 128 维映射到 512 维的线性层后，变成 32 个 512 维的向量。\n\n把形状推导练成条件反射，你会少掉一半的 debug 时间。"},

{id:"m1q4", type:"num", q:"手算矩阵乘法 [[1,2],[3,4]] · [[0,1],[1,0]]，结果矩阵左上角那个元素（第 1 行第 1 列）是多少？",
hint:"结果的 [1][1] 位置 = 左边矩阵的第 1 行 与 右边矩阵的第 1 列 做点积。左边第一行是 [1,2]，右边第一列是 [0,1]。", ans:2,
why:"[1,2] · [0,1] = 1×0 + 2×1 = **2**。\n\n完整结果是 [[2,1],[4,3]]。顺带一提，右边那个矩阵 [[0,1],[1,0]] 的作用是「交换两列」—— 你可以观察到结果正是原矩阵的两列对调了。\n\n这再次印证了那个视角：**矩阵是一台变换机器**，看它对基向量做了什么就知道它在干嘛。"},

{id:"m1q5", type:"num", q:"矩阵 [[1,2,3],[2,4,6],[1,1,1]] 的秩是多少？",
hint:"逐行看：第二行和第一行有什么关系？第三行能不能由前面的行组合出来？数一数「真正独立」的行有几个。", ans:2,
why:"**答案是 2。**\n\n第二行 = 第一行 × 2，所以这两行只贡献 1 个独立方向。第三行 [1,1,1] 不是第一行的倍数，是一个新方向。\n\n所以独立的行只有 2 个 → rank = 2。\n\n虽然这是个 3×3 矩阵，看起来有 9 个数，但它把三维空间压扁成了一个**平面**。信息丢失了，因此它没有逆矩阵。"},

{id:"m1q6", type:"num", q:"权重矩阵形状是 1024×1024，用 LoRA 微调，秩 r=16。LoRA 需要训练的参数量是多少？（填数字，不要千位分隔符）",
hint:"LoRA 用两个矩阵 B(d×r) 和 A(r×k) 代替 ΔW(d×k)。分别算出两个矩阵各有多少参数，再相加。d=k=1024，r=16。", ans:32768, tol:0,
why:"B 是 1024×16 = 16384 个参数，A 是 16×1024 = 16384 个参数，合计 **32768**。\n\n公式：\`r × (d + k) = 16 × 2048 = 32768\`。\n\n对比全参数微调的 1024×1024 = 1,048,576，LoRA 只需要 **3.1%**。\n\n再想一层：如果 r 增加到 32，参数量翻倍到 65536，但仍然只有全参的 6.25%。这就是为什么 r 从 8 调到 64 的代价其实很小 —— 值得做消融实验找最优值。"},

{id:"m1q7", type:"mc", q:"关于 SVD 的低秩近似，下面哪个说法是**错的**？",
choices:[
"任何矩阵都可以做 SVD，不要求是方阵",
"奇异值通常按从大到小排列，保留前 k 个就得到最优的秩-k 近似",
"保留的奇异值越少，近似误差越小",
"PCA 本质上就是对中心化后的数据矩阵做 SVD"], ans:2,
hint:"想想图像压缩：只保留 5 个奇异值和保留 200 个奇异值，哪个还原出来的图更接近原图？",
why:"**第三个说法是错的**，正好说反了。保留的奇异值**越多**，近似越精确、误差越小；保留得越少，压缩率越高但失真越大。\n\n这是一个典型的权衡：在 LoRA 里，r 越大表达能力越强但参数越多，r 越小越省但可能学不动复杂任务。\n\n其余三个说法都对。特别注意第一条 —— SVD 相比特征分解的最大优势就是**不要求方阵**，任何形状都能拆。"},

{id:"m1q8", type:"open", q:"用你自己的话解释：为什么说「一个神经网络如果没有激活函数，不管叠多少层，都等价于单层」？（写 2-3 句话）",
hint:"假设两层都是纯矩阵乘法：h = W₁x，y = W₂h。把第一个式子代入第二个，会得到什么？矩阵乘法有结合律吗？",
ref:"因为两层纯线性变换可以合并：\n\n\`y = W₂(W₁x) = (W₂W₁)x = Wx\`\n\n由于矩阵乘法满足结合律，W₂ 和 W₁ 可以先乘起来得到一个新矩阵 W。所以「两层线性层」和「一层权重为 W₂W₁ 的线性层」在数学上完全等价，叠多少层都一样。\n\n换个说法：**线性函数的复合仍然是线性函数**，永远画不出曲线。而现实世界的规律绝大多数是非线性的。\n\n激活函数（ReLU、GELU 等）的作用就是在层与层之间插入非线性，破坏这种可合并性，让网络的表达能力随深度真正增长。\n\n**扩展一句**：ReLU 看起来只是「把负数变成 0」这么简单的操作，但足以让网络逼近任意连续函数（通用逼近定理）—— 这是深度学习能 work 的理论基础之一。"}
]
};

window.LESSONS.s0n2 = {
title: "概率与统计：从抛硬币到交叉熵",
sub: "所有的损失函数都是从概率视角推出来的。这一课结束时，你会明白训练模型为什么本质上是在做「极大似然估计」。",
est: "读 90 分钟 + 做题 45 分钟",
sections: [
{h:"1. 概率的三条地基",
b:`概率就是「某件事发生的可能性」，取值在 0 到 1 之间。三个必须牢固的概念：

**① 联合概率 P(A, B)**：A 和 B 同时发生的概率。

**② 条件概率 P(A|B)**：**在 B 已经发生的前提下**，A 发生的概率。竖线读作「在…条件下」。

$$P(A|B) = P(A, B) / P(B)$$

理解方式：本来样本空间是全世界，现在已知 B 发生了，样本空间**缩小**到了 B 的范围内。所以要用「A 和 B 都发生」的部分，除以「B 的全部」。

举例：随机抽一个人，是程序员的概率 P(程序员) 可能是 0.5%。但如果已知这个人在参加 PyCon 大会，那么 P(程序员 | 在 PyCon) 可能高达 90%。**条件改变了概率**。

**③ 独立**：如果 \`P(A|B) = P(A)\`，说明 B 发不发生对 A 毫无影响，两者独立。此时 \`P(A,B) = P(A)·P(B)\`。

> **为什么这对 LLM 很重要？** 语言模型干的事情就是算条件概率：
> \`P(下一个词 | 前面所有的词)\`
> 输入「今天天气真」，模型输出一个覆盖全词表的概率分布，可能「好」的概率是 0.6，「热」是 0.2，「差」是 0.1……
> **整个 GPT 就是一个巨大的条件概率计算器。** 记住这句话，很多事情会豁然开朗。`},

{h:"2. 贝叶斯公式：用证据更新信念",
b:`把条件概率的定义写两遍（一遍 A|B，一遍 B|A），联立消掉联合概率，就得到贝叶斯公式：

$$P(A|B) = P(B|A) · P(A) / P(B)$$

四个部分各有名字：
- \`P(A)\` —— **先验**：看到证据之前，你对 A 的信念
- \`P(A|B)\` —— **后验**：看到证据 B 之后，更新过的信念
- \`P(B|A)\` —— **似然**：如果 A 为真，看到证据 B 的可能性
- \`P(B)\` —— 归一化项

**一个能改变你直觉的经典例子（务必跟着算一遍）：**

某种癌症在人群中的发病率是 **0.1%**。有一种检测方法：
- 如果真的患病，99% 的概率会检测出阳性（真阳性率高）
- 如果没患病，也有 5% 的概率误报为阳性（假阳性率）

现在你体检查出阳性。**请先凭直觉猜一下，你真的患病的概率是多少？**

多数人会猜 90% 以上。真实答案是 **不到 2%**。

算一下。假设有 10 万人：
- 真患病的：100 人。其中检出阳性的：100 × 99% = **99 人**
- 没患病的：99900 人。其中误报阳性的：99900 × 5% = **4995 人**
- 阳性总人数：99 + 4995 = 5094 人
- 其中真患病的比例：99 / 5094 ≈ **1.9%**

**为什么反直觉？** 因为健康人的基数太大了。哪怕误报率只有 5%，5% 的 99900 人也远远多于患病的 100 人。

> 这个现象叫**基础率谬误（base rate fallacy）**，它在机器学习里天天发生：
> 一个「准确率 99%」的欺诈检测模型，在欺诈只占 0.1% 的真实流量里，报出来的警报可能 95% 都是误报。
> 这就是为什么**类别极度不平衡时不能看准确率**，必须看精确率、召回率、PR 曲线。先验概率（基础率）永远不能忽略。`},

{h:"3. 随机变量、期望、方差",
b:`**随机变量**：一个结果不确定的量，比如「掷一次骰子的点数」X。

**期望 E[X]**：长期平均下来会是多少。算法是「每个可能取值 × 它的概率，然后全部加起来」：

$$E[X] = Σ xᵢ · P(xᵢ)$$

骰子的期望：\`(1+2+3+4+5+6)/6 = 3.5\`。注意 3.5 这个值骰子根本掷不出来 —— **期望是长期平均值，不是「最可能出现的值」**。

**方差 Var[X]**：数据有多分散。定义是「偏离期望的平方的期望」：

$$Var[X] = E[(X - E[X])²]$$

平方是为了让正负偏差都变成正贡献，不会互相抵消。**标准差**就是方差开根号，好处是单位和原数据一致。

**两条要背的性质：**

$$E[aX + b] = a·E[X] + b$$
$$Var[aX + b] = a² · Var[X]$$

注意方差里的**平方**，以及常数 b 不影响方差（整体平移不改变分散程度）。

**这在深度学习里的直接应用：**

参数初始化的核心问题是：信号在层层传递中，方差会不会爆炸或消失？如果每层都把方差放大 2 倍，传 30 层就是 2³⁰ 倍 —— 数值直接溢出。**He 初始化、Xavier 初始化的整个推导，就是在解「让每层输出的方差保持为 1」这个方程。**

另一个应用，就是那道高频面试题：注意力为什么要除以 √d_k？

假设 q 和 k 的每个分量都是独立的、均值 0 方差 1 的随机数。它们的点积是 d_k 个乘积项之和，每项方差为 1，独立相加**方差可加**，所以点积的方差是 **d_k**，标准差是 **√d_k**。

d_k = 128 时标准差约 11.3 —— 点积动辄跑到 ±30。这么大的数进 softmax，会被压成几乎 one-hot 的分布，**梯度趋近于 0，网络没法学**。除以 √d_k 恰好把方差拉回 1。`},

{h:"4. 三个必须认识的分布",
b:`**① 伯努利分布**：只有两个结果（成功/失败），成功概率为 p。

期望 = p，方差 = p(1-p)。

注意方差在 p=0.5 时最大（0.25），在 p 接近 0 或 1 时趋近 0 —— **最不确定的时候方差最大**，这很符合直觉。

二分类问题输出的就是伯努利分布。

**② 正态分布（高斯分布）**：那条著名的钟形曲线，由均值 μ 和方差 σ² 决定。

为什么它无处不在？因为**中心极限定理**：大量独立随机因素叠加起来，不管每个因素本身服从什么分布，它们的和都会趋近正态分布。身高、测量误差、模型权重…… 都是众多微小因素的叠加，所以都近似正态。

深度学习里：权重初始化用它，噪声建模用它，QLoRA 的 NF4 量化更是直接基于「预训练权重近似正态分布」这个观察来设计最优量化区间的。

**③ 分类分布（Categorical）**：多个互斥结果，每个有自己的概率，全部加起来等于 1。

**这就是 LLM 每一步输出的东西** —— 一个覆盖整个词表（比如 15 万个 token）的概率分布。softmax 函数的作用就是把模型输出的一堆任意实数（logits）转换成一个合法的分类分布：

$$softmax(zᵢ) = e^{zᵢ} / Σⱼ e^{zⱼ}$$

指数保证全为正，除以总和保证加起来为 1。`},

{h:"5. 极大似然估计：训练模型到底在干什么",
b:`这是整课最重要的一节。

**问题**：你有一枚不知道是否均匀的硬币，抛了 10 次，出现 7 次正面。你猜正面概率 p 是多少？

几乎所有人脱口而出：0.7。**但为什么是 0.7？** 把这个「为什么」讲清楚，你就理解了模型训练的本质。

**思路**：对于任意一个候选的 p，我们可以算出「在这个 p 之下，观察到 7 正 3 反的概率是多少」。这个概率叫**似然（likelihood）**：

$$L(p) = p⁷ · (1-p)³$$

然后我们挑那个让似然**最大**的 p。这就叫**极大似然估计（MLE）**：

> **哪个参数值，最能解释我实际观察到的数据，就选哪个。**

代入几个值试试：
- p = 0.5 → 0.5⁷ × 0.5³ = 0.000977
- p = 0.7 → 0.7⁷ × 0.3³ = 0.00222 ← 最大
- p = 0.9 → 0.9⁷ × 0.1³ = 0.000478

确实 0.7 胜出。用微积分可以严格证明最优解就是 7/10。

**从 MLE 到交叉熵 —— 关键的一步推导：**

似然是一堆概率连乘。概率都是小于 1 的数，几千个连乘会直接下溢成 0（浮点数存不下）。解决办法是**取对数**：连乘变连加，且对数是单调递增函数，不改变最优解的位置。

$$log L = Σ log P(每个样本)$$

优化领域习惯最小化而不是最大化，所以取负号：

$$负对数似然 = -Σ log P(每个样本)$$

**这个式子，就是交叉熵损失。**

所以：**训练神经网络 = 最小化交叉熵 = 最大化似然 = 寻找最能解释训练数据的那组参数。**

**具体到 LLM**：训练语料里有一句「今天天气真好」。当模型看到「今天天气真」时，它输出一个概率分布。假设它给「好」这个词的概率是 0.3，那么这一个 token 的损失就是 \`-log(0.3) = 1.20\`。

- 如果模型很自信且正确，P(好)=0.9 → loss = -log(0.9) = **0.105**（很小）
- 如果模型完全没料到，P(好)=0.01 → loss = -log(0.01) = **4.6**（很大）

训练就是不断调整参数，让正确答案的概率变高。就这么简单。

**困惑度（Perplexity）**：把平均交叉熵取指数，\`PPL = e^{平均loss}\`。它的含义是「模型在每个位置平均纠结于多少个候选词」。PPL=10 表示模型的困惑程度相当于在 10 个词里瞎猜。这是评价语言模型最常用的指标。`},

{h:"6. MLE、MAP 与正则化的关系",
b:`MLE 只看数据。但如果数据很少呢？

抛 3 次硬币全是正面，MLE 会告诉你 p = 1.0（这枚硬币永远不会出反面）。这显然太激进了 —— 你**事先就知道**硬币大概率是接近均匀的。

这个「事先就知道」的信息叫**先验**。把它加进来，最大化的就不是 \`P(数据|参数)\` 而是 \`P(参数|数据)\`，由贝叶斯公式：

$$P(θ|D) ∝ P(D|θ) · P(θ)$$

这叫**最大后验估计（MAP）**。

**关键结论（面试高频）：**

> **MAP 等价于带正则化的 MLE。**
> - 假设参数服从**高斯先验** → 推导出来就是 **L2 正则**（权重衰减）
> - 假设参数服从**拉普拉斯先验** → 推导出来就是 **L1 正则**（产生稀疏解）

所以「加正则项防止过拟合」这个工程手段，在数学上就是「我事先相信参数不应该太大」这个信念。两种语言在描述同一件事。

**数据量的影响**：数据越多，似然项的影响越大，先验被逐渐「淹没」，MAP 收敛到 MLE。这解释了为什么：
- 小数据集上正则化、预训练模型至关重要
- 大规模预训练时 dropout 和权重衰减的作用大幅下降（LLaMA 预训练干脆把 dropout 设为 0）

**最后补两个交叉熵相关的概念：**

**KL 散度** \`KL(p‖q)\` 衡量两个分布的差异，等于 \`交叉熵 H(p,q) - 熵 H(p)\`。训练时真实分布 p 是 one-hot，熵 H(p)=0，所以**最小化交叉熵就等于最小化 KL 散度**。

KL 散度**不对称**：\`KL(p‖q) ≠ KL(q‖p)\`。这不是数学瑕疵，而是有实际后果的 —— RLHF 里用 KL 惩罚约束模型别跑太偏，用哪个方向会导致不同的行为（一个倾向覆盖所有模式，一个倾向塌缩到单个模式）。`}
],
quiz: [
{id:"m2q1", type:"num", q:"某工厂 A 车间生产 60% 的零件，次品率 2%；B 车间生产 40%，次品率 5%。随机抽一个零件，它是次品的概率是多少？（填百分数的数字部分，如 3.5 表示 3.5%）",
hint:"全概率公式：分两条路径（来自A且是次品、来自B且是次品），各自算出概率再相加。0.6×0.02 + 0.4×0.05。", ans:3.2, tol:0.05,
why:"P(次品) = 0.6×0.02 + 0.4×0.05 = 0.012 + 0.020 = 0.032 = **3.2%**\n\n这叫**全概率公式**：把一个复杂事件按互斥的情况拆开，各自算完再加总。\n\n它是贝叶斯公式分母的来源 —— 下一题你就会用到。"},

{id:"m2q2", type:"num", q:"接上题：抽到的零件确实是次品，那么它来自 B 车间的概率是多少？（填百分数的数字部分，保留一位小数）",
hint:"贝叶斯：P(B|次品) = P(次品|B)·P(B) / P(次品)。分子是「来自B且是次品」= 0.4×0.05，分母用上一题的答案 0.032。", ans:62.5, tol:0.3,
why:"P(B|次品) = (0.4 × 0.05) / 0.032 = 0.020 / 0.032 = **62.5%**\n\n注意这里的思维转换：B 车间只生产 40% 的零件，但在**所有次品中**它占了 62.5%。因为它的次品率是 A 的 2.5 倍。\n\n这正是贝叶斯的价值 —— **用观察到的证据（是次品）反推原因（来自哪个车间）**。\n\n机器学习里的分类问题本质上就是这个操作：观察到特征，反推类别。"},

{id:"m2q3", type:"num", q:"随机变量 X 的期望是 5，方差是 4。那么 Y = 3X + 2 的方差是多少？",
hint:"两条性质：E[aX+b] = aE[X]+b，但 Var[aX+b] = a²·Var[X]。注意方差里系数要平方，而常数项 b 完全不影响方差。", ans:36,
why:"Var[Y] = 3² × 4 = **36**。\n\n两个关键点：\n1. **系数要平方**（因为方差的定义里带平方）\n2. **常数 +2 不影响方差** —— 把整组数据平移，分散程度不变\n\n顺带：E[Y] = 3×5 + 2 = 17。\n\n这条性质直接解释了注意力为什么除以 √d_k：点积方差是 d_k，要让方差变回 1，得除以 √d_k（因为除以 √d_k 会让方差除以 d_k）。"},

{id:"m2q4", type:"num", q:"点积缩放：假设 q 和 k 的每个分量都独立、均值 0、方差 1，向量维度 d_k = 64。那么点积 q·k 的标准差是多少？",
hint:"点积是 64 个乘积项之和。每一项 qᵢkᵢ 的方差是 1，独立随机变量之和的方差等于各自方差之和。先求方差，再开根号得标准差。", ans:8,
why:"点积 = 64 项之和，每项方差为 1，独立相加**方差可加** → 总方差 = 64。\n\n标准差 = √64 = **8**。\n\n也就是说未缩放的点积典型取值会在 ±8 到 ±16 这个范围。这些数进 softmax，e⁸ 和 e⁻⁸ 相差近 900 万倍 —— 分布几乎变成 one-hot，**梯度消失**。\n\n除以 √d_k = 8 之后，方差回到 1，softmax 工作在敏感区间。\n\n**这道题的推导过程就是面试答案本身**，能把方差推出来的候选人和只会说「为了数值稳定」的，完全是两个档次。"},

{id:"m2q5", type:"mc", q:"一个语言模型在某个位置给正确答案词的概率是 0.05，这一个 token 的交叉熵损失约等于多少？（ln 0.05 ≈ -3.0）",
choices:["0.05","-3.0","3.0","0.95"], ans:2,
hint:"交叉熵损失 = -log(正确答案的概率)。注意前面那个负号 —— 损失必须是正数。",
why:"loss = -log(0.05) = -(-3.0) = **3.0**\n\n注意那个负号：概率小于 1，log 出来是负数，加负号变正。**损失永远是非负的**，概率越接近 1 损失越接近 0。\n\n对照感受一下量级：\n- P=0.9 → loss 0.105\n- P=0.5 → loss 0.693\n- P=0.05 → loss 3.0\n\n顺带算困惑度：如果整个语料的平均 loss 就是 3.0，那么 PPL = e³ ≈ 20，意味着模型平均在 20 个词里纠结。\n\n现代 LLM 在通用语料上的 PPL 通常在 3~10 之间。"},

{id:"m2q6", type:"mc", q:"以下关于 MLE 和 MAP 的说法，哪个是**正确**的？",
choices:[
"MAP 一定比 MLE 准确，因为它用了更多信息",
"假设参数服从高斯先验时，MAP 等价于带 L1 正则的 MLE",
"数据量趋于无穷时，MAP 会收敛到 MLE",
"MLE 考虑先验分布，MAP 不考虑"], ans:2,
hint:"想想抛硬币的例子：数据越多，你事先那点「硬币应该是均匀的」的信念，还剩多少影响力？",
why:"**正确答案是第三个。** 数据量增大时，似然项 P(D|θ) 主导一切，先验被淹没，MAP → MLE。\n\n其余错在哪：\n- 第一个：如果先验假设本身是错的，MAP 反而更糟。先验是双刃剑。\n- 第二个：**高斯先验对应 L2 正则**，拉普拉斯先验才对应 L1。这个对应关系要记牢。\n- 第四个：说反了，**MAP 才是考虑先验的那个**。\n\n实践推论：小数据必须靠正则和预训练；大规模预训练时正则化作用很小（LLaMA 预训练 dropout 直接设为 0）。"},

{id:"m2q7", type:"open", q:"一个信用卡欺诈检测模型，在测试集上准确率高达 99.8%，团队很兴奋想上线。但真实流量中欺诈交易只占 0.1%。请指出这个指标的问题，并说明你会改看什么指标、为什么。",
hint:"如果模型偷懒，对所有交易都预测「不是欺诈」，它的准确率是多少？再想想：这个业务真正怕的是漏掉欺诈，还是误报正常交易？",
ref:"**问题：准确率在极度不平衡的数据上毫无信息量。**\n\n一个把所有交易都判为「正常」的模型，什么都不用学，准确率就有 **99.9%** —— 比这个「兴奋」的 99.8% 还高。所以 99.8% 很可能意味着模型表现比「全猜正常」还差。\n\n这是**基础率谬误**在指标选择上的体现：多数类的基数压倒一切。\n\n**应该看的指标：**\n\n1. **召回率（Recall）** —— 真实的欺诈里抓到了多少。这是业务最关心的，漏掉一笔欺诈的代价远大于误报。\n2. **精确率（Precision）** —— 报警里有多少是真的。太低会让风控团队被误报淹没。\n3. **PR-AUC**（而不是 ROC-AUC）—— 在极度不平衡时，ROC-AUC 会因为真阴性巨大而虚高得好看，PR 曲线更能反映真实可用性。\n4. **固定精确率下的召回率**，比如「Precision=90% 时 Recall 是多少」—— 这个说法最贴近业务决策。\n5. **混淆矩阵**要直接看，别只看汇总数字。\n\n**更进一步（能说出来是加分项）：**\n\n- 不要用默认的 0.5 阈值，应该根据误报和漏报的**实际业务成本**去调整决策阈值。\n- 可以做成**分级策略**：高分直接拦截，中间分数送人工复核，低分放行。\n- 如果不平衡到 1:10⁶ 的程度，应该考虑把问题重构成**异常检测**而不是二分类。\n- 评估时要用**时间切分**而非随机切分，因为欺诈手法会随时间演变。"}
]
};

window.LESSONS.s0n3 = {
title: "微积分与优化：梯度下降到底在做什么",
sub: "反向传播就是链式法则。这一课你会亲手算一次梯度下降，从此「模型在学习」不再是一句玄话。",
est: "读 80 分钟 + 做题 45 分钟",
sections: [
{h:"1. 导数：变化率",
b:`导数回答一个问题：**x 动一点点，y 会跟着动多少？**

$$f'(x) = 当 x 增加极小量时，f(x) 增加量与之的比值$$

几何上就是函数曲线在那一点的**切线斜率**。

- \`f'(x) > 0\`：往右走，函数上升
- \`f'(x) < 0\`：往右走，函数下降
- \`f'(x) = 0\`：此处平坦（极大、极小，或鞍点）

**要熟练的几条规则**（这些够用了，不用背更多）：

| 函数 | 导数 |
|---|---|
| \`c\`（常数） | \`0\` |
| \`xⁿ\` | \`n·xⁿ⁻¹\` |
| \`eˣ\` | \`eˣ\` |
| \`ln x\` | \`1/x\` |
| \`f + g\` | \`f' + g'\` |
| \`f · g\` | \`f'g + fg'\` |

其中 \`xⁿ → n·xⁿ⁻¹\` 用得最多，务必熟练。比如 \`f(x) = x²\` 的导数是 \`2x\`，\`f(x) = 3x²\` 的导数是 \`6x\`。

**为什么深度学习离不开导数？**

训练模型 = 找一组参数让损失最小。损失是参数的函数。想知道「参数往哪个方向调能让损失变小」，就得知道损失对参数的导数。

**导数指出了上坡方向，所以我们往反方向走 —— 这就是梯度下降。**`},

{h:"2. 偏导数与梯度",
b:`真实的损失函数有几十亿个参数，不是一元函数。

**偏导数** \`∂f/∂x\`：把其他变量**全当成常数**，只对 x 求导。符号 ∂ 读作「偏」。

例：\`f(x,y) = x²y + 3y\`
- \`∂f/∂x = 2xy\`（把 y 当常数，3y 对 x 求导是 0）
- \`∂f/∂y = x² + 3\`（把 x 当常数）

**梯度** \`∇f\`：把所有偏导数打包成一个向量。

$$∇f = [∂f/∂x, ∂f/∂y, …]$$

**梯度最重要的性质：它指向函数上升最快的方向。**

想象你站在一座山上，梯度就是「最陡的上坡方向」，梯度的长度就是「有多陡」。

既然我们要让损失变**小**，就沿着**梯度的反方向**走：

$$θ_新 = θ_旧 - lr × ∇L$$

这一行就是**梯度下降**，是所有深度学习训练的核心。\`lr\` 是学习率，控制每步迈多大。

**学习率的直觉：**
- 太小 → 收敛极慢，可能一辈子训不完
- 太大 → 在山谷两侧来回横跳，甚至发散成 NaN
- 这就是为什么学习率是**最重要的超参数**，没有之一

**为什么要 warmup？** 训练刚开始时参数是随机的，梯度方向噪声极大，而且 Adam 的二阶矩统计量样本太少不可靠。这时候大步走很容易冲进坏区域。所以先用很小的学习率走几百步，等统计量稳定了再升到峰值。`},

{h:"3. 链式法则：反向传播的全部",
b:`这一节是整个深度学习的心脏。请慢读。

**链式法则**处理的是复合函数：y 依赖 u，u 依赖 x，那么 x 变化对 y 的影响是：

$$dy/dx = (dy/du) × (du/dx)$$

**直觉**：齿轮传动。x 转一圈带动 u 转 3 圈，u 转一圈带动 y 转 2 圈，那么 x 转一圈 y 转 6 圈。影响是**相乘**传递的。

**具体算一个**：\`y = (3x + 1)²\`

拆开：设 \`u = 3x + 1\`，则 \`y = u²\`
- \`dy/du = 2u = 2(3x+1)\`
- \`du/dx = 3\`
- \`dy/dx = 2(3x+1) × 3 = 6(3x+1) = 18x + 6\`

验算：展开 \`y = 9x² + 6x + 1\`，求导得 \`18x + 6\` ✓

**神经网络就是一长串复合函数：**

\`\`\`
x → [层1] → h₁ → [层2] → h₂ → [层3] → 输出 → 损失 L
\`\`\`

想知道第 1 层的权重 \`W₁\` 对损失 L 的影响，就把路径上所有的导数乘起来：

$$∂L/∂W₁ = (∂L/∂输出) × (∂输出/∂h₂) × (∂h₂/∂h₁) × (∂h₁/∂W₁)$$

**这就是反向传播**。它并不是什么神秘算法，就是：从损失开始，沿着计算图往回走，把每一段的局部导数乘起来。之所以叫「反向」，是因为要从后往前算才高效（前面的结果可以复用）。

> **梯度消失和梯度爆炸，现在你能秒懂了：**
> 梯度是一长串数字的**连乘**。
> - 每项都是 0.5，传 30 层 → 0.5³⁰ ≈ 0.0000000009 → **梯度消失**，底层网络根本收不到信号
> - 每项都是 1.5，传 30 层 → 1.5³⁰ ≈ 191751 → **梯度爆炸**，参数被巨大更新直接冲飞
>
> **残差连接为什么是神来之笔？** 因为 \`y = x + F(x)\` 求导得到 \`1 + F'(x)\`，那个 **+1** 保证了无论 F'(x) 多小，梯度至少能以 1 的强度直接传回去。等于给梯度修了一条不衰减的高速公路。这是深层网络能训起来的第一功臣。`},

{h:"4. 手算一次完整的反向传播",
b:`这一节请拿出纸笔，跟着算。算完一遍，你对训练的理解会彻底不同。

**一个极简的「网络」**：一个参数 w，一个输入 x，预测 \`ŷ = w·x\`，用平方损失。

$$L = (ŷ - y)² = (wx - y)²$$

**给定**：x = 2，真实值 y = 6，当前 w = 1，学习率 lr = 0.1

**第一步 · 前向传播**
\`\`\`
ŷ = w·x = 1 × 2 = 2
L = (2 - 6)² = 16
\`\`\`
预测 2，真实 6，差得远，损失 16。

**第二步 · 反向传播（求 ∂L/∂w）**

用链式法则，拆成两段：
\`\`\`
∂L/∂ŷ = 2(ŷ - y) = 2(2 - 6) = -8
∂ŷ/∂w = x = 2
∂L/∂w = (-8) × 2 = -16
\`\`\`

**第三步 · 更新参数**
\`\`\`
w_new = w - lr × ∂L/∂w
      = 1 - 0.1 × (-16)
      = 1 + 1.6
      = 2.6
\`\`\`

梯度是负的，说明「增大 w 能减小损失」，所以 w 变大了。方向对了。

**第四步 · 验证损失真的下降了**
\`\`\`
ŷ = 2.6 × 2 = 5.2
L = (5.2 - 6)² = 0.64
\`\`\`

损失从 **16 降到 0.64**。一步之内，模型「学习」了。

真实的训练就是把这个过程重复几百万次，只不过参数从 1 个变成几十亿个，链条从 2 段变成几百段。**原理一模一样。**

> 再迭代一次你会发现 w 逼近 3（因为 3×2=6 正好），损失趋近 0。这就是收敛。`},

{h:"5. 优化器：从 SGD 到 AdamW",
b:`朴素梯度下降有两个毛病：一是在「峡谷」地形（一个方向陡、另一个方向缓）里会反复横跳，二是所有参数共用一个学习率，但有的参数需要大步、有的需要小步。

**动量（Momentum）**：给更新加上「惯性」，累积历史方向。

\`\`\`
v = β·v + (1-β)·g        # 梯度的滑动平均，β 通常 0.9
θ = θ - lr·v
\`\`\`

像一个滚下山的球：在一致的方向上越滚越快，在来回震荡的方向上正负抵消。**加速收敛、抑制震荡**。

**Adam**：同时维护梯度的一阶矩（动量 m）和二阶矩（梯度平方的滑动平均 v）。

\`\`\`
m = β₁·m + (1-β₁)·g           # 方向
v = β₂·v + (1-β₂)·g²          # 幅度
θ = θ - lr · m / (√v + ε)
\`\`\`

关键在除以 \`√v\`：**梯度一直很大的参数，步长被自动调小；梯度很小的参数，步长被放大**。这叫自适应学习率。

你可以把它理解成「穷人的二阶方法」—— 真正的二阶方法（牛顿法）需要 Hessian 矩阵求逆，参数量 d 时是 O(d³) 复杂度，70B 模型完全不可能。Adam 用一个对角近似换来了大部分好处。

**AdamW**：修正了 Adam 里权重衰减的实现。

Adam 把 L2 正则加进梯度里：\`g ← g + λθ\`，但这个惩罚项随后会被 \`1/√v\` 缩放，导致梯度大的参数实际受到的正则反而更弱 —— 正则失真了。

AdamW 把衰减**解耦**出来，直接作用在参数上：

\`\`\`
θ = θ - lr·(m̂/(√v̂+ε) + λ·θ)
\`\`\`

泛化更好、超参更好调，是所有 Transformer 训练的默认选择。

**LLM 训练的典型配置**（记下来，面试能报出来很加分）：
\`\`\`
优化器: AdamW,  β=(0.9, 0.95),  weight_decay=0.1
调度:   线性 warmup 前 1~2% 步数 → 余弦退火到峰值的 10%
裁剪:   clip_grad_norm = 1.0
\`\`\`

**一个重要的认知**：损失曲面是高度非凸的，有无数局部极小值。但实践发现深度网络的**大部分局部极小值效果都差不多好**，真正的麻烦是**鞍点**（某些方向上升、某些方向下降的平坦区域）。动量和自适应学习率主要就是帮助逃离鞍点的。`}
],
quiz: [
{id:"m3q1", type:"num", q:"求 f(x) = 3x² + 2x - 5 在 x = 2 处的导数值。",
hint:"先逐项求导：3x² 的导数是 6x，2x 的导数是 2，常数 -5 的导数是 0。得到 f'(x) 后代入 x=2。", ans:14,
why:"f'(x) = 6x + 2，代入 x=2 得 6×2 + 2 = **14**。\n\n导数是 14 意味着：在 x=2 这一点，x 每增加一点点，f 就增加大约 14 倍那么多。斜率很陡，而且是正的（上坡）。\n\n如果这是个损失函数，梯度下降会让 x **减小**（往梯度反方向走）。"},

{id:"m3q2", type:"num", q:"f(x, y) = x²y + 3xy²，求 ∂f/∂x 在点 (1, 2) 处的值。",
hint:"对 x 求偏导时，把 y 完全当成常数。x²y 对 x 求导得 2xy；3xy² 对 x 求导得 3y²（因为 3y² 是常数系数）。", ans:14,
why:"∂f/∂x = 2xy + 3y²\n\n代入 (1,2)：2×1×2 + 3×4 = 4 + 12 = **14**\n\n求偏导的关键就一句话：**盯住一个变量，其余全部视为常数**。\n\n顺带练一下另一个方向：∂f/∂y = x² + 6xy，在 (1,2) 处是 1 + 12 = 13。梯度向量就是 [14, 13]。"},

{id:"m3q3", type:"num", q:"用链式法则求 y = (2x + 1)³ 的导数在 x = 1 处的值。",
hint:"设 u = 2x+1，则 y = u³。dy/du = 3u²，du/dx = 2。相乘后代入 x=1（此时 u=3）。", ans:54,
why:"dy/dx = 3u² × 2 = 6(2x+1)²\n\n代入 x=1：6 × (3)² = 6 × 9 = **54**\n\n链式法则的机械步骤：**从外往里剥，每剥一层乘一次导数**。\n\n这正是反向传播在做的事 —— 神经网络就是几百层的复合函数，反向传播就是从损失开始一层层往回剥，把局部导数连乘起来。"},

{id:"m3q4", type:"num", q:"手算梯度下降：损失 L = (wx - y)²，已知 x=3, y=12, 当前 w=2, 学习率 lr=0.05。求一步更新后的新 w 值。",
hint:"三步走：① 算预测 ŷ=wx 和梯度 ∂L/∂w = 2(wx-y)·x　② 代入数字算出梯度　③ w_new = w - lr×梯度。注意梯度是负数时 w 会变大。", ans:5.6, tol:0.01,
why:"**① 前向**：ŷ = 2×3 = 6，真实值 12，L = (6-12)² = 36\n\n**② 梯度**：∂L/∂w = 2(wx - y)·x = 2×(6-12)×3 = 2×(-6)×3 = **-36**\n\n**③ 更新**：w_new = 2 - 0.05×(-36) = 2 + 1.8 = **5.6**\n\n**验算损失确实下降了**：ŷ = 5.6×3 = 16.8，L = (16.8-12)² = 23.04 < 36 ✓\n\n注意这一步「冲过头」了（最优解是 w=4，我们跳到了 5.6）。这说明学习率偏大，下一步梯度会变成正的把 w 拉回来，形成震荡。**这就是学习率过大时来回横跳的直观演示** —— 如果再大一些，比如 lr=0.2，你会发现它会越震越远直接发散。"},

{id:"m3q5", type:"mc", q:"某网络有 40 层，反向传播时每层的局部梯度约为 0.7。那么传到第一层时，梯度大约被缩放了多少倍？",
choices:["约 0.7 倍","约 28 倍","约 6×10⁻⁷ 倍","基本不变"], ans:2,
hint:"梯度是连乘的：0.7 的 40 次方。0.7¹⁰ ≈ 0.028，那么 0.7⁴⁰ = (0.7¹⁰)⁴ ≈ 0.028⁴。",
why:"0.7⁴⁰ ≈ **6×10⁻⁷**，也就是百万分之六。\n\n这就是**梯度消失**：底层网络收到的梯度信号小到几乎为 0，参数根本不更新，等于这些层白搭。\n\n**解法（按重要性排序）：**\n1. **残差连接** —— \`y = x + F(x)\` 求导得 \`1 + F'(x)\`，那个 +1 让梯度有一条不衰减的通路。这是最关键的一招。\n2. **归一化层**（LayerNorm / RMSNorm）稳住激活分布\n3. **ReLU 系激活** 替代 Sigmoid（Sigmoid 的导数最大只有 0.25，天生就在制造梯度消失）\n4. **合理初始化**（He / Xavier）让初始方差接近 1\n\n反过来，如果每层是 1.5，1.5⁴⁰ ≈ 1.1×10⁷ —— **梯度爆炸**，这时靠梯度裁剪（clip_grad_norm）来治。"},

{id:"m3q6", type:"mc", q:"关于 Adam 优化器，下面哪个说法**不正确**？",
choices:[
"它同时维护梯度的一阶矩（动量）和二阶矩",
"除以 √v 的作用是让梯度大的参数步长变小、梯度小的参数步长变大",
"AdamW 把权重衰减解耦出来，直接作用在参数上而不是加进梯度",
"Adam 需要计算并存储 Hessian 矩阵，所以显存开销是 SGD 的三倍"], ans:3,
hint:"Hessian 是二阶导数矩阵，对 d 个参数来说大小是 d×d。想想 70B 模型的 d 有多大，这个矩阵存得下吗？Adam 存的到底是什么？",
why:"**第四个说法是错的。**\n\nAdam **不计算 Hessian**。它存的是两个和参数**等长的向量**（m 和 v），不是 d×d 的矩阵。真正的 Hessian 对 70B 模型来说是 70B×70B，宇宙里也放不下。\n\nAdam 只是用「梯度平方的滑动平均」做了一个极其廉价的**对角近似**，所以被称为「穷人的二阶方法」。\n\n**显存账（面试常考）**：用 Adam 全参微调时，每个参数需要\n- 参数本身 FP32：4 字节\n- 梯度：4 字节\n- 动量 m：4 字节\n- 二阶矩 v：4 字节\n\n合计 **16 字节/参数**。7B 模型光优化器状态就要 112GB —— 这就是为什么必须用 LoRA、ZeRO 或梯度检查点。\n\n其余三个说法都正确。"},

{id:"m3q7", type:"open", q:"解释：为什么训练大模型时要用 learning rate warmup？如果不用会发生什么？",
hint:"想两件事：① 训练刚开始时参数是随机的，梯度方向可靠吗？② Adam 的二阶矩 v 是滑动平均，刚开始只积累了几个样本，用它去除会怎样？",
ref:"**两个原因：**\n\n**① 初期梯度方向不可靠。** 参数是随机初始化的，模型对数据一无所知，此时算出的梯度噪声极大、方向未必正确。用大学习率意味着朝着一个可能错误的方向迈一大步，容易把模型推进很差的参数区域，后面很难恢复。\n\n**② Adam 的二阶矩估计不稳。** 更新式里要除以 \`√v\`，而 v 是梯度平方的滑动平均。训练刚开始 v 只积累了极少的样本，估计值方差很大 —— 如果某一步 v 碰巧很小，\`1/√v\` 就会变得极大，产生一次巨大的参数更新，直接把训练打崩（loss 变 NaN）。\n\n**不用 warmup 的后果**：轻则 loss 曲线剧烈震荡、收敛变慢，重则训练直接发散。模型规模越大、batch 越大，这个问题越严重。原始 Transformer 用 Post-LN 结构时，**不加 warmup 基本必然发散**。\n\n**做法**：前 0.5%~2% 的训练步数，学习率从 0（或一个很小的值）线性升到峰值，之后再按余弦退火衰减。\n\n**能多说的加分点：**\n- Pre-LN 结构对 warmup 的依赖比 Post-LN 小得多，这是现代 LLM 全部转向 Pre-LN 的重要原因之一。\n- 有些工作（如 RAdam）试图从修正二阶矩方差的角度自动实现 warmup 的效果。\n- 现在流行的 **WSD 调度**（Warmup-Stable-Decay）把中间段保持恒定学习率，好处是可以从稳定段的任意 checkpoint 续训，不用重跑。"}
]
};
